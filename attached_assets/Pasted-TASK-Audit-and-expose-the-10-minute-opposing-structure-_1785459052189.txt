TASK: Audit and expose the 10-minute opposing-structure alert suppression rule.

PROBLEM OBSERVED

The Main Brain appears to provide less decision detail than the prior dashboard, and valid-looking alerts may be suppressed whenever opposing market structure occurred within the previous 10 minutes.

Do not assume the rule is correct or incorrect.

First identify exactly how the current production system evaluates recent opposing structure and prove whether it is blocking alerts.

PRIMARY GOALS

1. Show the operator exactly why an alert is blocked.
2. Determine whether recent opposing structure is being used as:
   - a hard veto,
   - a score penalty,
   - a readiness gate,
   - a strategy-specific filter,
   - or only explanatory context.
3. Identify whether the 10-minute window uses event time, candle time, ingestion time, or server time.
4. Verify whether stale, failed, reclaimed, or superseded opposing structure continues blocking alerts incorrectly.

STRICT SAFETY SCOPE

This task begins as an AUDIT AND DIAGNOSTICS task.

Do not change trading behavior until the current behavior is fully traced and documented.

DO NOT modify:

- Databento ingestion
- Databento normalization
- Databento subscriptions
- Market-data cache
- Edge Score calculations
- Strategy formulas
- Strategy selection
- TradersPost
- Order execution
- Active-trade management
- Risk controls
- Learning weights
- Polling intervals
- Authentication
- Existing API field meanings

PART 1 — TRACE THE RULE

Search the full codebase for all logic involving:

- opposing structure
- opposite structure
- conflicting structure
- recent structure
- structure conflict
- BOS
- CHOCH
- structure age
- 10 minutes
- 600 seconds
- alert suppression
- actionable
- readiness gates
- veto
- cooldown

Document the complete production path:

Databento event
→ normalized structure event
→ structure state/cache
→ strategy candidate
→ scoring
→ readiness/actionable decision
→ alert creation or suppression
→ Main Brain API
→ Main Brain display

For every relevant function, provide:

- file path
- function name
- input fields
- output fields
- exact condition
- whether it changes score, verdict, or alert eligibility

PART 2 — ANSWER THESE QUESTIONS WITH CODE EVIDENCE

1. Does any opposing structure inside 10 minutes automatically suppress an alert?

2. Is the rule instrument-specific?

Example:

An opposing MGC structure event must never block MNQ, MES, or MYM.

3. Is the rule mode-specific?

SCALP and SWING must not accidentally share an inappropriate time window.

4. Is the rule direction-specific?

A bearish structure event should affect only a proposed Long candidate, and vice versa.

5. Which timestamp is used?

Confirm whether age is calculated from:

- exchange event timestamp
- Databento timestamp
- normalized candle timestamp
- server receipt timestamp
- database insertion timestamp

6. What happens when opposing structure is later invalidated?

Examples:

- price reclaims the structure level
- a newer same-direction BOS occurs
- a stronger CHOCH supersedes it
- the opposing move fails
- the market changes regime
- a new strategy setup forms after the event

7. Can an old structure event be repeatedly reused from cache?

8. Can duplicate events refresh the 10-minute timer incorrectly?

9. Does missing timestamp data default to “recent”?

10. Does the UI currently hide the actual blocking reason?

PART 3 — ADD READ-ONLY DIAGNOSTICS

Add diagnostics without changing the alert decision.

Expose an `opposing_structure` diagnostic object through the existing Main Brain data path.

Use existing API patterns and naming conventions.

Suggested shape:

{
  "detected": true,
  "direction": "BEARISH",
  "candidate_direction": "LONG",
  "event_type": "CHOCH",
  "instrument": "MGC",
  "event_time": "2026-07-30T20:21:32-04:00",
  "age_seconds": 412,
  "window_seconds": 600,
  "status": "ACTIVE",
  "effect": "HARD_BLOCK",
  "source": "structure_cache",
  "reason": "Bearish CHOCH occurred 6m52s ago",
  "superseded": false,
  "invalidated": false
}

Use null or explicit unavailable values when evidence is missing.

Do not fabricate values.

PART 4 — MAIN BRAIN INFORMATION RESTORATION

The Main Brain must show more detail about why the system is waiting.

In the VERDICT panel, add a clearly visible section:

BLOCKING ALERT

Example:

BLOCKING ALERT
Recent bearish CHOCH
Age: 6m 52s
Window remaining: 3m 08s
Effect: Hard block
Source: MGC structure cache

Also show:

- candidate direction
- most recent same-direction structure
- most recent opposing structure
- whether the opposing event is active, invalidated, or superseded
- exact rule preventing action
- remaining block time, when applicable

If no rule is blocking the alert, display:

NO HARD ALERT BLOCK

Do not merely show a generic “Waiting for structure.”

PART 5 — DECISION TIMELINE

Add the following events to the existing Decision Timeline when available:

- opposing structure detected
- opposing structure block activated
- opposing structure invalidated
- opposing structure superseded
- opposing structure block expired
- candidate became actionable
- alert emitted
- alert suppressed

Each event must include:

- timestamp
- instrument
- candidate direction
- structure direction
- event type
- reason
- source

Do not add fake timeline entries.

PART 6 — REPRODUCTION TESTS

Create deterministic tests for at least these cases:

CASE A
Long candidate with bearish CHOCH 2 minutes ago.
Expected current production behavior must be documented.

CASE B
Long candidate with bearish CHOCH 11 minutes ago.
Expected: no longer considered inside a 10-minute window.

CASE C
Long candidate with bearish CHOCH 8 minutes ago, followed by bullish BOS 1 minute ago.
Determine and document whether the bullish BOS supersedes the bearish event.

CASE D
Long candidate with bearish CHOCH 8 minutes ago that has been invalidated by price reclaim.
Determine and document current behavior.

CASE E
Long MGC candidate with bearish MNQ structure 2 minutes ago.
Expected: MNQ must not block MGC.

CASE F
Duplicate bearish event received repeatedly.
Verify duplicates do not incorrectly restart the timer unless they are genuinely new exchange events.

CASE G
Missing or malformed event timestamp.
Verify the system fails safely and exposes the missing evidence.

CASE H
Opposing structure exists, but the alert is blocked by another rule.
The UI must identify the actual blocking rule and not falsely blame structure.

PART 7 — REPORT BEFORE CHANGING LOGIC

After diagnostics and tests are complete, STOP.

Do not tune or remove the 10-minute rule yet.

Provide a report containing:

1. Exact production behavior
2. Exact function enforcing it
3. Whether it is a hard block or score modifier
4. Timestamp source
5. Whether invalidated events continue blocking
6. Whether newer same-direction structure supersedes it
7. Whether duplicate events restart the timer
8. Whether cross-instrument contamination is possible
9. Screenshots showing the new blocking detail
10. Recommended correction, if a defect is confirmed

VALIDATION

Run:

- focused opposing-structure tests
- existing strategy regression tests
- existing alert regression tests
- Main Brain regression tests
- TypeScript typecheck
- production build
- git diff --check

Do not fix unrelated failures.

FINAL RESPONSE REQUIRED

Provide:

- starting commit
- final commit
- files changed
- complete opposing-structure rule trace
- diagnostic schema added
- screenshots
- test results
- build results
- explicit confirmation that no trading decision behavior was changed
- recommended Phase 2 logic change, but do not implement it