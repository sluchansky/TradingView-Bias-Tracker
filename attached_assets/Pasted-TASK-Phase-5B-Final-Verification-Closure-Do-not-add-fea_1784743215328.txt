TASK: Phase 5B Final Verification Closure

Do not add features or change trading behavior.

The previous verification found and fixed three defects. We now need a clean, post-fix verification report and resolution of the remaining concerns.

1. CORRECT THE REPORT ACCURACY

Explicitly state:

* The original implementation failed Cases C, D, and J.
* Three fixes were made during the verification task.
* The final verdict applies only to the corrected implementation.
* full_analysis() was modified by adding a flag-gated trace-cache block.
* Do not claim that no existing functions were modified.

Provide the final post-fix git diff.

2. JUSTIFY OR CORRECT THE ROUTE DEVIATION

The requirement requested owner-only diagnostics exposure through /diagnostics-live.

The implementation created /decision-trace.

Choose one:

A. Move the trace into the owner-only /diagnostics-live response as originally specified.

OR

B. Keep /decision-trace, but document this as an intentional contract deviation and prove it provides equal or stronger owner-only protection.

Do not make both routes expose the trace.

3. PROVE ENDPOINT AUTHORIZATION

Prove that an unauthenticated user cannot retrieve decision traces.

Test all relevant access paths:

* Public production domain through the Express proxy
* Replit development URL, if one exists
* Direct Flask deployment URL, if reachable
* Local Flask port without the proxy

Return the HTTP status and response body for each unauthorized request.

Required result:

* Unauthorized requests must receive 401, 403, or 404.
* The trace contents must never be returned.

If the Flask route itself has no authentication and can be reached directly, mark this as FAILED and fix the authorization before proceeding.

4. REMOVE SILENT FAILURE

Replace:

except Exception:
pass

with non-fatal, rate-limited logging.

Requirements:

* Logging failure must not affect full_analysis().
* Do not log complete alert payloads, account details, broker data, or secrets.
* Add a test proving an adapter exception does not change the analysis result.
* Add a test proving the failure produces an observable log record.

5. VERIFY CACHE BOUNDS

Prove that the trace cache cannot grow without limit.

Either:

* Demonstrate that instrument keys are restricted to the fixed supported-instrument registry.

OR

* Replace the dictionary with a bounded cache.

Add a test that malformed or unknown instrument identifiers cannot create arbitrary cache keys.

6. PROVIDE COMPLETE TEST RESULTS

Run and report:

* Phase 5B test file
* Four golden suites
* Critical Brain tests
* Complete test suite

Include exact commands and totals:

* Passed
* Failed
* Skipped
* Xfailed
* Errors

Do not summarize the full suite only as “green.”

7. PROVIDE POST-FIX RUNTIME COMPARISON

Using the same deterministic fixture, compare flag OFF and flag ON.

Return a field-by-field comparison for:

* verdict
* strict_reason
* strict_score
* edge_score
* trade_plan
* alert_level
* conviction_tier
* is_actionable
* auto-execute eligibility
* broker send count
* Discord send count
* database write count

All must match, and all side-effect counts caused by the trace must be zero.

8. FINAL VERDICT

Use exactly one:

* VERIFIED — safe read-only implementation
* VERIFIED WITH CONCERNS — no behavior change, but cleanup remains
* NOT VERIFIED — insufficient proof
* FAILED — behavior, security, or isolation requirement failed

Do not deploy or publish as part of this task.