TASK: Dashboard Cleanup — Phase 1.7

OBJECTIVE

Remove one duplicate dashboard panel:

Panel: Unified Analyst Report
Panel ID: mod-report

The AI Decision Center already presents the consolidated decision view, and
mod-analyst contains a broader version of the same analyst reasoning.

MAKE ONLY THIS ONE CHANGE.

BEFORE EDITING

Inspect and report:

1. The exact file or files that will be modified.
2. The exact markup belonging to mod-report.
3. Every JavaScript function, DOM selector, CSS rule, category entry,
   navigation entry, and panel registry reference connected to mod-report.
4. Which d.analyst_report fields are also consumed by:
   - mod-ai-decision-center
   - mod-analyst
   - mod-brain
   - any API or export feature
5. Which rendering code is exclusive to mod-report.
6. Which data-generation or shared rendering code must remain.
7. Whether removing the panel would affect any backend calculation or status
   payload.

Then perform the smallest safe change.

REQUIREMENTS

1. Remove the standalone mod-report panel from the rendered dashboard.

2. Preserve:
- d.analyst_report in the /status response
- Analyst report generation
- Any data used by mod-ai-decision-center
- Any data used by mod-analyst
- Any data used by mod-brain
- Any shared formatting functions
- Any logs, exports, or diagnostics that depend on analyst_report

3. Remove mod-report from any:
- Dashboard markup
- Section navigation registry
- Panel category registry
- Advanced View visibility rules
- Panel lookup lists
- Dashboard inventory structures
- Client-side update loops that target only mod-report

4. Remove JavaScript and CSS only when used exclusively by mod-report.

5. Do not remove or rename:
- d.analyst_report
- analyst_report
- Analyst Mode
- AI Decision Center
- Main Brain

6. Do not change the content or logic of any remaining panel.

7. Do not:
- Move panels
- Resize panels
- Change navigation
- Redesign layout
- Change analyst logic
- Change confidence scoring
- Change verdict logic
- Perform unrelated cleanup

SUCCESS CRITERIA

- mod-report no longer exists in the rendered DOM.
- Unified Analyst Report is absent with Advanced View OFF.
- Unified Analyst Report is absent with Advanced View ON.
- AI Decision Center still displays its Decision content.
- Analyst Mode still works when enabled.
- Main Brain still shows its verdict and reasoning.
- d.analyst_report remains present in /status.
- No empty panel gap remains.
- No other panel changes.
- No new browser-console errors.

TESTING

1. Search the served HTML for:
   id="mod-report"

2. Confirm it is absent.

3. Load the dashboard with Advanced View OFF.

4. Confirm Unified Analyst Report does not appear.

5. Turn Advanced View ON.

6. Confirm it still does not appear.

7. Open AI Decision Center → Decision.

8. Confirm verdict, bias, confidence, and reasoning still populate.

9. Enable or open Analyst Mode if available.

10. Confirm Analyst Mode still renders normally.

11. Inspect /status and confirm analyst_report still exists.

12. Switch through all dashboard sections.

13. Check the browser console.

FINAL RESPONSE

Return:

- Files changed
- Exact markup removed
- Standalone JavaScript removed
- Shared analyst JavaScript preserved
- CSS, registry, and visibility references removed
- AI Decision Center test result
- Analyst Mode test result
- Main Brain test result
- /status analyst_report result
- Browser-console result
- Confirmation that no other panel was modified

Do not begin Phase 1.8.