TASK: Dashboard Cleanup — Phase 1.9

OBJECTIVE

Remove one duplicate dashboard panel:

Panel: Trade Memory
Panel ID: mod-memory

The AI Decision Center already contains a Memory tab that presents the same
trade-memory information.

MAKE ONLY THIS ONE CHANGE.

BEFORE EDITING

Inspect and report:

1. The exact file or files that will be modified.
2. The exact markup belonging to mod-memory.
3. Every JavaScript function, DOM selector, CSS rule, category entry,
   navigation entry, registry entry, and Advanced View reference connected to
   mod-memory.
4. Which d.trade_memory fields are also used by:
   - mod-ai-decision-center
   - Main Brain
   - confidence scoring
   - strategy selection
   - trade approval logic
   - learning systems
   - exports or diagnostics
5. Whether mod-memory contains any unique controls or details not available in
   AI Decision Center → Memory.
6. Which rendering code is exclusive to mod-memory.
7. Which backend data generation and shared logic must remain.

Then perform the smallest safe change.

REQUIREMENTS

1. Remove the standalone mod-memory panel from the rendered dashboard.

2. Preserve:
- d.trade_memory in the /status response
- Similar-trade matching
- Win-rate calculations
- Average R calculations
- Average hold calculations
- MFE and MAE calculations
- Failure-pattern detection
- Memory narrative generation
- AI Decision Center → Memory tab
- Main Brain references to trade memory
- Any trade-gating, scoring, or learning dependencies
- Any shared formatting functions
- Any exports or diagnostics

3. If mod-memory contains any information that is not currently visible in the
   AI Decision Center Memory tab:

Do not silently remove access to that information.

Report the missing details first.

Only remove the panel if the AI Decision Center already preserves the useful
information or the missing content is purely decorative.

4. Remove mod-memory from any:
- Dashboard markup
- Section navigation registry
- Panel category registry
- Advanced View visibility rules
- Panel lookup lists
- Dashboard inventory structures
- Client-side update loops used only by mod-memory

5. Remove JavaScript and CSS only when used exclusively by mod-memory.

6. Do not remove or rename:
- d.trade_memory
- trade_memory
- AI Decision Center Memory tab
- Similar-trade lookup logic
- Learning-engine dependencies
- Main Brain memory references

7. Do not modify any other panel.

8. Do not:
- Move panels
- Resize panels
- Change navigation
- Redesign layout
- Change memory calculations
- Change matching thresholds
- Change strategy logic
- Change confidence logic
- Perform unrelated cleanup

SUCCESS CRITERIA

- mod-memory no longer exists in the rendered DOM, unless Replit identifies
  useful unique information that has no safe replacement.
- Trade Memory is absent with Advanced View OFF.
- Trade Memory is absent with Advanced View ON.
- AI Decision Center → Memory still displays:
  - Similar trade count
  - Win rate
  - Average R
  - Average hold
  - MFE
  - MAE
  - Common failure
  - Memory narrative
- Trade-memory calculations still run normally.
- d.trade_memory remains present in /status.
- No empty panel gap remains.
- No other panel changes.
- No new browser-console errors.

TESTING

1. Search the served HTML for:
   id="mod-memory"

2. Confirm it is absent, unless Replit stops because unique useful content would
   be lost.

3. Load the dashboard with Advanced View OFF.

4. Turn Advanced View ON.

5. Confirm no standalone Trade Memory panel appears.

6. Open AI Decision Center → Memory.

7. Confirm all memory metrics and narrative populate correctly.

8. Inspect /status and confirm trade_memory still exists.

9. Verify one live or simulated decision can still access memory data.

10. Switch through all dashboard sections.

11. Check the browser console.

FINAL RESPONSE

Return:

- Files changed
- Exact markup removed
- Standalone JavaScript removed
- Shared trade-memory logic preserved
- Any unique information found in mod-memory
- Where that information remains accessible
- CSS, registry, and visibility references removed
- AI Decision Center Memory test result
- Trade-memory calculation test result
- /status trade_memory result
- Browser-console result
- Confirmation that no other panel was modified

Do not begin Phase 1.10.