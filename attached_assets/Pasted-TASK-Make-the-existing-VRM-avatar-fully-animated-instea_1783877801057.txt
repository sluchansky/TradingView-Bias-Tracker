TASK: Make the existing VRM avatar fully animated instead of remaining in a T-pose.

IMPORTANT:
Do not redesign the dashboard.
Do not replace the current VRM avatar.
Do not create fake movement using CSS.
Do not move the entire 3D canvas.
Animate the avatar’s actual humanoid skeleton and VRM expression system.

The current avatar successfully loads, but it remains in a T-pose. This means the model is being rendered without a working body animation clip. Fix that first.

PHASE 1 — INSPECT THE CURRENT IMPLEMENTATION

1. Find the component that loads and renders LordPiggington.vrm.
2. Confirm which libraries are currently being used:
   - three
   - @react-three/fiber
   - @react-three/drei
   - @pixiv/three-vrm
3. Confirm the VRM contains a humanoid skeleton.
4. Log whether these bones are found:
   - hips
   - spine
   - chest
   - upperChest
   - neck
   - head
   - leftUpperArm
   - leftLowerArm
   - leftHand
   - rightUpperArm
   - rightLowerArm
   - rightHand
   - leftUpperLeg
   - leftLowerLeg
   - leftFoot
   - rightUpperLeg
   - rightLowerLeg
   - rightFoot
5. Show a clear console error if the humanoid skeleton or any animation file fails to load.

PHASE 2 — REMOVE THE T-POSE

Create a proper animation controller for the VRM.

The avatar must automatically enter an IDLE animation after loading.

The idle animation must include:
- Arms lowered naturally beside the body
- Slight elbow bend
- Relaxed knees
- Subtle breathing in the chest
- Small weight shifts
- Occasional head movement
- Natural blinking

Do not manually rotate only the shoulders and call it an animation.

Use a real humanoid animation clip that affects the full skeleton.

PHASE 3 — ADD ANIMATION FILES

Create this folder if it does not exist:

/public/animations/

Support these animation files:

/public/animations/idle.fbx
/public/animations/talking.fbx
/public/animations/walk.fbx
/public/animations/point.fbx
/public/animations/thinking.fbx
/public/animations/wave.fbx

The system must still work when only idle.fbx is present.

If a file is missing:
- Do not crash
- Log which file is missing
- Fall back to idle

Use FBXLoader or the appropriate Three.js loader.

Because Mixamo animations use different bone names than VRM models, implement animation retargeting from the Mixamo skeleton to the VRM humanoid skeleton.

Do not assume an FBX animation will automatically work on a VRM.

PHASE 4 — CREATE A REUSABLE AVATAR ANIMATION CONTROLLER

Create a hook or service similar to:

useAvatarAnimations(vrm, animationState)

Supported animation states:

"idle"
"talking"
"walking"
"pointing"
"thinking"
"waving"

The controller must:

1. Load each animation only once.
2. Retarget it to the current VRM humanoid skeleton.
3. Create THREE.AnimationAction objects.
4. Crossfade smoothly between animations.
5. Prevent two full-body animations from fighting each other.
6. Return to idle after one-time animations finish.
7. Clean up mixers and listeners when the component unmounts.
8. Update the AnimationMixer every frame using useFrame.

Use approximately:
- 0.25 to 0.4 second crossfades
- LoopRepeat for idle, talking and walking
- LoopOnce for pointing, thinking and waving
- clampWhenFinished for one-time animations

PHASE 5 — CONNECT ANIMATION TO THE AI

Connect avatar movement to the existing AI speaking state.

Required behavior:

When the avatar is not speaking:
animationState = "idle"

When speech audio begins:
animationState = "talking"

When speech audio ends:
animationState = "idle"

When the AI discusses a chart level or market feature:
Temporarily play "pointing", then return to talking if audio is still playing, otherwise return to idle.

When the AI is processing a question:
Play "thinking".

When the user first opens the dashboard:
Play "waving" once, then return to idle.

Do not use hard-coded timers as the primary way to determine whether speech is active. Use the actual audio element or speech synthesis start, pause and ended events.

PHASE 6 — WALKING

Add a controlled walking system, but do not allow the avatar to wander freely across the dashboard yet.

Create predefined stage positions:

center
leftPanel
rightPanel
chartPanel

When a destination is requested:

1. Rotate the avatar toward the destination.
2. Switch to the walking animation.
3. Move the avatar root smoothly toward the target position.
4. Keep both feet approximately aligned with the stage floor.
5. Stop within a small distance threshold.
6. Return to idle, talking or pointing depending on the active AI state.
7. Rotate the avatar toward the camera or selected panel after arriving.

Walking must change both:
- The avatar’s world position
- The leg and arm walking animation

Do not simply slide the avatar while its body remains idle.

Start with only small movements inside the avatar’s existing 3D area. Do not let it cover buttons, text or dashboard controls.

PHASE 7 — BLINKING AND LIP SYNC MUST REMAIN SEPARATE

Body animation must not break the existing facial animation.

The final frame update order should support:

1. AnimationMixer update
2. Head/eye tracking adjustments
3. Lip-sync expression updates
4. Blink expression updates
5. vrm.update(delta)

Make sure full-body animations do not overwrite mouth expressions.

PHASE 8 — ADD A DEVELOPMENT CONTROL PANEL

Add a small development-only animation test panel with these buttons:

Idle
Talk
Walk Left
Walk Center
Walk Right
Point
Think
Wave
Stop

This panel is for testing and can be hidden in production.

Each button must trigger the real animation controller.

Also display:

Current animation:
Previous animation:
Speech active:
Current destination:
Animation file loaded:
Mixer active:
Last animation error:

PHASE 9 — PERFORMANCE AND MOBILE REQUIREMENTS

This dashboard is used on a Tesla browser and mobile devices.

Requirements:

- Load animation files once
- Avoid creating a new AnimationMixer on every render
- Avoid React state updates on every animation frame
- Use refs for frame-by-frame animation values
- Cap device pixel ratio if needed
- Pause unnecessary processing when the page is hidden
- Do not create multiple WebGL canvases
- Preserve the current dashboard’s buttons and scrolling
- Do not allow the avatar canvas to intercept dashboard clicks unless clicking the avatar is intentional

PHASE 10 — ACCEPTANCE TESTS

Do not mark this complete until all of these work:

1. The avatar no longer remains in a T-pose after loading.
2. The avatar automatically enters a natural idle pose.
3. Arms are naturally lowered.
4. Breathing or subtle idle motion is visible.
5. The avatar blinks.
6. Pressing Talk moves the body and does not only move the mouth.
7. Speech automatically activates talking animation.
8. Speech ending returns the avatar to idle.
9. Pressing Walk Left makes the legs walk while the avatar moves left.
10. The avatar stops walking when it reaches the target.
11. Point plays once and returns to the correct previous state.
12. Missing animation files do not crash the dashboard.
13. Avatar body movement does not disable lip sync.
14. No existing trading logic, dashboard data or buttons are removed.
15. No major dashboard redesign is made.

IMPLEMENTATION ORDER:

Do not attempt everything at once.

Complete and test in this exact order:

STEP 1:
Get one idle animation working and eliminate the T-pose.

STEP 2:
Add smooth switching between idle and talking.

STEP 3:
Connect talking to the actual speech audio events.

STEP 4:
Add pointing, thinking and waving.

STEP 5:
Add walking between predefined positions.

After each step, report:
- Files changed
- What was implemented
- What was tested
- Any missing animation assets
- Any console errors
- What remains incomplete

Do not claim success based only on code compilation. Verify visible skeleton movement in the browser.