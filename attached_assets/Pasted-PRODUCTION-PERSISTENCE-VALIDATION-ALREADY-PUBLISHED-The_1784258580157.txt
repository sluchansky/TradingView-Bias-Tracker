PRODUCTION PERSISTENCE VALIDATION — ALREADY PUBLISHED

The persistence changes are already live.

Do not make additional code changes.
Do not republish again.
Do not tune trading logic.
Keep THESIS_ENFORCEMENT_MODE=shadow.

Immediately validate the current production deployment.

==================================================
1. PRODUCTION BOOT CHECK
==================================================

Capture the current production boot logs and report:

- deployment timestamp
- deployed commit
- database connection success
- market_state_cache table readiness
- restore summary counts
- any restore errors
- any JSON decode errors
- any database write errors
- any stale-state rejection logs
- any duplicate-state rejection logs

Confirm the application and dashboard are healthy.

==================================================
2. MONEY-PATH SAFETY CHECK
==================================================

Since the deployment is already live, verify immediately that boot restoration caused:

- zero TradersPost requests
- zero broker submissions
- zero new trades
- zero duplicate open trades
- zero journal entries
- zero new shadow evaluations
- zero new thesis transitions
- zero order-management actions

Check production logs from deployment start through the present.

If any unexpected money-path action occurred:

- stop
- disable the affected execution path using the existing safety control
- do not delete evidence
- report the exact timestamp and request

==================================================
3. RESTORED READY SAFETY
==================================================

Confirm from actual production behavior and code path that no restored READY state is actionable.

Report:

- whether any READY state was restored
- whether restored state has actionable=false or an equivalent guard
- whether Fast Entry was restarted
- whether an entry countdown restarted
- whether restored state could reach TradersPost

Do not rely only on tests. Include the live restore result.

==================================================
4. CHECK WHAT SURVIVED
==================================================

Compare the dashboard and server state after deployment with the state recorded before publish.

Verify individually:

- active thesis state
- latest analysis per instrument
- current bias
- current verdict
- confidence
- edge score
- active strategy
- CVD state
- volatility state
- Fast Entry state
- alert deduplication state
- TradersPost deduplication state
- auto-trade deduplication state
- open trades
- manual trades
- journal history
- shadow evaluations

For each classify:

- restored correctly
- intentionally reset
- not previously persisted
- not provable with current live evidence

Do not claim the problem is solved from the boot log alone.

==================================================
5. DATABASE WRITE CHECK
==================================================

Observe production for at least 15 minutes.

Determine whether market_state_cache writes occur on:

- every dashboard poll
- every status request
- every market-data refresh
- every repeated identical alert

Report:

- number of writes during the observation period
- state keys written
- duplicate or unchanged writes
- any database lock or latency errors
- estimated write rate per hour

If writes are happening continuously without state changes, do not change code yet. Report it as a production issue requiring correction.

==================================================
6. RESTART DEDUPLICATION CHECK
==================================================

Review the first alerts received after deployment.

Confirm they did not create duplicates because the in-memory deduplication state was empty before restore completed.

Report:

- first alert timestamp
- restore completion timestamp
- duplicate alert count
- duplicate shadow evaluation count
- duplicate thesis transition count
- duplicate journal count
- duplicate TradersPost request count

==================================================
7. LIVE USER CHECK
==================================================

Ask the owner to inspect the dashboard and identify whether the data that normally disappears is now visible.

Provide a concise checklist:

- previous thesis visible
- latest analysis visible
- confidence visible
- current strategy visible
- recent trade/journal history visible

Do not republish as part of this check.

==================================================
8. TESTS
==================================================

Report the exact 11 persistence tests already run.

Also report:

- PARITY
- SCALP GOLDEN
- DUAL-SIM
- BREAKOUT MODE
- all money-path tests

If money-path tests were not run, run them now without changing code.

==================================================
FINAL STATUS
==================================================

Return one of:

PERSISTENCE VALIDATION PASSED

or

PERSISTENCE VALIDATION FAILED

or

PERSISTENCE VALIDATION PARTIAL

Include:

- live restore counts
- unexpected actions
- duplicate counts
- write-rate findings
- user-visible state results
- unresolved risks

Do not begin another development phase.