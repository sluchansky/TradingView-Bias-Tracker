STOP BEFORE COMMITTING.

Perform the following Phase 5C final validation steps. Do not modify any files unless a validation command reveals a genuine Phase 5C defect.

1. Repository identity and status

Run:

git branch –show-current
git rev-parse HEAD
git status –short
git diff –check
git diff –stat
git diff –name-only

Report:

* Current branch
* Current HEAD
* Every modified, added, deleted, or untracked file
* Whether any unrelated files changed
* Whether git diff –check is clean

If HEAD changed unexpectedly during this task, stop and report the actual and expected HEAD before continuing.

2. Review the complete Phase 5C diff

Run:

git diff – <each Phase 5C modified file>

Inspect the full diff and confirm:

* Phase 5B behavior was not rewritten
* No execution, verdict, risk, sizing, entry, stop, target, or order-routing logic changed
* Phase 5C additions are diagnostics/display-only
* No global resolver behavior changed
* No silent fallback was added
* No existing API schema keys were removed or renamed
* Direction-aware labels are consumed correctly without double-remapping
* Existing SCALP output remains byte-identical
* No temporary debugging statements, commented-out code, placeholders, or test-only production branches remain

Explicitly list every production function changed and explain why each change is diagnostics-only.

3. Run the required validation suite again from the final working tree

Run and record exact pass counts and exit codes:

python3 artifacts/tradingview-webhook/test_decision_trace_phase5c.py
python3 artifacts/tradingview-webhook/test_decision_trace_phase5b.py

Then run the exact golden/parity/smoke commands used during implementation and report their complete results, including:

* Phase 5C test count
* Phase 5B test count
* SCALP golden byte-identical result
* PARITY result
* Breakout smoke result
* dual_sim smoke result

Do not summarize a command as passing unless it exits with code 0.

4. Run broader regression tests

Identify and run the repository’s normal Python test suite or all relevant tradingview-webhook test files.

Report:

* Total passed
* Total failed
* Total skipped
* Exact failure messages
* Whether each failure is new, pre-existing, or related to Phase 5C

Do not dismiss a failure as pre-existing without evidence from the parent commit or another clean baseline.

5. Validate the Phase 5C test quality

Inspect the new Phase 5C tests and report whether they cover:

* Long direction
* Short direction
* Neutral or missing direction
* Missing optional diagnostic data
* Malformed optional diagnostic data
* Edge-component labels
* Volatility details
* Gate-debug details
* Schema stability
* Adapter or formatter failure isolation
* No change to verdict/result behavior
* No regression to Phase 5B output

List the exact test names for each category.

6. Search for accidental behavioral changes

Search the Phase 5C diff for these terms:

entry
stop
target
risk
size
contract
order
submit
send
execute
verdict
action
WAIT
BUY
SELL
instrument_of
gate_debug
edge_breakdown
vol_diag

For every hit involving trading behavior, explain whether it is:

* Read-only formatting
* Test fixture data
* Existing unchanged context
* An actual behavioral change

Any actual trading-behavior change is out of scope and must be reported instead of committed.

7. Produce a final closure report

The report must include:

A. Repository state
B. Files changed
C. Production functions changed
D. Test files added or changed
E. Exact validation results
F. Golden/parity results
G. Broader regression results
H. Proof that Phase 5C is diagnostics-only
I. Known limitations or unresolved issues
J. Recommended commit message

Do not create the commit yet.

End with one of these exact conclusions:

READY TO COMMIT — Phase 5C is validated and diagnostics-only.

or

NOT READY TO COMMIT — followed by the exact blocking issues.