TASK: Phase 6B.2 — First Baseline Audit and Trustworthiness Review

OBJECTIVE

Audit the first official real-data baseline for realism, internal consistency,
data quality, fill assumptions, and statistical trustworthiness.

Baseline under review:

BL-20260726-042830-0cc8364

Reported baseline size:

- 40 matrix combinations
- 7,909 stored trades
- 4 instruments
- 5 strategies
- 2 modes

This task is audit-only.

DO NOT optimize parameters.

DO NOT change strategy logic.

DO NOT change live trading behavior.

DO NOT feed results into learning.

DO NOT promote or demote strategies.

DO NOT generate a replacement official baseline unless the audit proves that the
current baseline is invalid.

----------------------------------------------------
PART 1 — PRE-FLIGHT
----------------------------------------------------

Run:

git branch --show-current
git rev-parse HEAD
git log -7 --oneline
git status --short
git diff --check

Confirm:

- Phase 6B.1 is committed
- baseline BL-20260726-042830-0cc8364 exists
- 40 matrix rows exist
- 7,909 trade rows exist
- no task attachments are staged
- no historical data files are tracked by Git
- no source changes are currently uncommitted

Return:

- branch
- current HEAD
- Phase 6B.1 commit
- baseline ID
- matrix count
- trade count
- breakdown count

If the baseline record is incomplete or missing, stop.

----------------------------------------------------
PART 2 — BASELINE IMMUTABILITY
----------------------------------------------------

Verify that the baseline is immutable.

Confirm that:

- baseline configuration cannot be edited in place
- matrix results cannot be overwritten by a later run
- trade records are linked to the correct baseline
- breakdown records are linked to the correct baseline
- dataset IDs are fixed
- dataset fingerprints are fixed
- source commit is fixed
- configuration hash is fixed
- viewing the baseline does not recompute it
- GET endpoints do not alter stored values

Return:

- stored source commit
- stored configuration hash
- stored dataset IDs
- stored dataset fingerprints
- creation timestamp
- last-modified behavior, if any

If any baseline field can change after generation, classify that as a blocker.

----------------------------------------------------
PART 3 — TRADE COUNT AUDIT
----------------------------------------------------

Audit all 7,909 trades.

Return counts by:

- instrument
- mode
- strategy
- direction
- month
- weekday
- ET hour
- session
- contract
- exit reason

For every instrument/mode/strategy combination return:

- completed trades
- average trades per trading day
- maximum trades in one day
- median trades per day
- days with zero trades
- days with more than 10 trades
- days with more than 25 trades
- days with more than 50 trades

Flag combinations with suspiciously high trade frequency.

Do not assume high frequency is valid just because the engine completed.

----------------------------------------------------
PART 4 — DUPLICATE TRADE AUDIT
----------------------------------------------------

Search for exact and near-duplicate trades.

Exact duplicate key:

- baseline ID
- instrument
- mode
- strategy
- direction
- signal timestamp
- entry timestamp
- entry price
- stop price
- target price

Near-duplicate definitions:

1. Same instrument, mode, strategy, direction, and entry timestamp
2. Same instrument, mode, strategy, and signal timestamp
3. Same instrument and entry timestamp across multiple strategies
4. Same instrument, strategy, and entry price within the same minute
5. Same trade stored more than once with different matrix-result IDs

Return:

- exact duplicate count
- near-duplicate count for each definition
- representative examples
- whether duplicates are expected or erroneous

Do not delete anything during this audit.

----------------------------------------------------
PART 5 — OVERLAPPING TRADE AUDIT
----------------------------------------------------

Determine whether multiple trades are open simultaneously.

Audit overlaps by:

- same instrument and same strategy
- same instrument across different strategies
- same instrument across SCALP and SWING
- same strategy across different instruments
- all combinations globally

Return:

- number of overlapping trade pairs
- maximum simultaneous open trades
- average simultaneous open trades
- maximum simultaneous trades on one instrument
- percentage of trades that overlap another trade

Classify overlap policy:

- ALLOWED_BY_DESIGN
- UNINTENDED
- UNKNOWN

Trace the code that permits or blocks overlap.

Flag whether aggregate results assume unlimited capital or unlimited simultaneous
risk.

----------------------------------------------------
PART 6 — POSITION AND RISK ASSUMPTIONS
----------------------------------------------------

Document the exact historical risk model.

Return:

- risk unit definition
- whether each trade risks 1R independently
- whether portfolio capital is modeled
- whether account balance changes after each trade
- whether position sizing is modeled
- whether margin is modeled
- whether max contracts are modeled
- whether daily loss limits are modeled
- whether prop trailing drawdown is modeled
- whether correlated exposure is modeled
- whether multiple simultaneous trades consume shared buying power
- whether SCALP and SWING trades share one account

Clearly label the baseline as either:

- SIGNAL-LEVEL R MODEL
- PORTFOLIO-LEVEL MODEL

Do not present signal-level aggregate R as an achievable account equity curve.

----------------------------------------------------
PART 7 — ENTRY AND FILL REALISM
----------------------------------------------------

Audit entry assumptions.

Confirm:

- signal generated at bar close
- entry occurs at next bar open
- whether price gaps are accepted
- whether entry can occur beyond the intended setup level
- whether limit orders are modeled
- whether stop orders are modeled
- whether missed entries are modeled
- whether partial fills are modeled
- whether market liquidity is modeled
- whether spread is modeled
- whether slippage is modeled
- whether commissions are modeled
- whether contract-specific fees are modeled

Return counts for:

- entry gaps greater than 0.25R
- entry gaps greater than 0.50R
- entry gaps greater than 1.00R
- trades where next-bar open invalidated the original setup
- trades where entry opened beyond the target
- trades where entry opened beyond the stop
- trades with zero initial risk
- trades with negative initial risk

Flag any impossible or unrealistic fills.

----------------------------------------------------
PART 8 — STOP AND TARGET AUDIT
----------------------------------------------------

For every strategy and mode return:

- average stop distance in points
- median stop distance
- minimum stop distance
- maximum stop distance
- average target distance
- average planned R:R
- realized average R
- percentage stopped
- percentage targeted
- percentage exited by other reasons
- same-bar stop-and-target count
- same-bar conflict resolution

Confirm:

- ORB uses ATR-based stop
- ORB target is 4R
- original four strategies retain original management
- same-bar stop wins when stop and target are both touched
- stop and target prices are on the correct side of entry
- tick rounding is valid for each instrument

Flag:

- stops below minimum instrument floor
- targets with malformed geometry
- R:R values that do not match stored stop and target
- trades with target closer than stop when not intended
- trades with extreme stop distances

----------------------------------------------------
PART 9 — COMMISSION AND SLIPPAGE AUDIT
----------------------------------------------------

Return exact values used for:

- commission per contract
- exchange fees
- NFA fees, if applicable
- routing fees, if applicable
- slippage per side
- spread assumptions
- tick value
- contract multiplier

For each instrument calculate:

- gross Net R before costs
- commission impact
- slippage impact
- net Net R after modeled costs
- average cost per trade
- percentage of gross edge consumed by costs

If commissions or slippage are zero, explicitly state:

COSTS_NOT_MODELED

Produce a descriptive sensitivity table using:

- current settings
- 1 tick slippage per side
- 2 ticks per side
- 3 ticks per side

Do not change the stored baseline.

Do not generate a replacement baseline.

----------------------------------------------------
PART 10 — CONTINUOUS CONTRACT AND ROLL AUDIT
----------------------------------------------------

Audit the Databento continuous-contract handling.

Return:

- raw symbols included
- contract transition dates
- roll rule used
- whether the source was continuous or stitched locally
- whether prices were back-adjusted
- whether volume was combined
- whether overlapping contract bars existed
- whether duplicate timestamps occurred at rolls
- price gap at each roll
- largest roll gap
- trades opened across a roll boundary
- trades exited across a roll boundary

Flag whether contract rolls distort:

- ATR
- stops
- targets
- returns
- opening ranges
- VWAP
- volatility regimes

If no contract identifier exists on stored bars or trades, report that clearly.

----------------------------------------------------
PART 11 — SESSION AND TIMEZONE AUDIT
----------------------------------------------------

Verify:

- source timestamps
- UTC normalization
- America/New_York conversion
- daylight-saving transitions
- session labels
- 08:00–08:30 ET OR construction
- midnight session reset
- weekend handling
- holiday handling
- early closes
- market-maintenance breaks

Return:

- trades by ET hour
- trades during expected session windows
- trades outside expected windows
- trades during maintenance periods
- trades during weekends
- trades during known market closures
- ORB trades before OR completion
- ORB trades after intended ORB session window

Any trade during an impossible market period is a blocker.

----------------------------------------------------
PART 12 — LOOK-AHEAD AND CAUSALITY AUDIT
----------------------------------------------------

Run direct runtime tests using real imported data.

Prove that mutating bars after a signal cannot change:

- prior signal direction
- prior OR high
- prior OR low
- prior ATR
- prior volume confirmation
- prior entry
- prior stop
- prior target
- prior exit decision before the mutated bar
- prior metrics

Use representative long, short, and no-signal cases.

Return exact test names and results.

Do not rely only on static code inspection.

----------------------------------------------------
PART 13 — TRADE LIFECYCLE AUDIT
----------------------------------------------------

For every trade verify:

- one signal
- one entry
- one exit
- exit timestamp is after entry timestamp
- exit reason exists
- realized R reconciles to prices
- holding time reconciles to timestamps
- no trade exits before it enters
- no trade remains open past dataset end without incomplete status
- incomplete trades are not counted as completed
- wins, losses, and breakevens reconcile

Return null/error counts for each lifecycle rule.

----------------------------------------------------
PART 14 — METRIC RECONCILIATION
----------------------------------------------------

Recalculate matrix metrics directly from stored trades.

For each of the 40 combinations reconcile:

- completed trade count
- wins
- losses
- breakevens
- win rate
- gross positive R
- gross negative R
- net R
- average R
- median R
- expectancy
- profit factor
- maximum drawdown
- longest winning streak
- longest losing streak
- average holding time
- median holding time
- long Net R
- short Net R

Return mismatch counts and maximum absolute differences.

Expected:

0 material mismatches

Define the numeric tolerance used.

----------------------------------------------------
PART 15 — AGGREGATE METRIC AUDIT
----------------------------------------------------

Audit the baseline-level aggregate figures.

Confirm whether aggregate metrics were calculated by:

- summing matrix summaries
- concatenating all trades
- averaging matrix metrics
- weighted averaging
- another method

Verify:

- total trades reconcile to 7,909
- total wins reconcile
- total losses reconcile
- gross positive R reconciles
- gross negative R reconciles
- Net R reconciles
- aggregate expectancy is weighted correctly
- aggregate profit factor is calculated from gross totals
- maximum drawdown method is clearly labeled

Do not report a combined maximum drawdown as portfolio drawdown unless the
baseline models one shared chronological portfolio.

----------------------------------------------------
PART 16 — EXTREME RESULT AUDIT
----------------------------------------------------

Identify the most extreme combinations by:

- highest trade count
- highest win rate
- highest profit factor
- highest Net R
- lowest Net R
- highest expectancy
- lowest expectancy
- largest drawdown
- smallest drawdown
- longest winning streak
- longest losing streak

For each extreme result return:

- combination
- sample size
- value
- possible explanation
- audit status

Flag results with:

- win rate above 80% with at least 20 trades
- profit factor above 3.0 with at least 20 trades
- no losing trades
- no winning trades
- expectancy above 1R
- unusually low drawdown relative to trade count
- unusually high trades per day

Do not describe extreme positive results as proven edge.

----------------------------------------------------
PART 17 — DATA QUALITY AUDIT
----------------------------------------------------

For each dataset return:

- total bars
- expected bars
- missing intervals
- duplicate timestamps
- invalid OHLC rows
- negative volume rows
- zero-volume rows
- largest gap
- median spacing
- timeframe consistency
- OR coverage
- session coverage
- roll transitions
- warnings

Classify each:

- READY
- READY_WITH_WARNINGS
- REJECTED

If any official dataset should be REJECTED, classify the baseline as blocked.

----------------------------------------------------
PART 18 — DASHBOARD AUDIT PANEL
----------------------------------------------------

Add a read-only section under the real historical baseline panel:

BASELINE AUDIT

Display:

- audit status
- duplicate trade count
- overlap rate
- maximum simultaneous positions
- trade frequency warnings
- fill warnings
- cost-model status
- roll warnings
- session/timezone warnings
- metric reconciliation status
- data-quality status
- blocker count
- warning count
- audit timestamp

Use labels:

- PASS
- PASS_WITH_WARNINGS
- FAIL

Do not alter strategy rankings or live behavior.

----------------------------------------------------
PART 19 — AUDIT API
----------------------------------------------------

Add owner-protected research-only endpoints:

POST /backtest/baselines/<baseline_id>/audit

Runs and stores one immutable audit version.

GET /backtest/baselines/<baseline_id>/audits

Lists audit versions.

GET /backtest/baselines/<baseline_id>/audits/<audit_id>

Returns the complete audit.

Requirements:

- owner-only
- no Databento download
- no baseline mutation
- no live-state mutation
- no execution
- no TradersPost
- no learning updates
- no Decision Quality updates

Each audit must store:

- audit ID
- baseline ID
- source commit
- audit timestamp
- audit rules version
- findings
- warnings
- blockers
- final audit status

----------------------------------------------------
PART 20 — TESTS
----------------------------------------------------

Add focused Phase 6B.2 tests for at least:

1. Exact duplicate detection
2. Near-duplicate detection
3. Overlap detection
4. Maximum simultaneous position calculation
5. Trade-per-day calculation
6. Gap-entry calculation
7. Impossible-fill detection
8. Stop geometry validation
9. Target geometry validation
10. R:R reconciliation
11. ORB ATR stop validation
12. ORB 4R validation
13. Same-bar stop-wins validation
14. Commission reconciliation
15. Slippage sensitivity
16. Contract-roll detection
17. Weekend-trade detection
18. Maintenance-window detection
19. OR-completion validation
20. Real-data causality long
21. Real-data causality short
22. Real-data causality no-signal
23. Trade lifecycle reconciliation
24. Realized-R reconciliation
25. Matrix metric reconciliation
26. Aggregate metric reconciliation
27. Drawdown labeling
28. Extreme-result warnings
29. Dataset quality classification
30. Audit immutability
31. New audit creates new audit ID
32. Baseline remains unchanged
33. Audit does not call Databento
34. Audit does not call TradersPost
35. Audit does not alter live state
36. Audit does not alter learning
37. Audit does not alter Decision Quality
38. GET audit endpoints are read-only
39. POST audit endpoint is owner-protected
40. Dashboard script passes node --check

Aim for at least 50 focused tests.

----------------------------------------------------
PART 21 — VALIDATION
----------------------------------------------------

Run separately:

- Phase 6B.2 audit tests
- Phase 6B.1 baseline tests
- Phase 6B.0 acquisition/import tests
- Phase 6A.1 parity tests
- historical import tests
- ORB adapter tests
- coverage audit tests
- Stop Parity tests
- Decision Quality tests
- Strategy Scan tests
- all other backtest-related tests
- all four golden workflows

Report each suite separately.

Do not call this the full repository suite unless the full suite is run.

----------------------------------------------------
PART 22 — RUN THE FIRST AUDIT
----------------------------------------------------

Run and store one audit for:

BL-20260726-042830-0cc8364

Return:

- audit ID
- audit rules version
- total blockers
- total warnings
- duplicate count
- overlap count
- overlap rate
- maximum simultaneous positions
- impossible-fill count
- lifecycle-error count
- metric-mismatch count
- data-quality statuses
- cost-model status
- roll-policy status
- timezone/session status
- final audit status

Final audit status must be:

- PASS
- PASS_WITH_WARNINGS
- FAIL

Do not repair findings during the same audit task.

----------------------------------------------------
PART 23 — PROTECTED BOUNDARIES
----------------------------------------------------

Confirm no changes to:

- live Databento ingestion
- live subscriptions
- Databento normalization
- live strategy scorers
- Edge Score
- verdicts
- gates
- Decision Quality
- learning
- sizing
- live trade management
- execution
- TradersPost
- prop controls
- research strategy registry

Confirm no audit function is called from the live alert-processing path.

----------------------------------------------------
PART 24 — COMMIT
----------------------------------------------------

Commit only:

- audit engine
- audit schema
- audit API
- read-only audit dashboard
- tests

Do not commit:

- market data
- audit exports
- raw trade exports
- database dumps
- secrets
- task attachments

Suggested commit message:

Add first-baseline audit system

Return:

- starting HEAD
- ending commit
- exact files committed
- final git status
- untracked files
- test totals

----------------------------------------------------
PART 25 — FINAL STATUS
----------------------------------------------------

Return exactly one:

PHASE_6B_BASELINE_AUDIT_PASS

PHASE_6B_BASELINE_AUDIT_PASS_WITH_WARNINGS

PHASE_6B_BASELINE_AUDIT_FAIL

Use FAIL if any blocker affects causal correctness, data integrity, trade
lifecycle, metric correctness, or impossible market behavior.

----------------------------------------------------
PART 26 — FINAL REPORT
----------------------------------------------------

Return:

1. Branch
2. Starting commit
3. Ending commit
4. Baseline ID
5. Audit ID
6. Files changed
7. Schema changes
8. Duplicate findings
9. Overlap findings
10. Risk-model classification
11. Fill-model findings
12. Stop/target findings
13. Cost-model findings
14. Contract-roll findings
15. Session/timezone findings
16. Causality results
17. Lifecycle reconciliation
18. Matrix metric reconciliation
19. Aggregate reconciliation
20. Extreme-result warnings
21. Dataset quality
22. Dashboard status
23. API status
24. Test counts
25. Golden results
26. Protected-boundary proof
27. Blockers
28. Warnings
29. Final audit status
30. Final Phase 6B.2 status

After returning the report, stop.

DO NOT START PHASE 6C.

DO NOT OPTIMIZE ANY STRATEGY.