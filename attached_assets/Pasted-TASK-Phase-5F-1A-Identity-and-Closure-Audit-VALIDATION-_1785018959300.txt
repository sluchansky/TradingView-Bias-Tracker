TASK: Phase 5F.1A — Identity and Closure Audit

VALIDATION FIRST.

Do not begin additional backtest work.
Do not modify scoring, execution, Databento, TradingView, or strategy logic.

OBJECTIVE

Resolve the remaining ambiguity in Decision Quality setup identity and audit the
already-existing backtest expansion commit.

PART 1 — AUDIT TASK ORDER

Starting HEAD for Phase 5F.1 was:

c8e8e3f — "Expand backtesting to include more live trading strategies"

Return a read-only report for commit c8e8e3f:

- Parent commit
- Exact files changed
- Strategies added
- Live strategies considered
- Strategies excluded
- Historical inputs used by every added strategy
- Proof no fabricated inputs or look-ahead were introduced
- Exact focused and regression test results
- Proof the original four-strategy results remained unchanged

Do not modify the backtester during this audit.

PART 2 — PROVE FINGERPRINT UNIQUENESS

Current fingerprint:

"{instrument}::{direction}::{mode}"

Determine whether two distinct setups can share this value.

Test at minimum:

1. Two strategies, same instrument/direction/mode
2. Two distinct entry prices, same instrument/direction/mode
3. Two distinct stop/target plans, same instrument/direction/mode
4. Two READY candidates in the same evaluation cycle
5. Sequential READY setups without an intervening WAIT
6. Multiple active candidates for the same lane

If any distinct setups collide, report the stop condition before modifying code.

PART 3 — DESIGN A TRUE SETUP IDENTITY

A true diagnostics identity should prefer:

- Existing trade ID
- Existing setup ID
- Existing candidate ID
- Existing alert/event ID

Otherwise construct a deterministic fingerprint from stable fields such as:

- instrument
- direction
- mode
- strategy key
- entry
- stop
- targets
- originating event timestamp or setup timestamp

Do not use wall-clock capture time as the sole identity.

Repeated heartbeats of the same setup must produce the same fingerprint.

Distinct setups must produce different fingerprints.

PART 4 — OUTCOME MATCHING AUDIT

Audit the fallback logic that scans entries and uses the first match.

Prove whether it can select the wrong snapshot when multiple pending entries
share an instrument or direction.

The required matching hierarchy is:

1. Exact snapshot key stored with the managed trade
2. Trade ID or setup ID
3. Exact full fingerprint
4. Unmatched closure

Do not use arbitrary first-match selection when multiple candidates exist.

If no exact match exists:

- Increment unmatched closure diagnostics
- Leave rows unresolved or mark them abandoned safely
- Do not guess

PART 5 — TTL AUDIT

Current TTL is 240 minutes.

Evaluate separately:

- READY candidate not traded
- SCALP active trade
- SWING active trade

An active trade must not lose its analytics association merely because it remains
open longer than the candidate TTL.

Report whether active trades are distinguishable from untraded candidates.

Recommended model:

- Short TTL for abandoned/untraded READY candidates
- No ordinary TTL for an identity linked to an active trade, or a substantially
  longer bounded retention appropriate for Swing
- Explicit cleanup after closure

Do not change hold duration or trade management.

PART 6 — WAIT ABANDONMENT SAFETY

Prove that a WAIT result cannot abandon the analytics reference for a trade that
has already opened.

Test:

READY
→ snapshot captured
→ trade opens
→ later analysis returns WAIT while trade remains active
→ trade closes

Expected:

- The active trade’s snapshot remains resolvable
- WAIT cleanup only abandons untraded candidates
- Outcome attaches to the correct row

PART 7 — REQUIRED TESTS

Add or run tests proving:

1. Different strategies do not collide.
2. Different entry/stop/target plans do not collide.
3. Repeated heartbeat of the same setup deduplicates.
4. Two same-lane setups remain independently trackable.
5. No first-match ambiguity exists.
6. Ambiguous closure becomes unmatched rather than guessed.
7. WAIT does not abandon an active trade’s snapshot.
8. SCALP active trades survive candidate expiry.
9. SWING active trades survive beyond 240 minutes.
10. Exact outcome matching remains idempotent.
11. Scores, verdicts, learning, and execution remain byte-identical.
12. Databento ingestion remains unchanged.

STOP CONDITIONS

Stop and report before repair if:

- Two distinct setups share the current fingerprint
- First-match fallback can attach an outcome incorrectly
- WAIT can abandon an active-trade snapshot
- A valid Swing trade can expire before closure
- The backtest expansion added strategies without valid historical inputs
- Existing four-strategy backtest results changed unexpectedly

FINAL REPORT

Return:

- Finding for each stop condition
- Backtest commit c8e8e3f audit
- Fingerprint collision proof
- Outcome matching proof
- WAIT cleanup proof
- TTL findings
- Recommended minimal repair
- No additional enhancements

MAKE ONLY THIS AUDIT FIRST.