TASK: Diagnose and repair zero Databento bars for MGC without disturbing the working MNQ, MES, and MYM feeds.

CURRENT PRODUCTION SYMPTOM

Main Brain displays:

LEFT BRAIN THESIS — UNAVAILABLE
No thesis yet — awaiting first Databento bar-close scan
Source: MGC.c.0
Databento bars received: 0

This means the corrected Main Brain diagnostics are working, but the MGC data pipeline has produced zero completed bars.

PRIORITY

This is now a market-data pipeline investigation.

Do not attempt to hide the issue with a UI fallback.

Do not create synthetic bars.

Do not calculate a thesis from missing data.

STRICT INITIAL SCOPE

Begin audit-first.

DO NOT modify:

- strategy calculations
- Edge Score
- thesis formulas
- readiness
- alerts
- risk logic
- TradersPost
- execution
- learning
- Main Brain polling
- authentication

Do not change working MNQ, MES, or MYM behavior.

PART 1 — CAPTURE CURRENT LIVE STATUS

Using the currently deployed process, report for each instrument:

- requested Databento symbol
- resolved symbol
- dataset
- schema
- subscription start time
- subscription status
- records received
- trades received
- quotes received
- bars received
- last record timestamp
- last completed bar timestamp
- last error
- reconnect count

Compare:

- MGC
- MNQ
- MES
- MYM

Provide literal live values.

PART 2 — TRACE MGC SUBSCRIPTION CREATION

Trace the complete MGC subscription path:

application startup
→ enabled instrument list
→ canonical instrument mapping
→ Databento symbol selection
→ subscription request
→ record callback
→ instrument resolver
→ tick/bar accumulator
→ completed bar
→ _databento_bar_scan

For each step provide:

- file
- function
- expected MGC value
- actual live value
- whether the step ran
- diagnostic/log evidence

PART 3 — VERIFY SYMBOL VALIDITY

Inspect all symbol handling for:

- MGC
- MGC.c.0
- MGC.FUT
- active MGC contract
- raw symbol
- parent/root
- continuous contract
- instrument ID
- publisher ID
- dataset-specific symbol format

Answer precisely:

1. Is `MGC.c.0` valid for the configured Databento dataset and schema?
2. Does the subscription receive a symbol-mapping event?
3. Does the callback resolve Databento records back to canonical `MGC`?
4. Does the active contract have a different symbol?
5. Is MGC accidentally filtered because its raw symbol differs from the expected continuous symbol?

Do not use fuzzy matching.

PART 4 — VERIFY THE DATASET AND SCHEMA

Report the exact Databento:

- dataset
- schema
- stype_in
- stype_out
- start/end behavior
- live versus historical mode

Confirm that the selected schema actually produces the record type required by the bar builder.

Look for conditions such as:

- MGC subscribed with a quote-only schema while bars require trades
- bar builder ignores MBP records
- instrument resolver handles equity-index futures but not COMEX
- MGC records use a publisher or instrument class that is filtered out
- continuous symbol resolution failed
- subscription silently returned no matching symbols

PART 5 — CHECK RECORD FILTERING

Trace every filter between the Databento callback and the MGC bar accumulator.

Check for:

- instrument whitelist
- supported-instrument map
- symbol equality checks
- root-symbol parsing
- price validation
- zero-size filtering
- trade-action filtering
- record-type filtering
- session filtering
- timestamp filtering
- out-of-order filtering
- stale-record filtering
- duplicate filtering
- exchange/publisher filtering

For each filter, determine whether MGC records are being rejected.

Add temporary rate-limited counters such as:

MGC records received
MGC records accepted
MGC records rejected: symbol
MGC records rejected: schema
MGC records rejected: session
MGC records rejected: timestamp
MGC records rejected: invalid price
MGC records passed to bar builder
MGC bars completed

Do not log every market event individually.

PART 6 — CHECK BAR-BUILDER REQUIREMENTS

Document what creates a completed bar.

Determine whether MGC receives:

- no records at all
- records but no accepted trades
- accepted records but no bar bucket
- active bar that never closes
- bars built under the wrong instrument key
- bars stored but not counted by diagnostics

Verify:

- bucket duration
- timestamp source
- bucket-boundary calculation
- session/date rollover
- timezone behavior
- bar-close trigger
- first-bar behavior
- low-volume behavior

A low-volume instrument should still close a time bucket correctly once a valid record crosses the next bucket boundary.

PART 7 — PROCESS-RESTART AND SUBSCRIPTION STATE

Determine whether the zero count is caused by:

- the application restarting recently
- subscription starting during an illiquid period
- the MGC subscription failing after restart
- reconnect restoring only three of four instruments
- MGC omitted from a resubscribe list
- counters resetting while cached thesis data remains elsewhere

Report:

- process start time
- Databento connection time
- MGC subscription confirmation time
- last reconnect
- whether MGC is included after reconnect

PART 8 — DIRECT DATABENTO PROBE

Use the same credentials, dataset, schema, and symbol configuration in a minimal diagnostic probe.

Do not alter production state.

For a bounded recent period, request MGC data and report:

- whether records are returned
- exact resolved symbol
- record types
- first timestamp
- last timestamp
- record count

Then perform the same probe for one working instrument such as MNQ.

This determines whether the issue is:

A. upstream Databento availability/symbol selection
or
B. application filtering/bar construction.

Do not expose credentials in logs or the final report.

PART 9 — IMPLEMENT ONLY THE PROVEN DEFECT

After the failure point is proven, implement the narrowest correction.

Acceptable examples:

- correct MGC Databento symbol mapping
- include MGC in resubscription
- resolve MGC instrument IDs correctly
- accept the correct record schema
- fix MGC-specific root-symbol parsing
- ensure time bars close during low-volume periods
- correct diagnostic counting if bars already exist

Do not broadly rewrite Databento ingestion.

Do not change all instruments merely to fix MGC unless the shared defect is proven.

PART 10 — REQUIRED TESTS

CASE A
MGC continuous symbol resolves to canonical MGC.

CASE B
Active MGC contract resolves to canonical MGC.

CASE C
A valid MGC trade record reaches the bar builder.

CASE D
MGC records are not rejected by an equity-index-only filter.

CASE E
A low-volume MGC bucket closes correctly when the next timestamp bucket begins.

CASE F
Reconnect includes MGC.

CASE G
MNQ, MES, and MYM mappings remain unchanged.

CASE H
Unknown COMEX symbol does not incorrectly map to MGC.

CASE I
No record available.
Expected:
zero bars and explicit NO DATA, not a fabricated thesis.

CASE J
One completed MGC bar.
Expected:
bar count increments and Left Brain scan is invoked.

CASE K
Enough MGC observations.
Expected:
Left Brain thesis becomes AVAILABLE.

CASE L
Process restart.
Expected:
MGC subscription is restored exactly once.

PART 11 — LIVE VERIFICATION

After the narrow fix:

1. Restart the relevant services cleanly.
2. Confirm MGC subscription acknowledgement.
3. Confirm MGC record count increases.
4. Confirm MGC completed-bar count becomes greater than zero.
5. Confirm a Left Brain calculation attempt occurs.
6. Confirm Main Brain changes from:
   - UNAVAILABLE
   to either:
   - COLLECTING DATA
   - AVAILABLE
   - or an honest STALE state

Do not claim success merely because the UI component loads.

PART 12 — STOP CONDITIONS

Stop and report before changing code if:

- Databento returns zero MGC records in the direct probe
- the configured subscription does not support MGC
- MGC requires a different licensed dataset
- the symbol cannot be resolved deterministically
- fixing it would require replacing the established Databento pipeline
- the only proposed solution is synthetic market data

FINAL RESPONSE REQUIRED

Provide:

1. Starting commit
2. Final commit
3. Process and connection start times
4. Exact MGC subscription configuration
5. MGC versus MNQ record/bar comparison
6. Direct Databento probe result
7. Exact failure point
8. Root cause
9. Files changed
10. Narrow fix
11. Test results
12. Live MGC record count after fix
13. Live MGC bar count after fix
14. Left Brain status after fix
15. Confirmation that no synthetic bars were created
16. Confirmation that strategy, scoring, readiness, risk, alerts, execution, and other instruments were untouched