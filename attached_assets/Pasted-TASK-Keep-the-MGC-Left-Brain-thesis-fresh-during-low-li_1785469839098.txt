TASK: Keep the MGC Left Brain thesis fresh during low-liquidity periods without changing Databento ingestion or thesis formulas.

CONTEXT

The MGC thesis audit proved:

- MGC had only 1 Databento bar while MNQ/MES/MYM had approximately 127
- Left Brain thesis calculation currently depends on bar-close activity
- MGC can therefore remain stale for long periods overnight
- stale/no-data UI diagnostics are now working
- timestamp normalization has already been fixed

GOAL

Add a safe fallback evaluation path so the MGC Left Brain thesis can be reconsidered during low-tick periods, without fabricating market data and without changing the existing thesis methodology.

STRICT SCOPE

Allowed:

- scheduling an evaluation attempt from existing cached observations
- freshness diagnostics
- rate limiting
- fallback-trigger tests
- explicit insufficient-data outcomes

DO NOT modify:

- Databento subscriptions
- Databento event normalization
- bar construction
- synthetic bars
- thesis formulas
- thesis thresholds
- Edge Score
- strategy logic
- readiness
- alerts
- execution
- polling interval for Main Brain
- authentication

PART 1 — TRACE CURRENT TRIGGERING

Document exactly what currently calls:

- _databento_bar_scan
- compute_left_brain_thesis
- compute_left_brain_mi
- observation persistence
- thesis persistence

Confirm whether thesis computation only occurs after a completed bar or whether any other trigger exists.

PART 2 — DEFINE A SAFE FALLBACK TRIGGER

Add one rate-limited fallback evaluation attempt for instruments whose thesis is stale.

Preferred behavior:

- use existing cached observations only
- do not create or infer synthetic ticks or bars
- do not alter observation timestamps
- do not duplicate observations
- do not run more frequently than necessary
- preserve the normal bar-close path as primary

Suggested trigger conditions:

- instrument is enabled
- current thesis age exceeds the existing stale threshold
- no calculation attempt has occurred recently
- enough valid cached observations exist
- market/session state permits evaluation

Do not invent new freshness thresholds if an existing one is available.

PART 3 — INSUFFICIENT DATA BEHAVIOR

If cached observations are insufficient, do not output a fake neutral thesis.

Return or preserve an explicit state such as:

- COLLECTING_DATA
- NO_DATA
- STALE_LAST_KNOWN

Expose:

- observation count
- minimum required
- last data time
- last calculation attempt
- last successful calculation
- reason evaluation did not run

PART 4 — SESSION SAFETY

Verify the fallback respects:

- CME maintenance
- market closed
- weekend
- session transitions
- stale source data
- unsupported instruments

During maintenance or closed-market periods, do not repeatedly recompute from unchanged data.

PART 5 — DEDUPLICATION AND RATE LIMITING

Ensure:

- no overlapping thesis calculations
- no repeated persistence of the same unchanged thesis
- no timer accumulation
- no restart burst
- no duplicate observation writes
- normal bar-close calculation still takes precedence

Use an in-flight guard and last-attempt timestamp.

PART 6 — REQUIRED TESTS

CASE A
MGC thesis stale, enough cached observations.
Expected:
one fallback evaluation attempt.

CASE B
MGC thesis fresh.
Expected:
no fallback attempt.

CASE C
MGC stale but insufficient observations.
Expected:
explicit COLLECTING_DATA or STALE state, no fake neutral.

CASE D
Maintenance window.
Expected:
no repeated calculation.

CASE E
Market closed.
Expected:
no repeated calculation.

CASE F
Fallback and bar-close trigger occur together.
Expected:
one calculation only.

CASE G
Repeated scheduler cycles.
Expected:
rate limit prevents duplicate attempts.

CASE H
Evaluation result unchanged.
Expected:
no duplicate persistence event.

CASE I
MNQ/MES/MYM remain unchanged.

CASE J
Restart.
Expected:
no burst and prior timestamps respected.

PART 7 — DIAGNOSTICS

Extend the existing Left Brain diagnosis object with:

- calculation_trigger: BAR_CLOSE / STALE_FALLBACK / NONE
- last_attempt_at
- last_success_at
- fallback_eligible
- fallback_blocked_reason
- observations_available
- observations_required

Do not redesign the panel again.

PART 8 — VALIDATION

Run:

- focused fallback-scan tests
- MGC thesis tests
- Left Brain regressions
- observation-buffer tests
- Databento integration tests
- Main Brain regressions
- production build
- TypeScript typecheck
- git diff --check

FINAL RESPONSE REQUIRED

Provide:

1. Starting commit
2. Final commit
3. Current normal thesis trigger
4. New fallback trigger
5. Rate-limit behavior
6. MGC observation requirements
7. Session guards
8. Files changed
9. Tests
10. Build and TypeScript status
11. Proof that no synthetic market data was created
12. Confirmation that Databento ingestion, thesis formulas, scoring, alerts, execution, and Main Brain polling were unchanged