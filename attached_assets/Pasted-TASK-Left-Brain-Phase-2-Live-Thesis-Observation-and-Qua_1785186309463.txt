TASK: Left Brain Phase 2 — Live Thesis Observation and Quality Audit

OBJECTIVE

Validate the Phase 2 Dynamic Thesis using live Databento data while keeping it completely display-only.

Do not connect the thesis to:

* trade direction
* edge score
* confidence
* setup approval
* vetoes
* position sizing
* entry
* stop
* target
* trade management
* TradersPost
* broker execution

The purpose of this task is to determine whether the thesis is stable, accurate, useful, and timely enough to later advise the Right Brain.

STEP 1 — VERIFY COMMIT CONTENT

Run and report:

git status –short
git show –stat –oneline 64c8a25
git diff 64c8a25^ 64c8a25 –name-status
git diff 64c8a25^ 64c8a25 –check

Resolve the report mismatch between:

* “5 files changed”
* the four files listed in the prior report

Do not modify code unless an unexpected file or formatting problem is found.

STEP 2 — CONFIRM PRODUCTION FLAG STATE

Report the exact production value of:

LEFT_BRAIN_MARKET_INTELLIGENCE_ENABLED

Confirm the deployed process has reloaded the value.

Confirm:

GET /lb-thesis

returns:

* ok=true
* flag_enabled=true
* each actively scanned instrument
* a populated thesis after bar-close processing begins

Do not infer flag state from source code. Verify the running process.

STEP 3 — COLLECT LIVE THESIS SNAPSHOTS

For each supported active instrument:

* MGC
* MNQ
* MES
* MYM

Collect thesis snapshots at every Databento bar close.

For each snapshot store or log:

* timestamp
* instrument
* direction
* strength
* momentum
* established_at
* last_updated_at
* market_state
* session_character
* session_phase
* auction_control
* bullish probability
* bearish probability
* neutral probability
* data confidence
* top playbook
* top playbook fit score
* rapid_flip_warning
* time_in_current_thesis_min
* number_of_transitions
* input freshness
* selected VWAP source

Do not add database writes. Use bounded in-memory diagnostics or rate-limited structured logs.

STEP 4 — THESIS STABILITY REPORT

For each instrument calculate:

* total thesis observations
* bullish observations
* bearish observations
* neutral observations
* uncertain observations
* thesis transitions
* transitions per hour
* average thesis duration
* median thesis duration
* shortest thesis duration
* longest thesis duration
* rapid flip warnings
* A → B → A oscillations
* direction changes lasting less than 3 bars
* strength changes of 10 or more points
* momentum-state distribution

Report separately by market session.

STEP 5 — THESIS TIMELINESS CHECK

For each significant market transition, compare:

* timestamp when the underlying MI market state changed
* timestamp when thesis direction changed
* timestamp when thesis momentum changed
* timestamp when the top playbook changed

Report lag in:

* bars
* seconds

Flag cases where:

* thesis direction reacts more than 2 completed bars after the MI change
* momentum remains STABLE during a significant directional shift
* thesis stays directional after the supporting auction control reverses
* thesis strength remains above 70 after data confidence falls below 40

Do not tune the model during this task.

STEP 6 — INTERNAL CONSISTENCY CHECK

Validate every live thesis result:

* direction is from the canonical set
* strength is 0–100
* momentum is from the canonical set
* probabilities sum to exactly 100
* thesis instrument matches the scan instrument
* established_at is not later than last_updated_at
* timeline is newest-first
* timeline contains no more than 20 items
* market memory contains no more than 200 items
* playbook fit scores are 0–100
* no duplicate playbook names
* reasons and missing lists contain no blank strings
* weakens_if and fails_if are present
* no NaN
* no Infinity
* no malformed timestamps

Report every violation with the source snapshot.

STEP 7 — MEMORY QUALITY AUDIT

Inspect significant-transition memory for:

* duplicate events from the same bar
* repeated identical transitions
* false thesis-strengthened events
* false thesis-weakened events
* incorrect from_value or to_value
* incorrect confidence_at_time
* missing evidence
* stale evidence copied from an earlier thesis
* excessive DATA_CONFIDENCE_CHANGE events
* excessive PLAYBOOK_CHANGE events

Confirm memory remains isolated per instrument.

STEP 8 — RESTART BEHAVIOR

Perform one controlled application restart.

Before restart record:

* current thesis
* established_at
* memory count
* transition count

After restart confirm and document:

* in-memory thesis and memory reset
* cold-start response is valid
* first new thesis appears after the next completed Databento bar
* no stale thesis is shown from before restart
* trading and execution outputs remain unaffected during the cold-start period

Do not add persistence in this task.

STEP 9 — EXECUTION PARITY

While Phase 2 is enabled, capture and compare existing trade-decision fields against the same inputs with the feature disabled.

Compare:

* verdict
* strict_direction
* strict_reason
* edge_score
* confidence
* is_actionable
* selected strategy
* entry
* stop
* targets
* invalidation
* position size
* prop guard
* duplicate guard
* daily-loss guard
* trade-management output
* TradersPost payload

Expected result:

No differences caused by the Left Brain thesis.

Report a field-level diff.

STEP 10 — DASHBOARD VALIDATION

Verify the dashboard displays:

* thesis direction
* strength
* momentum
* established time
* narrative
* invalidation conditions
* playbooks
* stability
* timeline

Check:

* null cold-start state
* missing-data state
* long narrative text
* 20 timeline entries
* rapid-flip warning
* all four directions
* mobile layout
* no JavaScript console errors
* no repeated DOM elements
* no stale instrument data after switching instruments

STEP 11 — TEST AND HEALTH CHECK

Run:

* test_lb_phase2.py
* test_lb_shadow_validation.py
* test_left_brain_mi.py
* decision-trace tests
* scalp-quality tests
* TradersPost tests
* golden/parity suites
* full existing test suite

Report:

* passed
* failed
* skipped
* pre-existing failures
* newly introduced failures

Do not modify unrelated tests to force a passing result.

STEP 12 — FINAL RECOMMENDATION

Return one of:

* KEEP IN DISPLAY-ONLY SHADOW MODE
* READY FOR RIGHT-BRAIN ADVISORY MODE
* ROLLBACK REQUIRED

“READY FOR RIGHT-BRAIN ADVISORY MODE” requires all of the following:

* no execution parity differences
* no cross-instrument state leakage
* no malformed thesis outputs
* acceptable thesis flip frequency
* no persistent stale thesis
* no significant dashboard errors
* no new test failures

Do not implement Right Brain integration during this task.