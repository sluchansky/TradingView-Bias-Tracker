PHASE 7C.3 — MAIN BRAIN TRANSPARENCY CONTRACT TESTS

OBJECTIVE

Add comprehensive regression coverage for the Phase 7C.1 population adapter and Phase 7C.2 decision-transparency features.

This is a TEST-ONLY and DOCUMENTATION-ONLY phase.

Do not change production behavior unless a test exposes a genuine deterministic defect. If a defect is found, stop and report it before modifying production code.

NON-NEGOTIABLE CONSTRAINTS

- DO NOT modify Databento ingestion.
- DO NOT modify strategy calculations.
- DO NOT modify Edge Score calculations.
- DO NOT modify scoring thresholds.
- DO NOT modify trade eligibility.
- DO NOT modify trade frequency.
- DO NOT modify TradersPost or broker execution.
- DO NOT modify Left Brain thesis generation.
- DO NOT modify learning weights.
- DO NOT redesign the dashboard.
- DO NOT silently change API contracts to make tests pass.
- Preserve the current passing test baseline.

PRIMARY GOAL

Prove that the Main Brain operator console remains truthful, populated, and fail-safe across:

1. Complete live payloads
2. Partial payloads
3. Missing optional fields
4. Empty arrays
5. Null nested objects
6. Malformed display-only fields
7. Backend aliases
8. Strategy-scanner edge cases
9. Edge-score transparency cases
10. Active-trade versus no-trade states

TASK 1 — CAPTURE THE BASELINE

Before editing anything:

1. Record the current branch and HEAD.
2. Confirm the working tree state.
3. Run the three existing suites reported as:

   - 123/123
   - 103/103
   - 56/56

4. Record the exact commands and results.
5. Do not proceed if the baseline is unexpectedly failing.

TASK 2 — NORMALIZER CONTRACT TESTS

Add focused tests for:

normalizeMainBrainPayload()

Cover every Phase 7C.1 mapping, including at minimum:

- active_trades bare array becomes the expected normalized structure
- alerts bare array becomes the expected normalized structure
- left_brain.thesis fields are flattened correctly
- missing left_brain does not crash
- missing thesis does not crash
- null thesis does not crash
- market session is extracted correctly
- market regime is extracted correctly
- strategy scanner naming is normalized
- trade_plan wrapper is constructed correctly
- journal summary values are extracted correctly
- availability objects become correct booleans
- voice dictionary produces the narration string
- missing voice produces a safe empty fallback
- gateway_status alias is populated correctly
- db_ready alias is populated correctly
- databento_ready alias is populated correctly
- broker_ready alias is populated correctly
- unknown extra payload fields do not break normalization
- normalization does not mutate the original raw payload

Use realistic payload fixtures based on the actual live /main-brain schema.

Do not invent a second schema.

TASK 3 — VERDICT TRANSPARENCY TESTS

Add tests proving the Verdict panel correctly handles:

A. Complete edge breakdown

Given seven components:

- BOS
- CHOCH
- VWAP
- Sweep
- Volume
- CVD
- Session

Verify:

- all seven labels render
- present components render as satisfied
- absent components render as unsatisfied
- point values render correctly
- the running total matches the backend-provided values
- zero-point components render correctly
- negative adjustments, if supported by the payload, render without crashing

B. Waiting For checklist

Verify:

- only components with present=false appear
- satisfied components do not appear
- point values are shown correctly
- an empty missing-condition list does not show a misleading warning
- missing edge_breakdown produces a safe fallback
- malformed individual components do not crash the entire panel

C. Brain Reasoning

Verify:

- narration text renders when present
- empty narration does not show an empty quotation box
- missing narration does not crash
- narration is treated as text and not executable markup

TASK 4 — STRATEGY SCANNER TESTS

Add tests for the upgraded Strategy Scanner.

Required scenarios:

1. Eligible strategy with measurable completeness
2. Selected strategy
3. READY strategy
4. Ineligible strategy with skip reason
5. Multiple eligible strategies with different completeness
6. Tied completeness values
7. No eligible strategies
8. Empty strategy list
9. Missing scanner payload
10. Active trade present
11. No active trade present
12. Completeness below zero
13. Completeness above 100
14. Missing completeness
15. Missing strategy name

Verify:

- progress values are clamped for display to 0–100
- raw trading values are not mutated
- skip reasons display for ineligible strategies
- ineligible strategies are not selected as Best Opportunity
- the highest-completeness eligible strategy becomes Best Opportunity
- tie handling is deterministic
- Best Opportunity is hidden when an active trade exists
- Best Opportunity is hidden when no eligible candidate exists
- the currently selected strategy remains visibly distinguishable
- READY remains visibly distinguishable
- missing fields use safe operator-readable fallbacks

TASK 5 — TRUTHFULNESS AND NON-FABRICATION TESTS

Add explicit tests proving that the frontend does not fabricate information.

Verify:

- missing backend edge components are not invented
- missing strategy conditions are not shown as passed
- missing readiness values do not default to YES
- missing broker state does not default to ready
- missing Databento state does not default to live
- missing active trades do not create a phantom trade
- missing journal records do not create sample trades
- no Best Opportunity is displayed when eligibility cannot be established
- no score explanation claims a component contributed unless supplied by the backend

Fail closed for display truthfulness.

The interface may show UNKNOWN, unavailable, empty, or a safe placeholder—but it must never manufacture a positive status.

TASK 6 — BACKEND PASSTHROUGH TESTS

Add backend tests for the Phase 7C.1 and 7C.2 additions.

Verify that /main-brain or its controlling builder correctly passes through:

- voice
- gateway_status
- db_ready
- databento_ready
- broker_ready
- edge_breakdown

Verify:

- source values are preserved
- aliases are derived only from their intended canonical source
- false remains false
- null does not become true
- absent fields receive the documented safe fallback
- edge component ordering is deterministic
- the backend does not recalculate Edge Score for presentation purposes

TASK 7 — DISPLAY TOTAL CONSISTENCY

Add a consistency test between:

- displayed Edge Score
- edge_breakdown component points
- any backend-provided total or maximum

Do not assume that every component must sum directly to the final Edge Score if adjustments exist.

Instead:

1. Identify the canonical backend relationship.
2. Document it in the test.
3. Verify the UI accurately represents that relationship.
4. If totals legitimately differ, the UI must label the difference rather than imply an incorrect sum.

Do not change scoring logic.

Report any inconsistency as a finding before fixing it.

TASK 8 — ACCESSIBILITY AND OPERATOR READABILITY

Add lightweight component assertions for:

- WAIT, LONG, and SHORT are represented by text, not color alone
- satisfied and failed conditions use symbols plus text/state
- progress bars expose a readable numeric percentage
- missing data has readable fallback text
- panel headings remain present
- status meaning is not conveyed exclusively through CSS color

Do not redesign the interface in this phase.

TASK 9 — INTEGRATION FIXTURE

Create one representative full Main Brain payload fixture containing:

- live Databento status
- Left Brain thesis
- market session and regime
- seven edge components
- multiple strategies
- one ineligible strategy
- no active trade
- voice narration
- journal records
- system-health values

Use it for an integration-level render test proving the major dashboard sections populate simultaneously.

Also create one degraded fixture containing:

- partial market data
- missing voice
- no active trades
- empty journal
- unknown broker state
- partial strategy data
- partial edge breakdown

Verify the page still renders safely and truthfully.

TASK 10 — TEST ORGANIZATION

Keep tests focused and maintainable.

Preferred structure:

- normalizer contract tests
- verdict transparency tests
- strategy scanner tests
- backend passthrough tests
- full-page integration tests

Reuse fixtures where appropriate.

Avoid giant tests with dozens of unrelated assertions.

Avoid tests coupled to fragile CSS class ordering unless no semantic selector exists.

TASK 11 — VALIDATION

Run:

1. All new Phase 7C.3 tests
2. Existing frontend tests
3. Existing backend tests
4. Existing Main Brain regression tests
5. Existing Databento-related tests
6. Existing execution/TradersPost tests
7. TypeScript type checking
8. Production build
9. Python syntax/compile checks
10. git diff --check

Confirm:

- no production decision behavior changed
- no trading logic changed
- no execution logic changed
- no Databento logic changed
- no new browser console errors
- no broken dashboard panels
- all previous suites remain green

TASK 12 — COMPLETION REPORT

Return a precise report containing:

1. Branch and starting HEAD
2. Files added
3. Files modified
4. Tests added
5. Scenarios covered
6. Test commands
7. Exact pass counts
8. Type-check result
9. Build result
10. Any uncovered risks
11. Confirmation that trading logic was untouched
12. Confirmation that Databento was untouched
13. Confirmation that execution was untouched
14. Final commit hash

COMMIT MESSAGE

V1-P7C3 lock Main Brain transparency contracts

STOP CONDITIONS

Stop and report before making production changes if:

- the displayed Edge Score cannot be reconciled with its canonical backend data
- the Best Opportunity selection differs from backend eligibility
- a frontend fallback incorrectly shows a positive status
- tests reveal that normalization mutates the source payload
- the live schema differs materially from the documented Phase 7C schema
- fixing a failure would require changing trading, scoring, Databento, learning, or execution behavior