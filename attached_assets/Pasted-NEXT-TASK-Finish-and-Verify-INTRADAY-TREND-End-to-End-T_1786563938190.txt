NEXT TASK — Finish and Verify INTRADAY_TREND End-to-End

The native INTRADAY_TREND foundation is approved.

Current checkpoint:

* 109/109 tests passing
* parity smoke ✓
* scalp_golden ✓
* dual_sim ✓
* breakout_mode ✓
* native 15m classifier implemented
* native 1H context implemented
* native extension gate implemented
* native setup score implemented
* native entry-state machine implemented
* SWING strategy-policy isolation verified

Do NOT rewrite those working pieces.

Now complete the portions of the original INTRADAY_TREND spec that are still missing or not proven.

STEP 1 — AUDIT BEFORE CODING

For every item below, classify it:

COMPLETE
PARTIAL
MISSING

Provide exact function/file references.

Audit:

1. 5m setup/location detection
2. 5m pullback detection
3. 1m entry confirmation
4. configurable minimum confirmations
5. liquidity sweep detection
6. micro BOS/CHOCH confirmation
7. VWAP reclaim/rejection
8. structural stop placement
9. canonical risk sizing
10. TP1 = 1R behavior
11. TP2 = 2R behavior
12. one-contract management
13. conditional break-even logic
14. structural trailing
15. 08:00 ET entry start
16. 15:00 ET new-entry cutoff
17. 15:55 ET mandatory Intraday Trend flatten
18. SWING exclusion from that flatten
19. max IT trades/instrument/day
20. 15-minute IT cooldown
21. stale/missing 1H/15m/5m/1m data blocking
22. ghost MFE
23. ghost MAE
24. ghost TP1 outcome
25. ghost TP2 outcome
26. ghost stop outcome
27. ghost time-to-outcome
28. production /status visibility of the new IT fields
29. live VWAP availability/fallback behavior

Do not assume something is implemented because a similarly named SWING or SCALP feature exists.

⸻

STEP 2 — IMPLEMENT ONLY THE GAPS

After the audit, implement every PARTIAL or MISSING item necessary for a complete native Intraday Trend lifecycle.

Reuse shared infrastructure where appropriate.

Do NOT duplicate:

* risk engine
* broker gateway
* Databento ingestion
* prop guard
* safety system
* journal engine

But do NOT inherit SWING trading policy.

⸻

STEP 3 — COMPLETE THE 5m SETUP ENGINE

The 5m layer must answer:

Where is the pullback occurring, and is it a valid continuation location?

Identify usable locations from existing reliable data:

* VWAP
* prior breakout/retest
* 5m support/resistance
* prior session high/low
* overnight high/low
* opening-range boundary
* existing reliable FVG/OB information

Do not manufacture unavailable data.

Return explicit fields such as:

setup_location
pullback_state
location_quality

The state machine should remain:

WAITING_FOR_LOCATION

or

WAITING_FOR_PULLBACK

until those requirements actually pass.

⸻

STEP 4 — COMPLETE THE 1m TRIGGER ENGINE

The 1m layer is confirmation only.

It must never independently choose LONG/SHORT direction.

Evaluate available confirmations including:

* liquidity sweep
* rejection/reclaim
* micro BOS
* micro CHOCH
* VWAP reclaim/rejection
* momentum recovery
* delta/CVD only when reliable

Return:

confirmations_detected

and

confirmation_count

Use configurable:

IT_MIN_CONFIRMATIONS

Default:

2

If confirmation count is below the requirement:

WAITING_FOR_CONFIRMATION

Do not enter.

⸻

STEP 5 — STOP AND TARGET POLICY

Implement/verify native IT trade planning before risk sizing.

Stop should be based on:

1. pullback swing invalidation
2. structure invalidation
3. ATR buffer where appropriate

Then pass the stop distance into the canonical risk engine.

Default targets:

TP1 = 1R

TP2 = 2R

For one-contract trades, use the existing compatible one-contract policy, approximately 1.5R if no canonical policy already exists.

Do not create a second position-sizing system.

⸻

STEP 6 — FIX BREAK-EVEN BEHAVIOR

Do not automatically move to BE simply because TP1 was touched.

Require continuation/structure confirmation.

Record:

stop_move_reason

Prefer structural advancement beneath/above newly confirmed structure when possible.

This is important because we previously identified excessive TP1 → BE exits.

⸻

STEP 7 — STRUCTURAL TRAILING

For LONG:

trail beneath confirmed higher lows after sufficient favorable movement.

For SHORT:

trail above confirmed lower highs.

Do not trail every candle.

Reuse the existing management engine where possible.

⸻

STEP 8 — SESSION ENFORCEMENT

Defaults:

IT_ENTRY_START_ET=08:00

IT_ENTRY_CUTOFF_ET=15:00

IT_FORCE_FLAT_ET=15:55

Before 08:00:

BLOCKED_SESSION

After 15:00:

no new IT entries.

At 15:55:

close remaining INTRADAY_TREND positions.

CRITICAL:

The IT force-flat path must identify positions by strategy/mode ownership.

It must NEVER flatten SWING positions merely because they use the same instrument.

Add an explicit regression test for this.

⸻

STEP 9 — DAILY CONTROLS

Defaults:

MAX_INTRADAY_TREND_TRADES_PER_INSTRUMENT=3

INTRADAY_TREND_COOLDOWN_MINUTES=15

These are subordinate to canonical:

* prop rules
* daily-loss limits
* risk limits
* safety lock

⸻

STEP 10 — DATA FRESHNESS

Verify freshness independently for:

* 1H
* 15m
* 5m
* 1m

Required missing/stale data must produce:

BLOCKED_DATA

Never convert unavailable data into PASS.

Resolve the proposed live VWAP fallback issue as part of this work.

Fallback must be conservative.

If VWAP cannot be calculated reliably, expose:

UNAVAILABLE

Do not fabricate VWAP.

⸻

STEP 11 — COMPLETE GHOST OUTCOME TRACKING

Every qualified or near-qualified IT observation must eventually record:

* candidate entry
* candidate stop
* TP1
* TP2
* MFE
* MAE
* TP1 hit
* TP2 hit
* stop hit
* time to outcome
* final simulated result in R

This must remain observational.

Do NOT execute a real trade merely to measure outcomes.

Use the existing ghost watcher/observation infrastructure.

⸻

STEP 12 — PRODUCTION STATUS VERIFICATION

Complete proposed task #173 as part of this pass.

Verify /status actually returns the native IT fields from the running application.

Do not merely verify the Python functions.

Confirm real runtime output contains:

* 1H context
* native 15m trend
* 5m setup
* 1m trigger
* extension
* setup score
* entry state
* setup location
* confirmation count
* block reason

Verify dashboard rendering against actual /status output.

⸻

STEP 13 — FORCE-FLAT ALERT

Complete proposed task #174.

When an IT position is force-closed because of the session boundary:

journal it and send the existing Discord/notification mechanism an event equivalent to:

INTRADAY_TREND SESSION FLATTEN

Include:

* instrument
* direction
* quantity
* exit
* realized P/L if available
* reason
* timestamp

Do not build another notification system.

⸻

STEP 14 — TEST EVERYTHING

Add tests for all newly completed functionality.

Then rerun the entire suite.

Required regression verification:

* SCALP unchanged
* SWING unchanged
* ORB unchanged
* Databento unchanged
* canonical risk path unchanged
* safety gates unchanged

Then rerun:

* parity
* scalp_golden
* dual_sim
* breakout_mode

All must remain green.

⸻

STEP 15 — DO NOT PROMOTE EXECUTION

Even after everything passes:

INTRADAY_TREND LIVE EXECUTION = OFF

Keep it in ghost/shadow observation.

Do not PAPER or LIVE promote it yet.

⸻

FINAL RESPONSE

Return an end-to-end completion matrix for all 29 audit items:

ITEM | BEFORE | AFTER | FUNCTION/FILE | TEST

Then report:

Full tests: X/X

Parity: PASS/FAIL

Scalp golden: PASS/FAIL

Dual sim: PASS/FAIL

Breakout mode: PASS/FAIL

/status runtime verification: PASS/FAIL

Dashboard verification: PASS/FAIL

Ghost outcome tracking: PASS/FAIL

IT session force-flat: PASS/FAIL

SWING protected from IT force-flat: PASS/FAIL

Finally state:

INTRADAY_TREND LIVE EXECUTION: OFF

and tell me whether deployment/restart/republish is required.

Do not propose additional feature work until this audit is complete.