TASK: Restore the Ask AI feature on the Main Brain home page.

GOAL

Add the existing Ask AI capability to Main Brain so the operator can ask questions about the current instrument, thesis, verdict, strategy candidate, blockers, risk state, and trade plan.

IMPORTANT

Reuse the existing Ask AI backend, component, prompt builder, and conversation behavior.

Do not create a second AI implementation.

STRICT SCOPE

This is a UI integration and context-mapping task only.

DO NOT modify:

- Databento
- thesis calculations
- strategy logic
- Edge Score
- readiness
- alert rules
- learning
- risk calculations
- TradersPost
- execution
- polling
- authentication

PART 1 — TRACE THE EXISTING ASK AI FEATURE

Locate the existing Ask AI implementation from the old dashboard or another active page.

Document:

- frontend component
- backend endpoint
- request schema
- response schema
- authentication behavior
- current-context fields
- conversation-history behavior
- loading/error states

Do not duplicate the backend endpoint.

PART 2 — BUTTON PLACEMENT

Add a clearly visible button near the Main Brain header controls.

Preferred placement:

- beside Refresh, or
- in the top action strip beside Cleanest Trade Available

Label:

ASK AI

Do not place it in the left navigation sidebar.

PART 3 — MAIN BRAIN CONTEXT

When opened, Ask AI should receive the currently selected:

- instrument
- mode
- session
- market status
- Left Brain thesis
- thesis freshness
- verdict
- Edge Score
- candidate direction
- candidate status
- active strategy
- missing confirmations
- hard blockers
- opposing-structure diagnostics
- trade preview
- risk state
- active trade summary
- learning status
- system health

Only pass fields already available through canonical Main Brain data.

Do not calculate trading values in the frontend.

PART 4 — SAFE RESPONSE SEMANTICS

The Ask AI response must clearly distinguish:

- market analysis
- developing candidate
- actionable trade
- active trade
- unavailable data

It must not describe a POTENTIAL or WAIT setup as approved for execution.

PART 5 — UI STATES

Support:

- closed
- open
- submitting
- answered
- error
- unavailable
- authentication required

Opening and closing Ask AI must not create another polling stream.

PART 6 — TESTS

Verify:

- ASK AI button is visible
- current instrument and mode are passed correctly
- context updates when the user switches instruments
- WAIT remains described as non-actionable
- unavailable thesis is described honestly
- repeated clicks do not create duplicate requests
- closing the panel does not affect Main Brain polling
- existing Ask AI endpoint behavior remains unchanged

VALIDATION

Run:

- Ask AI focused tests
- Main Brain regressions
- navigation tests
- TypeScript typecheck
- production build
- git diff --check

FINAL RESPONSE REQUIRED

Provide:

- existing Ask AI source reused
- files changed
- button location
- context fields passed
- tests
- TypeScript and build results
- screenshot
- confirmation that trading and execution behavior were untouched