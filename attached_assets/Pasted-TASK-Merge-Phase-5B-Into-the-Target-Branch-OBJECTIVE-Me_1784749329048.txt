TASK: Merge Phase 5B Into the Target Branch

OBJECTIVE

Merge the completed Phase 5B work from branch:

polish-v1

Commit:

d79155a49320842a56301844d8781083ba5e43b1

Do not make additional code changes during this task.

IMPORTANT SCOPE RULES

* Do not modify Phase 5B code.
* Do not modify tests.
* Do not fix test_dynamic_stop.py during this task.
* Do not modify build_strict_trade_plan().
* Do not modify SWING R:R or trade-sizing logic.
* Do not refactor unrelated code.
* Do not combine the unrelated dynamic-stop failure with the Phase 5B merge.

STEP 1 — Confirm Source Branch State

Run:

git checkout polish-v1
git status
git rev-parse HEAD
git diff –check HEAD

Expected:

* Branch: polish-v1
* Working tree: clean
* HEAD:
    d79155a49320842a56301844d8781083ba5e43b1
* git diff –check: no output, exit 0

Stop and report if any unexpected local changes are present.

STEP 2 — Record the Phase 5B Validation State

Confirm the following evidence before merging:

* test_decision_trace_phase5b.py: 49 passed, 0 failed
* 19 of 20 regression test files exit successfully
* test_dynamic_stop.py: 28 passed, 1 failed
* The remaining failure is:
    test_fixed_1to1_rr_swing_no_longer_vetoes
* The failure exercises build_strict_trade_plan() SWING R:R sizing logic
* build_strict_trade_plan appears 0 times in the Phase 5B diff
* SWING_MIN_RR appears 0 times in the Phase 5B diff
* rr_num appears 0 times in the Phase 5B diff
* 4 of 4 golden suites pass

Do not rerun or reinterpret unrelated development work unless the merge itself changes the result.

STEP 3 — Update the Target Branch Safely

Identify the intended target branch from the repository configuration or existing workflow.

Common target branches may include:

* main
* master
* development
* production

Do not guess if the repository clearly identifies a default branch.

Checkout the target branch:

git checkout 

Confirm it is clean:

git status

Fetch the latest remote state:

git fetch origin

Update the target branch using the repository’s normal workflow.

For a direct fast-forward workflow:

git pull –ff-only origin 

Do not use force, reset –hard, rebase, or destructive cleanup commands.

STEP 4 — Merge Phase 5B

Merge the validated source branch:

git merge –no-ff polish-v1 -m “Merge Phase 5B decision trace closure”

If the repository requires fast-forward-only merges and the history permits it, use the repository’s required workflow instead.

Do not resolve conflicts by deleting or rewriting unrelated code.

If a conflict occurs:

* Report the conflicting files.
* Explain whether the conflict touches Phase 5B functions.
* Do not make speculative conflict resolutions.
* Do not alter trade-plan sizing or SWING R:R logic.

STEP 5 — Verify the Merge Result

Run:

git status
git log -1 –oneline
git diff –check HEAD^

Confirm:

* Working tree is clean.
* The merge commit exists.
* No whitespace errors were introduced.
* The Phase 5B commit remains present in history.

Verify:

git merge-base –is-ancestor d79155a49320842a56301844d8781083ba5e43b1 HEAD

Expected exit code:

0

STEP 6 — Run Post-Merge Validation

Run:

python3 artifacts/tradingview-webhook/test_decision_trace_phase5b.py

Expected:

49 passed, 0 failed

Run the critical regression and golden suites using the same commands used in the final Phase 5B validation.

Expected outcome:

* Phase 5B: 49/49 passing
* Golden suites: 4/4 passing
* No new failures caused by the merge
* test_dynamic_stop.py may still show the already-documented unrelated failure:
    test_fixed_1to1_rr_swing_no_longer_vetoes

Do not fix that failure as part of this merge.

STEP 7 — Push the Target Branch

Push only after the merge and post-merge validation are complete:

git push origin 

Do not force-push.

STEP 8 — Final Merge Report

Return a concise report containing:

1. Source branch
2. Target branch
3. Source commit SHA
4. Merge commit SHA
5. Push result
6. Working-tree status
7. git diff –check result
8. Phase 5B test result
9. Regression test result
10. Golden-suite result
11. Confirmation that no Phase 5B code was changed during the merge
12. Confirmation that the dynamic-stop failure remains a separate, unrelated issue

SUCCESS CRITERIA

The task is complete only when:

* Commit d79155a49320842a56301844d8781083ba5e43b1 is an ancestor of the target branch.
* The target branch is pushed successfully.
* The working tree is clean.
* Phase 5B remains 49/49 passing.
* All 4 golden suites pass.
* No new regression failures are introduced.
* No SWING R:R or trade-sizing code is modified.
* The unrelated test_dynamic_stop.py failure is not fixed or folded into this merge.