Please audit and improve the Entry Quality / Veto Armed module.

Issue:
The dashboard is showing:
- Entry Quality Score: 52 Poor
- Veto Armed
- ATR Extension: 13.14 ATR beyond VWAP
- Impulse Completed: 100%
- Flags: Chasing, At Swing High, Overextended

This is good logic, but the ATR Extension value may be wrong or too extreme. Please verify the math.

Required audit:

1. Confirm the ATR extension formula is:

atrExtension = abs(currentPrice - VWAP) / currentATR

2. Display the raw values on the dashboard:

currentPrice
VWAP
currentATR
distanceFromVWAP
atrExtension
atrExtensionDecision

Example display:

Price: 4289.50
VWAP: 4286.75
Distance from VWAP: 2.75 pts
ATR: 4.00 pts
ATR Extension: 0.69 ATR

3. Make sure ATR is using the correct timeframe for the active mode:

SCALP:
- 1m and 5m ATR

SWING:
- 15m, 1h, and 4h ATR

Do not mix daily ATR into scalp calculations unless clearly labeled.

4. Keep the VETO ARMED logic, but make it explain what would remove the veto.

Add a section called:

ENTRY IMPROVEMENT PLAN

It should show what the bot is waiting for.

Example:

Current Entry Score: 52/100

To improve:
- Wait for price to pull back within 1.0 ATR of VWAP
- Wait for 38–50% pullback of the current impulse
- Wait for fresh demand/support retest
- Wait for bullish CVD or volume confirmation
- Avoid entering at swing high

Projected Score If Conditions Improve:
75–90/100

5. Veto rules:

If Entry Quality < 70:
- Do not allow auto-entry unless Edge Score >= 90
- Show VETO ARMED

If Entry Quality < 60:
- Strongly reject entry
- Show WAIT / BAD LOCATION

If Entry Quality >= 70:
- Remove veto warning
- Allow normal trade evaluation

6. Do not let good room-to-target alone override bad location.

Example:
Even if Room to Resistance is 12/12, still reject if:
- impulse is already complete
- price is overextended from VWAP
- entry is at swing high
- bot is chasing

7. Add a plain English explanation:

Example:

“Trade idea may be directionally correct, but entry location is poor. Price has already completed the impulse and is extended from VWAP. Wait for pullback or fresh confirmation before entry.”

Goal:
The bot should think like a professional analyst:
- Direction can be right
- Setup can be valid
- But entry can still be bad

The bot should reject late/chasing entries, explain why, and tell me what needs to happen before the trade becomes valid again.