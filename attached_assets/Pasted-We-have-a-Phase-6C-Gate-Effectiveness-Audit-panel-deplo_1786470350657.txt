We have a Phase 6C Gate Effectiveness Audit panel deployed on /api/dashboard. Today the trading system has produced 0 live trades, but the Gate Effectiveness Audit also shows:

* Total Observations = 0
* Approved = 0
* Blocked = 0
* Completed Outcomes = 0
* Pending = 0

At the same time, other ghost/FVG experiment events are appearing elsewhere, so do not assume those are feeding Phase 6C. We need to verify the new Gate Effectiveness recorder is actually connected to the live decision path.

NON-NEGOTIABLE SCOPE

Do not modify:

* edge-score weights
* READY/WAIT thresholds
* strategy rules
* confirmation requirements
* execution rules
* risk sizing
* prop rules
* ghost/FVG experiment logic
* Databento ingestion
* dashboard layout except where needed to expose diagnostic status

This task is only to trace, diagnose, repair, and validate Phase 6C data collection.

1. TRACE THE COMPLETE LIVE PATH

Identify and document the exact production path for a real scanner evaluation:

market data -> candidate/setup evaluation -> strict decision gate -> ALLOWED or BLOCKED result -> Phase 6C recorder -> persistence -> API -> dashboard

Find the exact function names/files for each step.

Specifically locate:

* the canonical function that produces the final ALLOWED/BLOCKED or READY/WAIT decision
* every return path from that function
* the Phase 6C gate-effectiveness recorder function
* the database/table/model/file where Phase 6C observations are stored
* the API endpoint used by the dashboard panel
* the frontend fetch/render code for the panel

Produce a short trace report before changing code.

2. VERIFY WHETHER THE RECORDER IS ACTUALLY CALLED

Instrument the canonical gate path with temporary structured diagnostics.

For every evaluated candidate, log one concise line similar to:

GATE_AUDIT_TRACE instrument=MGC strategy=... direction=LONG decision=BLOCKED recorder_called=true observation_id=...

If the recorder is not called, log:

GATE_AUDIT_TRACE ... recorder_called=false reason=...

Do not spam logs from market ticks. Log only when a genuine setup/candidate reaches the decision gate.

Verify both:

* ALLOWED path
* BLOCKED path

The recorder must execute once per final gate decision, not once per component and not once per tick.

3. INSPECT ALL EARLY RETURNS

This is critical.

Check whether candidates can exit before the Phase 6C recorder due to:

* insufficient edge score
* failed component
* missing data
* stale data
* execution mode
* safety lock
* prop/risk gate
* duplicate guard
* session/window restriction
* volatility/state restriction
* any exception/fallback path

If any final trading decision is made on an early return, Phase 6C must still receive the decision before returning.

Do not force every raw scanner tick into Phase 6C. Only actual candidate/gate decisions belong there.

4. CONFIRM DECISION SEMANTICS

Phase 6C must distinguish at minimum:

* ALLOWED
* BLOCKED

For blocked decisions also record the canonical reason(s), including:

* primary blocking gate
* all failed gate/components if available
* edge score
* threshold
* instrument
* strategy
* direction
* mode
* timestamp
* candidate/setup identifier
* strategy/config version if already available

Do not invent new trading logic. Use existing canonical values.

5. VERIFY PERSISTENCE

Inspect the Phase 6C storage directly in production/dev DB.

Confirm:

* table exists
* current schema matches recorder writes
* inserts are committing
* no silent transaction rollback
* no serialization/type error
* timestamps are valid
* dashboard date filtering includes today
* deployment is reading the same database that the writer is writing

Run a direct count grouped by today:

* total observations
* approved
* blocked
* pending outcomes
* completed outcomes

Also group by:

* instrument
* strategy
* blocking reason

If records exist in DB but dashboard says 0, then the bug is downstream in the API/filter/frontend. Do not change recorder logic unnecessarily.

6. VERIFY API

Call the exact Gate Effectiveness API directly.

Capture and report:

* HTTP status
* raw JSON
* baseline ID/date
* observation count
* approved count
* blocked count
* completed count
* pending count

Compare API values against direct DB counts.

If DB != API, repair only the API/query/filter layer.

7. VERIFY DASHBOARD

Confirm /api/dashboard is fetching the Phase 6C endpoint successfully.

Check browser/server logs for:

* 401
* 404
* 500
* CORS errors
* malformed JSON
* stale cache
* wrong endpoint
* wrong field names
* incorrect default filters

The panel currently shows baseline:

GATE_BASELINE_2026-08-11

Make sure the dashboard is not accidentally showing a baseline shell while failing to load observations.

Add a small diagnostic status only if useful, for example:

Collector: ACTIVE | Last decision recorded: 13:42:18 ET

or

Collector: ACTIVE | No gate decisions received today

Do not redesign the panel.

8. CREATE A SAFE SYNTHETIC VALIDATION

We need proof without placing a real trade.

Add or use a test-only/dev-only mechanism that sends:

1. one synthetic BLOCKED gate decision
2. one synthetic ALLOWED gate decision

through the same Phase 6C recorder/persistence path.

It must not:

* send an order
* reserve real risk
* hit TradersPost/broker
* affect live strategy state
* alter performance statistics used for promotion

Mark synthetic records clearly, or clean them up immediately after validation.

Verify the two records:

recorder -> DB -> API -> dashboard

Then remove them or exclude them from live measurement counts.

9. ADD REGRESSION TESTS

Add tests proving:

* an ALLOWED final gate decision creates exactly 1 Phase 6C observation
* a BLOCKED final gate decision creates exactly 1 Phase 6C observation
* early-return BLOCKED decisions are still recorded
* one candidate cannot create duplicate observations
* recorder failure cannot crash live trading
* database/API counts agree
* dashboard JSON contract remains valid
* synthetic/test observations cannot contaminate live measurements

Run the relevant existing regression suite too.

10. IMPORTANT DISTINCTION

We are currently seeing events such as:

MGC GHOST_EXPERIMENT_COMPLETED -1.00R

Do not automatically connect those to Phase 6C.

Determine exactly which subsystem owns those events.

Document whether:

* FVG ghost experiments
* strategy ghost trades
* Phase 6C blocked-trade counterfactuals

are separate systems or share components.

Do not merge them in this task.

11. FINAL REPORT

When finished, report only:

1. Root cause of the Phase 6C zero-observation problem
2. Exact files/functions changed
3. Whether production gate decisions now reach Phase 6C
4. Direct DB counts after validation
5. API counts after validation
6. Test results
7. Whether republish/redeploy is required
8. Any remaining limitation

Most importantly, explicitly state one of:

PASS — Gate Effectiveness Audit is now connected to the canonical live gate and collecting decisions

or

FAIL — Gate Effectiveness Audit is still not collecting canonical live decisions

Do not start any Phase 6D work, threshold tuning, strategy changes, or profitability optimization after completing this task.