TASK: Phase 6B.1C — Final Validation and Freeze

Do not start another phase.

Do not connect eligibility to learning.

Do not allow eligibility to block, promote, demote, or rescore strategies.

Return a complete final validation report for the Strategy Eligibility Engine.

----------------------------------------------------
PART 1 — GIT STATE
----------------------------------------------------

Run:

git branch --show-current
git rev-parse HEAD
git log -7 --oneline
git status --short
git diff --check
git show --stat --oneline HEAD
git show --name-only --format="" HEAD

Return:

- branch
- starting HEAD
- ending commit
- commit message
- exact files changed
- staged files
- untracked files
- task attachment status

Do not commit task attachments, temporary scripts, raw diagnostic exports,
database files, market data, or secrets.

----------------------------------------------------
PART 2 — IMPLEMENTATION INVENTORY
----------------------------------------------------

Identify every eligibility-related addition.

Return exact names and source locations for:

- feature flag
- eligibility input resolver
- session resolver
- strategy profile definitions
- strategy scorer functions
- aggregate eligibility function
- full-analysis hook
- decision-trace hook
- status exposure
- serializer or output formatter
- tests

Explain the call path from market inputs to diagnostic output.

----------------------------------------------------
PART 3 — FEATURE-FLAG BEHAVIOR
----------------------------------------------------

Return the exact flag name and default.

Prove:

Flag OFF:
- eligibility engine is not called, or returns no diagnostic influence
- full_analysis output remains backward-compatible
- decision trace remains backward-compatible
- no score changes
- no verdict changes
- no trade-plan changes
- no detector changes
- no execution changes

Flag ON:
- eligibility diagnostics are produced
- diagnostics are read-only
- execution result is unchanged
- scoring result is unchanged
- trade plan is unchanged

If the feature is always enabled, explain why and prove that it remains
diagnostics-only.

----------------------------------------------------
PART 4 — OUTPUT CONTRACT
----------------------------------------------------

Return the exact output schema.

For every strategy include:

- strategy key
- eligible
- confidence
- reasons
- warnings
- preferred conditions
- weak conditions
- historical confidence or evidence label
- current session
- current regime
- direction context, if used
- mode
- instrument

Clarify whether `eligible` is:

- strict boolean
- descriptive label
- nullable diagnostic

If confidence is 0–100, document:

- scoring inputs
- weights
- caps
- default behavior
- missing-input behavior
- tie behavior

----------------------------------------------------
PART 5 — STRATEGY PROFILE TABLE
----------------------------------------------------

Return the actual implemented profile for:

1. OPENING_DRIVE
2. LIQUIDITY_SWEEP_REVERSAL
3. VWAP_TREND_CONTINUATION
4. RANGE_EXPANSION_BREAKOUT
5. OPENING_RANGE_BREAKOUT

For each show:

- favored sessions
- weak sessions
- favored volatility regimes
- weak volatility regimes
- favored trend regimes
- weak trend regimes
- direction considerations
- instrument considerations
- mode considerations
- confidence adjustments
- warning conditions

Clearly distinguish:

- profile facts derived from Phase 6B.1B
- neutral defaults
- assumptions introduced by implementation

Do not present six months of history as permanent truth.

----------------------------------------------------
PART 6 — HISTORICAL-EVIDENCE SAFETY
----------------------------------------------------

Verify the engine does not hard-code conclusions as universal rules.

Specifically prove:

- ORB is not permanently long-only
- ORB Short is not blocked
- LSR Asia is not blocked
- VTC overnight is not blocked
- REB London is not blocked
- MGC is not automatically promoted
- equity-index shorts are not automatically rejected
- weekday and hour findings are diagnostic only
- no historical metric directly changes trade eligibility in execution

If historical findings affect confidence, document exactly how and confirm they
remain informational.

----------------------------------------------------
PART 7 — EXECUTION ISOLATION PROOF
----------------------------------------------------

Trace all paths that can produce:

- verdict
- edge score
- confidence
- trade_plan
- entry
- stop
- target
- quantity
- TradersPost payload
- broker send
- Discord notification
- learning update

Prove the eligibility result is not consumed by any of them.

Search for every reference to:

- eligibility
- strategy_eligibility
- eligibility_score
- eligibility_confidence
- eligible

Return each match and classify it as:

- definition
- diagnostic calculation
- serialization
- display
- test
- prohibited execution consumption

There must be zero prohibited execution consumers.

----------------------------------------------------
PART 8 — BYTE-IDENTICAL / STRUCTURAL PARITY
----------------------------------------------------

Run representative comparisons with the flag OFF and ON.

Use at least:

- actionable Long setup
- actionable Short setup
- WAIT setup
- active-trade management setup
- SCALP mode
- SWING mode
- ORB
- LSR
- VTC
- REB
- Opening Drive

Compare all non-diagnostic result fields.

At minimum compare:

- verdict
- bias
- direction
- confidence
- edge_score
- trade_plan
- entry
- stop
- target
- invalidation
- quantity/contracts
- gate results
- dispatch decision
- TradersPost payload, if generated
- learning write intent

Return:

- scenarios compared
- fields compared
- mismatch count
- expected diagnostic-only differences
- unexpected differences
- parity status

Preferred status:

EXECUTION_PARITY_PROVED

Do not say byte-identical if diagnostics add keys to the returned object.
Use “all pre-existing non-diagnostic fields identical” where appropriate.

----------------------------------------------------
PART 9 — SESSION RESOLUTION
----------------------------------------------------

The initial test failure showed `_eligibility_session()` depended on the live
clock.

Explain the final behavior.

Return:

- source of session time
- timezone used
- precedence between injected/test context and live clock
- deterministic testing behavior
- behavior when timestamp is missing
- behavior around session boundaries
- behavior around daylight saving transitions

Add or confirm tests for:

- Asia
- London
- New York
- off-hours
- exact boundary timestamps
- injected timestamps overriding wall-clock time

The diagnostic engine should prefer event/market timestamps when available,
not server wall-clock time.

If it still relies primarily on the live clock, classify this as an open issue.

----------------------------------------------------
PART 10 — INPUT AVAILABILITY
----------------------------------------------------

For every eligibility input report:

- input name
- source field/function
- present in full_analysis
- present in decision trace
- live availability
- historical/replay availability
- fallback
- missing-value behavior

Cover:

- instrument
- mode
- session
- ET hour
- volatility regime
- trend regime
- VWAP relationship
- opening-range status
- ATR regime
- weekday
- higher-timeframe bias

Do not silently invent unavailable inputs.

----------------------------------------------------
PART 11 — STATUS AND API EXPOSURE
----------------------------------------------------

Return:

- exact `/status` field added
- whether it exposes only flag state or live diagnostics
- authentication behavior
- Express proxy whitelist change
- whether any new route was added
- whether output is backward-compatible
- whether secrets or internal data are exposed

If `/status` was changed, prove existing status clients remain valid.

----------------------------------------------------
PART 12 — FAILURE HANDLING
----------------------------------------------------

Prove eligibility is fail-open and diagnostics-only.

Test:

- scorer exception
- malformed strategy key
- missing instrument
- missing mode
- missing regime
- missing session
- non-serializable input
- profile lookup failure
- aggregate engine failure

Expected behavior:

- original analysis continues unchanged
- no execution path is interrupted
- diagnostic warning may be recorded
- no trade is blocked
- no score is changed

Return exact exception-handling behavior.

----------------------------------------------------
PART 13 — TEST INVENTORY
----------------------------------------------------

List all 31 tests by name or grouped test class.

At minimum confirm coverage for:

- deterministic output
- all five strategy profiles
- each major session
- each major volatility regime
- trend regime behavior
- missing inputs
- malformed inputs
- flag OFF
- flag ON
- serialization
- scorer exception
- aggregate exception
- no score mutation
- no trade-plan mutation
- no verdict mutation
- Long and Short cases
- SCALP and SWING
- status exposure
- decision-trace output
- event timestamp versus live clock

If any item is not tested, state it clearly.

----------------------------------------------------
PART 14 — REGRESSION TESTS
----------------------------------------------------

Run and report separately:

A. Eligibility-focused tests

B. Phase 6B.1 baseline tests

C. Backtest-related tests

D. Decision-trace tests

E. Full-analysis tests

F. Learning-score influence tests

G. Strategy-engine tests

H. TradersPost isolation tests

I. Golden workflows:
- parity
- scalp_golden
- dual_sim
- breakout_mode

J. Full repository test suite, only if actually run

Do not describe 31 tests as the repository suite.

Report any pre-existing unrelated failure separately.

----------------------------------------------------
PART 15 — PROTECTED BOUNDARIES
----------------------------------------------------

Explicitly verify unchanged:

- Databento ingestion
- historical datasets
- baseline tables
- baseline configuration
- baseline hash
- strategy detectors
- stop construction
- target construction
- position sizing
- TradersPost gateway
- duplicate guard
- prop guard
- daily-loss guard
- Discord behavior
- active-trade management
- learning database
- decision-quality writes
- live strategy selection
- strategy weight influence

----------------------------------------------------
PART 16 — SAMPLE OUTPUTS
----------------------------------------------------

Return at least five real serialized sample outputs:

1. Opening Drive in trending New York conditions
2. LSR in balanced Asia conditions
3. VTC near VWAP during New York conditions
4. REB in balanced London conditions
5. ORB with opening range complete and unclear trend

Also include:

- one missing-input example
- one scorer-exception example
- one flag-OFF example

Show diagnostics only. Do not include secrets or full market payloads.

----------------------------------------------------
PART 17 — ARCHITECTURE ASSESSMENT
----------------------------------------------------

Explain whether eligibility currently lives in the correct layer.

Answer:

- Is it below strategy detection?
- Is it above scoring?
- Is it inside full_analysis?
- Is it included in decision trace?
- Could another developer accidentally consume it for execution?
- Is its naming sufficiently explicit that it is shadow-only?
- Should the returned field be renamed to include `shadow` or `diagnostic`?

If `eligible: false` could be mistaken as an execution gate, consider renaming
the diagnostic field to something unambiguous such as:

shadow_eligible
diagnostic_eligible
historical_fit

Do not make the rename without reporting the current ambiguity first unless it
is clearly necessary for safety.

----------------------------------------------------
PART 18 — FINAL CLASSIFICATION
----------------------------------------------------

Return one architecture classification:

SHADOW_ENGINE_SAFE

SHADOW_ENGINE_SAFE_WITH_WARNINGS

SHADOW_ENGINE_NOT_ISOLATED

Use SAFE_WITH_WARNINGS if:

- session resolution still uses wall-clock time
- profile rules are overfit to the six-month baseline
- `eligible` naming could be mistaken for a real gate
- test coverage is incomplete
- full repository tests were not run
- execution parity was structurally inferred rather than directly compared

----------------------------------------------------
PART 19 — GIT CLEANUP
----------------------------------------------------

Remove any accidentally committed task attachment.

Confirm no task attachment is tracked.

Return:

git status --short
git diff --check
git ls-files attached_assets/
git show --stat --oneline HEAD
git show --name-only --format="" HEAD

Create a cleanup commit only if needed.

Suggested implementation commit message:

Add shadow strategy eligibility diagnostics

Suggested cleanup commit message:

Remove Phase 6B.1C task attachment

----------------------------------------------------
PART 20 — FINAL REPORT
----------------------------------------------------

Return:

1. Branch
2. Starting HEAD
3. Ending commit
4. Exact files changed
5. Feature flag and default
6. Architecture and call path
7. Output schema
8. Five strategy profiles
9. Input availability table
10. Session-resolution behavior
11. Failure behavior
12. Execution-isolation proof
13. ON/OFF parity comparison
14. Search-reference audit
15. Status exposure
16. Test inventory
17. Regression results
18. Golden workflow results
19. Protected-boundary verification
20. Sample outputs
21. Known limitations
22. Architecture classification
23. Task-attachment status
24. Final git status
25. Recommended next investigation
26. Final status

Final status must be exactly one:

PHASE_6B_1C_SHADOW_ELIGIBILITY_COMPLETE

PHASE_6B_1C_SHADOW_ELIGIBILITY_COMPLETE_WITH_WARNINGS

PHASE_6B_1C_SHADOW_ELIGIBILITY_BLOCKED

After returning the report, stop.

DO NOT CONNECT ELIGIBILITY TO EXECUTION.

DO NOT CONNECT ELIGIBILITY TO SCORING.

DO NOT CONNECT ELIGIBILITY TO LEARNING.

DO NOT START PHASE 6B.2.

DO NOT START PHASE 6C.