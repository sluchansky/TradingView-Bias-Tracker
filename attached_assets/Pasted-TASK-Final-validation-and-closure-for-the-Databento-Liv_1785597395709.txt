TASK: Final validation and closure for the Databento Live Market chart.

CONTEXT

The Databento-powered Main Brain chart has been implemented with:

- lightweight-charts v5.2.0
- MGC, MNQ, MES, MYM
- 1m, 5m, 15m
- completed candles
- partial candle
- VWAP
- active-trade levels
- structure markers
- connection/freshness status
- a dedicated 5-second chart refresh

The development screenshot only showed the disabled or unauthenticated state.

GOAL

Complete authenticated live-data validation, fix any defects found, commit the feature, and prepare it for republish.

Do not add new chart features during this task.

STRICT SCOPE

DO NOT modify:

- Databento subscriptions
- market-data normalization
- bar-building formulas
- strategy logic
- Edge Score
- readiness
- alerts
- thesis formulas
- risk
- TradersPost
- execution
- learning
- authentication

PART 1 — VERIFY GIT STATE

Report:

- current branch
- current HEAD
- modified files
- untracked files
- exact chart-related diff

Create a clean feature commit after validation.

Do not include unrelated pasted instruction files or assets unless intentionally required.

PART 2 — AUTHENTICATED LIVE CHART PROOF

Use an authenticated browser session against the active Databento-enabled environment.

Capture screenshots for:

1. MNQ 1-minute chart
2. MGC 1-minute chart
3. MES 5-minute chart
4. MYM 15-minute chart
5. VWAP overlay enabled
6. Trade-plan or active-trade price lines
7. Structure markers
8. Collapsed chart state

Each screenshot must show:

- Main Brain route
- selected instrument
- selected timeframe
- live status
- native contract
- candles
- last update/freshness

PART 3 — VERIFY CANDLE ACCURACY

For at least one instrument, compare the last five displayed candles with the canonical backend response.

Verify exact equality for:

- timestamp
- open
- high
- low
- close
- volume
- complete flag

For 5-minute and 15-minute candles, verify aggregation against the underlying completed one-minute bars.

Do not compare rounded display text only. Compare raw values.

PART 4 — PARTIAL BAR BEHAVIOR

Verify:

- partial candle is visually distinguishable
- it updates in place
- it is not added as a completed bar
- when it closes, it becomes completed exactly once
- no duplicate candle remains
- no candle disappears incorrectly

Test a low-volume instrument when possible.

PART 5 — MGC LOW-VOLUME PROOF

Verify MGC behavior during a quiet period:

- real gaps remain visible
- no flat synthetic candles
- partial bar is shown only when real trades exist
- `NO DATA`, `DELAYED`, or `STALE` status is accurate
- completed bars increase after the stale-partial flush closes a real bar

Do not claim `LIVE` merely because the connection itself is open.

PART 6 — OVERLAY VALIDATION

Verify each overlay separately:

VWAP
- price line equals canonical VWAP

TRADE
- entry, stop, and targets equal the canonical trade object
- preview levels are not confused with active-trade levels

STRUCTURE
- event direction, type, timestamp, and price match backend events
- `OVERRIDDEN` and `EXPIRED` lifecycle states are represented honestly
- no frontend-derived BOS or CHOCH

PART 7 — INSTRUMENT SYNCHRONIZATION

Verify:

- changing Main Brain instrument updates the chart
- changing the chart instrument updates Main Brain through the approved application state
- no panel remains on the old instrument
- Ask AI context updates to the same instrument
- Trade Plan, Thesis, Verdict, and chart remain synchronized

Do not allow independent conflicting instrument state.

PART 8 — REFRESH AND NETWORK VALIDATION

Measure browser network behavior for at least one minute.

Verify:

- Main Brain continues its existing 7-second request
- chart uses exactly one controlled refresh stream
- chart refresh cadence is approximately 5 seconds
- no overlap while a prior chart request is in flight
- collapse stops chart refreshes
- expand restarts exactly one stream
- instrument/timeframe change does not leave old timers running
- unmount cleans up the timer

Report whether the separate 5-second chart stream is necessary.

If the same data can safely ride the existing 7-second Main Brain response without making the payload excessive, recommend consolidation—but do not redesign it during this closure task.

PART 9 — ERROR STATES

Using safe interception or fixture support, verify:

- 401 authentication failure
- 500 endpoint error
- disconnected feed
- reconnecting feed
- empty bars
- malformed partial bar
- stale data
- unsupported instrument
- unsupported timeframe

The chart must not crash Main Brain.

PART 10 — PERFORMANCE

Measure:

- initial chart response size
- chart endpoint response size after updates
- approximate render/update duration
- number of bars rendered
- whether the full 300-bar history is refetched every five seconds

Preferred behavior:

- bounded history
- incremental update
- no repeated full-history replacement unless needed

If the endpoint currently returns all bars every five seconds, report that clearly. Do not hide it behind frontend incremental rendering.

PART 11 — SECURITY AND AUTH

Verify:

- endpoint is authenticated through the existing proxy
- unsupported direct access is rejected as expected
- no Databento credentials or internal connection strings are exposed
- raw error messages do not leak secrets
- limit is clamped
- instrument and timeframe are validated

PART 12 — REGRESSION

Run:

- chart endpoint tests
- chart frontend tests
- Databento bar tests
- stale-partial flush tests
- MGC thesis tests
- Main Brain navigation tests
- Main Brain normalizer tests
- candidate-preview tests
- active-trade tests
- Ask AI tests
- Risk State tests
- Send to TradersPost tests
- parity
- scalp golden
- dual-sim
- breakout smoke
- TypeScript typecheck
- production build
- git diff --check

Report the full TypeScript status honestly.

PART 13 — COMMIT

After all validation passes:

- create one clean commit
- include only intended chart files
- report the commit hash
- do not start another feature

Suggested commit message:

Phase 7M: Add Databento live candlestick chart to Main Brain

FINAL RESPONSE REQUIRED

Provide:

1. Starting commit
2. Final commit
3. Files changed
4. Authenticated screenshot locations
5. Live instruments verified
6. Raw candle comparison results
7. Partial-bar proof
8. MGC low-volume proof
9. Overlay proof
10. Instrument synchronization proof
11. Network cadence and timer proof
12. Response sizes and performance findings
13. Error-state results
14. Test results
15. Full TypeScript status
16. Production build result
17. Confirmation that no trading logic or Databento ingestion behavior changed