TASK: Phase 6A.1 — ORB Replay Parity and Historical Data Readiness

OBJECTIVE

Phase 6A successfully registered OPENING_RANGE_BREAKOUT in the historical
backtester, but the closure report identified material differences between the
live ORB trade plan and the historical ORB simulation.

Before beginning Phase 6B, repair or explicitly isolate those parity differences
and establish a real historical-data workflow.

DO NOT begin Phase 6B.

DO NOT add research strategies.

DO NOT alter live trading behavior.

DO NOT alter Databento live ingestion.

----------------------------------------------------
PART 1 — PRE-FLIGHT AND COMMIT HISTORY
----------------------------------------------------

Run:

git branch --show-current
git rev-parse HEAD
git log -5 --oneline
git status --short
git diff --check

Identify separately:

1. The commit that changed:
   - backtest_engine.py
   - app.py

2. The commit that changed:
   - test_backtest_orb_adapter.py
   - test_backtest_coverage_audit.py

Do not describe the test-only commit as the complete Phase 6A implementation
commit.

Return both hashes and exact file lists.

----------------------------------------------------
PART 2 — VERIFY THE LIVE ORB TRADE PLAN
----------------------------------------------------

Trace the complete live OPENING_RANGE_BREAKOUT path from:

_score_opening_range_breakout()

through:

- strategy selection
- trade-plan construction
- stop construction
- target construction
- management selection
- execution payload construction

Prove from source whether live ORB actually uses:

- opposite opening-range boundary as stop
- fixed 4R target
- ATR stop
- another shared stop planner
- another management override

The previous report stated:

Live stop = opposite OR boundary
Live target = 4R

Do not rely on that statement.

Verify the actual executable path.

Return exact functions and line numbers.

Stop if the reported live stop or target was incorrect.

----------------------------------------------------
PART 3 — CREATE AN ORB-SPECIFIC HISTORICAL TRADE PLAN
----------------------------------------------------

Only if source verification confirms the live ORB uses OR-opposite stop and 4R
target:

Add an ORB-specific historical trade-plan path.

Requirements:

Long ORB:

entry:
- use the existing historical signal-entry convention
- normally next bar open plus configured slippage

stop:
- opening-range low
- include any exact live buffer only if live uses one

risk:
- entry minus stop
- must be positive

target:
- entry plus 4 × risk

Short ORB:

entry:
- next bar open minus configured slippage

stop:
- opening-range high
- include any exact live buffer only if live uses one

risk:
- stop minus entry
- must be positive

target:
- entry minus 4 × risk

Do not change the stop or target behavior of the original four strategies.

Do not globally change bt_stop_plan().

Do not change default management for other strategies.

The ORB-specific geometry should be selected by strategy key.

If the live executable path uses different geometry, reproduce the verified live
geometry instead.

----------------------------------------------------
PART 4 — VOLUME PARITY
----------------------------------------------------

Audit the live volume_ok field.

Identify exactly how these inputs are produced:

- RVOL confirmation
- volume_spike_fresh
- webhook volume spike
- Databento-derived volume spike
- expiration time
- mutable state

Classify:

A. historically reconstructable from stored OHLCV
B. reconstructable only with additional stored fields
C. unavailable historically

If volume_spike_fresh can be deterministically reconstructed from OHLCV, implement
the same historical calculation.

If it cannot be reconstructed honestly:

- retain RVOL-only historical behavior
- mark ORB volume parity as PARTIAL
- include the limitation in every baseline artifact
- do not claim exact live parity

Do not fabricate a historical volume-spike signal.

----------------------------------------------------
PART 5 — INTRABAR SEMANTICS
----------------------------------------------------

Keep the backtest bar-close signal convention unless historical tick replay is
already available.

Explicitly classify ORB replay as:

BAR_CLOSE_CONSERVATIVE

Document:

- live can react to current price intrabar
- historical OHLCV replay confirms only at candle close
- wick-only breakouts are not accepted unless the live strategy also requires a
  close

Do not attempt to infer unknown intrabar event ordering from OHLCV bars.

----------------------------------------------------
PART 6 — REPAIR COVERAGE SEMANTICS
----------------------------------------------------

The current coverage response reportedly includes:

"invoked": true

while also claiming the endpoint makes no runtime invocation claim.

Resolve this contradiction.

Preferred fix:

Remove "invoked" from the static coverage response.

Or rename it to an accurate static field such as:

"would_be_dispatched": true

The endpoint must communicate registry eligibility only.

It must not imply that a detector was executed.

Update tests accordingly.

----------------------------------------------------
PART 7 — ADD REAL LOOK-AHEAD TESTS
----------------------------------------------------

Add direct runtime tests proving future isolation.

Required test pattern:

1. Build candles through signal bar i.
2. Compute the detector result at i.
3. Append or modify candles only after i.
4. Recompute the detector result at i.
5. Assert the complete result is identical.

Test separately:

- long signal
- short signal
- no-signal case
- OR high
- OR low
- volume classification
- confirmation candle
- stop
- target

Do not use repeated identical runs as proof of future isolation.

----------------------------------------------------
PART 8 — ADD REAL SESSION-RESET TESTS
----------------------------------------------------

Create a multi-session fixture containing:

Day 1:
- a valid opening range
- an extreme OR high and OR low

Day 2:
- a different opening range
- a breakout that would produce the wrong result if Day 1 leaked

Assert:

- Day 2 or_high comes only from Day 2 bars
- Day 2 or_low comes only from Day 2 bars
- Day 1 values cannot trigger a Day 2 signal
- Day 2 uses its own completed range

Also document the actual reset boundary:

- ET calendar date
- CME trading date
- 18:00 ET
- another boundary

Do not describe calendar-date reset as an 18:00 ET reset unless source proves it.

----------------------------------------------------
PART 9 — SAME-BAR CONFLICT TEST
----------------------------------------------------

Add a dedicated ORB runtime fixture where one candle touches both:

- ORB stop
- ORB target

Assert the exact conservative conflict result.

Do not rely on detector determinism as proof.

Return the exact exit reason and resulting R.

----------------------------------------------------
PART 10 — DIRECT IMMUTABILITY TEST
----------------------------------------------------

Create a pinned fixture for the original four strategies.

Serialize the complete results for those strategies before and after ORB
registration using deterministic key ordering.

Compare all supported fields, including:

- trades
- every trade field
- entries
- exits
- stops
- targets
- reasons
- timestamps
- wins
- losses
- breakevens
- win rate
- Net R
- average R
- profit factor
- maximum drawdown
- ranking

Assert serialized equality for the original four strategies.

Do not describe selected metric equality as byte-identical complete output.

----------------------------------------------------
PART 11 — HISTORICAL DATA READINESS AUDIT
----------------------------------------------------

The previous report found no real historical CSV data in the repository.

Audit the existing application for any supported historical-data sources:

- stored Databento files
- downloaded OHLCV
- database tables
- backtest uploads
- cached market data
- object storage
- attached assets
- existing import endpoints

For each source report:

- instrument
- schema
- timeframe
- start date
- end date
- bar count
- timezone
- volume availability
- whether accessible by the backtester
- whether accessing it makes a network call
- cost implications

Do not download paid data yet.

Do not modify live Databento subscriptions.

----------------------------------------------------
PART 12 — ADD A SAFE HISTORICAL IMPORT PATH
----------------------------------------------------

If no existing stored-data path is usable, add a research-only import path for
historical OHLCV files.

Support at minimum:

- CSV
- timestamp
- open
- high
- low
- close
- volume
- instrument
- timeframe

Requirements:

- owner-only
- research/backtest-only
- no live state mutation
- no Decision Quality mutation
- no learning mutation
- no execution calls
- no TradersPost calls
- validates timestamp order
- rejects duplicate timestamps or reports them clearly
- validates OHLC consistency
- validates instrument
- normalizes timestamps to ET while retaining original timezone metadata
- reports missing intervals
- stores imported datasets separately from live market state
- fingerprints imported files
- does not silently overwrite datasets

Add a small read-only inventory view to the backtest page showing available
historical datasets.

Do not fetch data automatically in this phase.

----------------------------------------------------
PART 13 — REQUIRED VALIDATION
----------------------------------------------------

Run separately:

- new ORB geometry parity tests
- new look-ahead tests
- new session-reset tests
- new same-bar conflict test
- new complete-result immutability test
- historical import tests, if import was added
- ORB adapter tests
- coverage audit tests
- Decision Quality tests
- Strategy Scan tests
- Stop Parity tests
- all backtest-related tests
- all four golden workflows

For any claimed pre-existing failure:

1. Check out or run against the direct parent commit.
2. Use the same command and environment.
3. Show that it fails there too.

Git history alone is not sufficient proof.

----------------------------------------------------
PART 14 — COMMIT
----------------------------------------------------

Commit only Phase 6A.1 files.

Suggested commit message:

Repair ORB backtest parity and data readiness

Return:

- starting HEAD
- ending commit
- exact files committed
- final git status
- untracked task attachments
- test counts

----------------------------------------------------
PART 15 — CLOSURE REPORT
----------------------------------------------------

Return:

1. Implementation commit hash
2. Test commit hash
3. Phase 6A.1 commit hash
4. Verified live ORB stop rule
5. Verified live ORB target rule
6. Historical stop rule
7. Historical target rule
8. Exact parity status
9. Volume parity status
10. Intrabar parity status
11. Coverage-field correction
12. Look-ahead runtime proof
13. Session-reset runtime proof
14. Same-bar conflict runtime proof
15. Complete-result immutability proof
16. Historical-data inventory
17. Historical import capability
18. Dataset limitations
19. Focused test counts
20. Golden results
21. Confirmed pre-existing failures
22. Live scoring unchanged
23. Execution unchanged
24. Decision Quality unchanged
25. Learning unchanged
26. Databento live ingestion unchanged
27. Whether Phase 6B is now safe to begin

After returning the report, stop.

DO NOT BEGIN PHASE 6B.