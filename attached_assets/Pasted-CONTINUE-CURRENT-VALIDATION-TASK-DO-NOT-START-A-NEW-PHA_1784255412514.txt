CONTINUE CURRENT VALIDATION TASK — DO NOT START A NEW PHASE

The validation remains incomplete.

Do not add features.
Do not change trading logic.
Do not add a debug endpoint.
Do not implement SWEEP_RECLAIM_SHORT.
Keep THESIS_ENFORCEMENT_MODE=shadow.

==================================================
1. COMPLETE PRODUCTION DISCORD VERIFICATION
==================================================

DISCORD_LIVE=1 has been added as a production-only environment variable, but it has not yet been proven active in the deployed production process.

During a verified non-trading period:

1. Confirm before restart:
   - no open managed position
   - no working order
   - no pending TradersPost submission
   - no active order-management process
   - current thesis snapshots recorded
   - current Discord message count recorded

2. Restart or republish the actual production deployment so the new environment value is loaded.

3. Capture the production boot log proving:
   - DISCORD_LIVE_ENABLED=True
   - THESIS_ENFORCEMENT_MODE=shadow
   - the application is not in analysis-only mode for Discord delivery
   - broker behavior remains under its existing configuration

4. Do not expose secret values or webhook URLs.

5. Send exactly one labeled non-trading Discord test through the production application.

The test must confirm:
   - exactly one Discord message delivered
   - correct channel
   - no trade created
   - no journal outcome created
   - no thesis transition created
   - no shadow evaluation created
   - no TradersPost request generated

The previous local /notify-test result proves webhook plumbing only. Do not present it as production delivery proof.

==================================================
2. COMPLETE THE CONTROLLED PRODUCTION RESTART TEST
==================================================

The local Flask workflow restart does not satisfy the required production restart test.

Use the same verified non-trading production restart from Item 1 and provide before-and-after evidence.

After production restart verify:

- production application starts successfully
- production database reconnects
- dashboard reconnects
- recent eligible thesis state restores safely
- stale thesis state is rejected
- restored READY state cannot submit a trade
- no Discord transition is resent
- no duplicate thesis transition is stored
- no duplicate shadow evaluation is stored
- no duplicate journal entry is created
- no TradersPost request is generated

Report exact counts rather than only saying “none.”

Required restart report:

- restart timestamp
- open managed positions before/after
- working orders before/after
- pending TradersPost submissions before/after
- restored thesis count
- stale thesis rejection count
- duplicate transition count
- duplicate shadow-evaluation count
- duplicate journal-entry count
- Discord resend count
- TradersPost request count after restart

==================================================
3. NATURAL READY EVENT — REMAIN IN OBSERVATION MODE
==================================================

Observe the next active US trading session.

Do not manufacture READY.
Do not lower thresholds.
Do not inject market evidence.
Do not force a trade.

For the first natural READY event capture all fields from the original task, including:

- timestamp
- instrument
- direction
- original verdict
- edge score
- entry
- stop
- targets
- contracts
- risk
- thesis ID
- thesis direction
- thesis status
- thesis confidence
- alignment classification
- shadow decision
- confidence adjustment
- reasons
- Discord result
- persistence result
- TradersPost result

Provide explicit before-and-after assertions proving:

original_verdict_before_shadow == original_verdict_after_shadow

entry_before_shadow == entry_after_shadow
stop_before_shadow == stop_after_shadow
targets_before_shadow == targets_after_shadow
contracts_before_shadow == contracts_after_shadow
risk_before_shadow == risk_after_shadow

Also verify:

- exactly one shadow evaluation
- exactly one eligible Discord message
- no duplicate TradersPost request
- shadow result records WOULD_ALLOW or WOULD_BLOCK
- enforcement remains shadow

==================================================
4. OUTCOME LINKING
==================================================

When the natural READY event resolves, verify linkage to the exact shadow evaluation.

Do not call it a winner or loser unless entry triggered.

Allowed statuses:

- UNTRIGGERED
- OPEN
- PENDING
- COMPLETED

For a completed triggered setup report:

- evaluation linkage
- thesis ID
- instrument
- direction
- alignment
- shadow decision
- final outcome
- final R
- target-first or stop-first
- loss avoided, if applicable
- false block, if applicable

If unresolved at the end of observation, report OPEN or PENDING and leave this item incomplete.

==================================================
5. SWEEP_RECLAIM_SHORT
==================================================

The investigation finding is accepted as provisional.

Do not create:

- /debug-types
- a new status route
- a database browser
- an admin interface
- new logging infrastructure

Use existing production logs, endpoints, and startup diagnostics only.

Before drawing a final conclusion, compare the deployed production revision with the inspected source revision.

Determine whether production is running the same commit/build that contains:

- SWEEP_RECLAIM_SHORT in _SHARED_ALERT_TYPES
- ALERT_TYPES.update(_SHARED_ALERT_TYPES)
- SWEEP_RECLAIM_SHORT in FAST_ENTRY_TYPES

A deployment revision mismatch may explain why static source analysis conflicts with production behavior.

Report:

- inspected source revision
- deployed production revision
- whether they match
- alert/Pine source
- active instruments observed
- expected routing
- actual routing
- likely explanation
- recommendation for later review

Make no code change.

==================================================
6. FINAL STATUS RULE
==================================================

Do not mark the overall validation complete until:

- production Discord is proven after deployment restart
- the controlled production restart is fully evidenced
- one natural READY event passes through shadow evaluation
- verdict and trade parameters are proven unchanged
- persistence occurs exactly once
- outcome linkage is verified or explicitly remains pending
- duplicate counts are documented
- all required tests pass

Until then, report:

VALIDATION STATUS: IN PROGRESS

Do not begin another development phase.