TASK: Left Brain Phase 2 — Observation Infrastructure Closure and Multi-Session Collection

OBJECTIVE

Close the remaining validation gaps in the Left Brain Dynamic Thesis audit.

Keep the thesis completely display-only.

Do not connect the Left Brain to:

* Right Brain scoring
* trade direction
* edge score
* confidence
* setup approval
* vetoes
* strategy selection
* position sizing
* entries
* stops
* targets
* trade management
* TradersPost
* broker execution

Do not tune classification thresholds during this task.

PART 1 — FIX OBSERVATION RETENTION

Current issue:

_LB_THESIS_OBS_BY_INST uses maxlen=120.

At approximately one observation per minute, this retains only about two hours and cannot support the required collection of two full trading sessions.

Increase the bounded retention capacity to support at least three complete futures trading days per instrument.

Use:

maxlen=5000

Keep the storage:

* in memory
* bounded
* isolated per instrument
* diagnostics-only

Do not add database writes.

Add these fields to each observation if they are not already present:

* ts
* instrument
* direction
* strength
* momentum
* established_at
* last_updated_at
* market_state
* session_character
* session_phase
* auction_control
* bullish_pct
* bearish_pct
* neutral_pct
* data_confidence
* top_playbook
* top_playbook_fit_score
* rapid_flip_warning
* time_in_current_thesis_min
* number_of_transitions
* vwap_source
* vwap_age_ms
* MI input timestamp

Prevent two observations for the same instrument and completed-bar timestamp.

PART 2 — ADD OBSERVATION METADATA

Update GET /lb-thesis-obs so its response includes:

{
“ok”: true,
“flag_enabled”: true,
“generated_at”: “…”,
“retention”: {
“max_observations_per_instrument”: 5000,
“estimated_minutes”: 5000
},
“instruments”: {
“MGC”: {
“count”: 0,
“oldest_ts”: null,
“newest_ts”: null,
“observations”: []
}
}
}

Support query parameters:

* inst=MGC
* limit=500
* summary=1

Rules:

* limit must be bounded between 1 and 5000
* invalid instruments return HTTP 400
* observations remain newest-first or clearly document the returned order
* summary=1 may omit the full observation list and return calculated metrics only
* this endpoint must remain owner-only through the existing proxy/auth path

PART 3 — FIX PLAYBOOK ORDERING

Reproduce the reported case where:

* RANGE_FADE fit_score = 85
* AVOID_CHOPPY_CONDITIONS fit_score = 70
* AVOID_CHOPPY_CONDITIONS appears first

Inspect every path that creates, filters, slices, copies, serializes, or displays the playbook list.

Required behavior:

1. Playbooks must be sorted by fit_score descending.
2. Equal fit scores must use a deterministic secondary ordering by canonical playbook name.
3. The first playbook must always be the highest-scoring playbook.
4. The ordering must be identical across repeated calls with identical inputs.
5. Do not change fit-score calculations.

Add regression tests using the exact 85-versus-70 case.

PART 4 — IMPROVE OUTLOOK-SHIFT EVIDENCE

For OUTLOOK_SHIFT memory events, guarantee at least one human-readable evidence item.

Preferred evidence order:

1. Existing current MI supporting evidence
2. Current thesis narrative evidence
3. Deterministic fallback derived from the actual probability change

Example fallback:

“Bearish directional weight changed from 0% to 20%.”

Requirements:

* evidence must never contain blank strings
* evidence must correspond to the current event
* do not copy stale evidence from a prior bar
* do not invent market facts not present in the MI result
* keep the existing reason field

Add tests for:

* bullish probability shift
* bearish probability shift
* neutral probability shift
* empty MI supporting evidence
* repeated identical input
* no stale evidence leakage

PART 5 — VERIFY THE REAL DEPLOYED ENVIRONMENT

The previous audit verified the development process only.

Verify the actual deployed application serving the production domain.

Report separately:

DEV:

* commit hash
* process boot timestamp
* feature-flag value
* /lb-thesis response
* /lb-thesis-obs response

PRODUCTION:

* deployed revision or commit hash
* deployment timestamp
* process boot timestamp
* feature-flag value from the running process
* /lb-thesis response through the authenticated production proxy
* /lb-thesis-obs response through the authenticated production proxy

Do not call the dev environment “production.”

If the production deployment is not running commit 64c8a25 or a later commit containing Phase 2, report that clearly.

Do not deploy or republish unless deployment is explicitly part of the current authorized workflow.

PART 6 — ADD A DISPLAY-ONLY THESIS VIEW

The prior dashboard audit could not validate thesis display because no dedicated display exists.

Add a display-only Left Brain Dynamic Thesis section inside the existing Left Brain area.

Do not create another top-level dashboard system.

Display:

* instrument
* direction
* strength
* momentum
* established time
* last update time
* narrative
* weakens-if conditions
* fails-if conditions
* ranked playbooks with fit scores
* stability note
* time in current thesis
* rapid-flip warning
* newest significant timeline events

Required UI states:

1. Feature disabled
2. Cold start / thesis null
3. Thesis available
4. Missing optional evidence
5. Empty timeline
6. Rapid-flip warning
7. API error
8. Instrument switch

Requirements:

* read-only
* no controls that alter trading
* no polling faster than existing dashboard refresh cadence
* no duplicate endpoint requests if /status already contains the data
* no new backend computation
* escape all rendered text
* no innerHTML with unescaped API content
* mobile-safe layout
* no console errors

PART 7 — BASELINE THE FULL-SUITE FAILURES

The prior report classified 20 failures as pre-existing.

Prove this using the same test command and environment.

Run the full suite on:

A. Parent commit before the new observation-infrastructure changes
B. Current working commit after the changes

Use the same:

* Python version
* environment variables
* working directory
* test order
* pytest options

Produce a comparison table containing:

* test name
* parent result
* current result
* classification
* notes

Do not rely only on tests passing individually.

If checking out the parent commit would risk the active workspace, use a temporary git worktree or another safe isolated copy.

Do not modify unrelated tests to make the suite pass.

PART 8 — COLLECT MULTI-SESSION DATA

Collect at least two complete active market sessions after the observation-retention fix.

Coverage must include, when available:

* Opening Drive
* Morning Session
* Midday
* Power Hour
* Close Approach

For each of MGC, MNQ, MES, and MYM report:

* observation count
* first timestamp
* last timestamp
* coverage duration
* missing-bar gaps
* observations by session phase
* direction distribution
* momentum distribution
* transitions per hour
* median thesis duration
* rapid-flip warnings
* A → B → A oscillations
* direction changes lasting fewer than 3 bars
* average strength
* average data confidence
* percentage of observations with Databento VWAP
* stale-data observations
* top-playbook distribution
* playbook-ordering violations
* empty-evidence events

Do not claim completion if only Close Approach or another narrow session was observed.

PART 9 — ADVISORY-READINESS CRITERIA

Evaluate these criteria without changing the model:

* zero money-path differences
* zero cross-instrument leakage
* zero malformed outputs
* zero playbook ordering violations
* zero blank OUTLOOK_SHIFT evidence
* no stale thesis after restart
* no duplicate observations for one bar
* full requested session coverage
* rapid-flip behavior measured with sufficient data
* no new full-suite failures
* production deployment state clearly verified
* dashboard thesis view operates without console errors

Return exactly one recommendation:

* KEEP IN DISPLAY-ONLY SHADOW MODE
* READY FOR DISPLAY-ONLY PRODUCTION USE
* READY FOR RIGHT-BRAIN DISAGREEMENT DISPLAY
* ROLLBACK REQUIRED

Do not implement Right Brain integration.

“READY FOR RIGHT-BRAIN DISAGREEMENT DISPLAY” means only that a future UI may compare Left Brain and Right Brain opinions. It does not authorize score influence, vetoes, or execution changes.

PART 10 — TESTS

Add or update focused tests for:

* observation retention capacity
* duplicate-bar observation prevention
* instrument isolation
* endpoint limit validation
* invalid instrument response
* summary-only response
* oldest/newest timestamp metadata
* playbook descending ordering
* deterministic equal-score ordering
* OUTLOOK_SHIFT fallback evidence
* stale evidence prevention
* cold-start UI state
* disabled-feature UI state
* rapid-flip UI state
* safe text escaping
* no money-path influence

Run:

* all Left Brain tests
* endpoint tests
* dashboard tests
* decision-trace tests
* scalp-quality tests
* TradersPost tests
* parity and golden suites
* complete test suite

FINAL REPORT

Return:

1. Files changed
2. Commit hash
3. Exact observation retention capacity
4. Endpoint schema
5. Playbook ordering root cause and fix
6. Evidence fallback implementation
7. Dev versus production verification
8. Dashboard validation evidence
9. Parent-versus-current full-suite comparison
10. Multi-session statistics by instrument
11. Execution parity result
12. New and pre-existing test failures
13. Final recommendation

Do not connect the Left Brain thesis to trading behavior.