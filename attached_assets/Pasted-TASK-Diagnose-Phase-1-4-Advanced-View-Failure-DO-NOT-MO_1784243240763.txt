TASK: Diagnose Phase 1.4 Advanced View Failure

DO NOT MODIFY ANY CODE.

OBSERVED PROBLEM

The Advanced View toggle appears to change state, but all dashboard panels remain visible whether Advanced View is ON or OFF.

The previous automated tests passed, but the actual browser behavior is incorrect.

OBJECTIVE

Determine exactly why ADVANCED panels are not hiding in the real rendered dashboard.

DO NOT attempt a fix yet.

INSPECT THESE ITEMS

1. Toggle state

When Advanced View is switched OFF, report the actual runtime values of:

- document.documentElement.dataset.adv
- localStorage.getItem('dashAdv')
- the toggle button active/inactive state
- any checkbox, aria-pressed, class, or dataset value used by the toggle

2. Rendered panel markup

For each of these panels, inspect the actual DOM element after the dashboard loads:

- mod-analyst
- mod-report
- mod-pro
- mod-entryq
- mod-debate
- mod-strategy
- mod-sessionq
- mod-trademgmt
- mod-learning
- mod-memory
- mod-governor
- mod-assistant
- mod-training
- mod-swingstrat

For each panel report:

- Exact DOM ID
- Exact class list
- Whether it has data-category
- Whether it has cat-advanced
- Whether it has data-adv
- Whether it is inside #view-live
- Its computed display value when Advanced View is OFF
- The CSS rule currently winning for display
- The source file and line number of that winning rule

3. CSS selector

Report the exact CSS selector intended to hide ADVANCED panels.

Confirm whether that selector actually matches the rendered panel elements.

Do not assume the category badge text controls visibility.

4. JavaScript

Trace toggleAdvPanels() and report:

- What element it updates
- What value it applies
- Whether another function changes that value afterward
- Whether dashboard refresh/render logic restores all panels to display:block
- Whether inline styles override the Advanced View CSS
- Whether section switching re-shows the panels

5. Browser test

In the actual running browser:

- Load the dashboard
- Turn Advanced View OFF
- Inspect mod-analyst
- Report its computed display
- Turn Advanced View ON
- Inspect mod-analyst again
- Report its computed display

Repeat for mod-learning and mod-assistant.

6. Identify the single root cause

Choose the actual cause from evidence, such as:

- CSS selector does not match the panels
- Category classes are on badges instead of panel containers
- Inline display styles override CSS
- Toggle updates the wrong DOM element
- Render function resets panel visibility
- Browser is serving stale code
- Duplicate CSS rule wins later
- Panels are rendered outside the targeted container
- Another confirmed cause

RESTRICTIONS

- Do not edit files.
- Do not rewrite the toggle.
- Do not add new CSS.
- Do not hide panels manually.
- Do not proceed to Phase 1.5.
- Do not claim success based only on unit or golden tests.
- The diagnosis must be based on the actual rendered browser DOM.

FINAL RESPONSE

Return:

1. Exact root cause
2. Evidence from the rendered DOM
3. The CSS rule that should hide the panels
4. The CSS rule or inline style currently preventing it
5. The smallest possible fix
6. Exact file and line range that would need modification
7. Confirmation that no code was changed