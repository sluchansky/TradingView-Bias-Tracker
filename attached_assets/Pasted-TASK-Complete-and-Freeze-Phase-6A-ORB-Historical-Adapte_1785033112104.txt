TASK: Complete and Freeze Phase 6A — ORB Historical Adapter

DO NOT begin Phase 6B.

The ORB adapter implementation is complete and the following have already passed:

- ORB adapter tests: 35/35
- Backtest coverage audit: 27/27
- Stop parity: 4/4
- Golden parity workflow: PASS

Finish Phase 6A validation, representative replay evidence, commit, and closure
report only.

----------------------------------------------------
PART 1 — VERIFY CURRENT WORKING TREE
----------------------------------------------------

Run:

git branch --show-current
git rev-parse HEAD
git status --short
git diff --check
git diff --stat

Return:

- current branch
- starting HEAD
- every modified file
- every untracked file
- confirmation that pasted task attachments are not staged

Do not make additional implementation changes unless required to fix a failing
Phase 6A test.

----------------------------------------------------
PART 2 — COMPLETE ALL FOCUSED VALIDATION
----------------------------------------------------

Run these suites separately:

1. ORB adapter tests
2. Backtest coverage audit tests
3. Decision Quality tests
4. Strategy Scan tests
5. Stop Parity tests
6. Every other existing backtest-related test file
7. All four golden workflows

Expected known counts include:

- ORB adapter: 35/35
- Coverage Audit: 27/27
- Decision Quality: 103/103
- Strategy Scan: 30/30
- Stop Parity: 4/4

Report every suite separately.

Also provide the exact combined focused total.

Do not call this the full repository regression unless every applicable test file
in the repository is run.

----------------------------------------------------
PART 3 — VERIFY THE ACTUAL ORB IMPLEMENTATION
----------------------------------------------------

Return the exact Phase 6A implementation details:

1. ORB live scorer function and line
2. Historical detector function and line
3. Backtest STRATEGY_DEFS entry and line
4. DETECTORS entry and line
5. STRATEGY_ORDER entry and line
6. Dropdown option and line
7. BT_STRAT_LABELS entry and line
8. Coverage endpoint result for ORB
9. Coverage eligible_count
10. EXHAUSTION_FADE status

Explicitly confirm the historical detector uses the existing snapshot fields:

- or_high
- or_low
- or_complete
- VWAP-related fields
- ATR/volatility fields
- volume fields
- current and prior candles only

State whether any new session tracker or mutable global state was introduced.

----------------------------------------------------
PART 4 — LIVE-VERSUS-HISTORICAL RULE MAPPING
----------------------------------------------------

Return a complete table:

Live ORB rule
Historical ORB equivalent
Parity status
Known limitation

Include:

- opening-range start
- opening-range end
- timezone
- range-complete rule
- earliest signal time
- long breakout rule
- short breakout rule
- close-versus-wick requirement
- VWAP requirement
- volume requirement
- ATR requirement
- entry
- stop
- target
- reward-to-risk
- invalidation
- session reset
- cooldown behavior
- mode applicability
- instrument applicability

Do not merely say “matches live.”

Show the exact historical reconstruction used.

----------------------------------------------------
PART 5 — LOOK-AHEAD AND EXECUTION SEMANTICS
----------------------------------------------------

Document:

1. The historical snapshot available to the detector at bar index i
2. Confirmation that candles after index i are unavailable
3. How OR high and OR low are calculated
4. When or_complete becomes true
5. Whether the signal is generated at current-bar close or next-bar open
6. How entry is simulated
7. How stop and target are simulated
8. The same-bar stop/target conflict rule
9. Whether future-candle mutation tests passed
10. Whether previous-session leakage tests passed

Return the exact test names proving these claims.

----------------------------------------------------
PART 6 — DIRECT METRIC IMMUTABILITY
----------------------------------------------------

Run pinned baseline fixtures for the original four strategies before and after the
ORB registry addition.

Compare directly:

- total trades
- wins
- losses
- breakeven trades, if supported
- win rate
- Net R
- average R
- profit factor
- maximum drawdown
- ranking order

Do not infer equality from trade-count equality.

Return a before-versus-after table for:

- OPENING_DRIVE
- LIQUIDITY_SWEEP_REVERSAL
- VWAP_TREND_CONTINUATION
- RANGE_EXPANSION_BREAKOUT

Confirm byte-identical output where the fixture supports byte-level comparison.

----------------------------------------------------
PART 7 — REPRESENTATIVE ORB REPLAY RUNS
----------------------------------------------------

Run ORB independently for:

- MES SCALP 5m
- MES SWING 5m
- MNQ SCALP 5m
- MGC SCALP 5m
- MYM SCALP 5m

Use the best existing stored historical dataset available for each instrument.

For every run report:

- historical source
- date range
- timeframe
- total bars
- sessions represented
- detector dispatch count
- range-complete snapshots
- candidate long signals
- candidate short signals
- blocked signals by reason
- completed trades
- wins
- losses
- breakeven trades
- win rate
- Net R
- average R
- profit factor
- maximum drawdown
- average holding time

If no suitable dataset exists for a requested run:

- do not invent results
- state the exact missing-data reason
- run the largest honest subset available
- clearly distinguish synthetic-test evidence from real historical evidence

Do not optimize ORB.

Do not change parameters to produce more trades.

----------------------------------------------------
PART 8 — VERIFY PROTECTED BOUNDARIES
----------------------------------------------------

Confirm with source diff and tests that Phase 6A did not change:

- live ORB scorer
- other live strategy scorers
- Edge Score
- verdict selection
- gates
- learning
- Decision Quality
- trade sizing
- trade management
- auto-trade eligibility
- TradersPost
- Databento ingestion
- Databento subscriptions
- Databento normalization
- research strategy registries

Return exact evidence, not only a general statement.

----------------------------------------------------
PART 9 — COVERAGE ENDPOINT
----------------------------------------------------

Call or evaluate GET /backtest/coverage and return the resulting ORB section.

Expected:

{
  "eligible": true,
  "in_live": true,
  "in_bt": true,
  "has_detector": true,
  "in_order": true,
  "disabled": false,
  "reason": null
}

Expected eligible_count:

5

Also confirm:

- source remains "static_registry"
- the endpoint does not launch a backtest
- the endpoint does not claim runtime invocation
- EXHAUSTION_FADE remains disabled
- research strategies remain excluded

----------------------------------------------------
PART 10 — TEST GAP REVIEW
----------------------------------------------------

Map all 35 ORB tests to the Phase 6A requirements.

For each test return:

- exact test name
- requirement covered
- runtime proof, static proof, or structural proof

Identify any requirement not actually proven.

Pay special attention to:

- live scorer unchanged
- exact live/historical geometry parity
- future-bar isolation
- same-bar conflict handling
- previous-session reset
- zero-trade result inclusion
- all-strategy invocation
- ORB-only selection
- direct metric immutability
- no Databento calls
- no execution calls
- coverage endpoint does not run a backtest

Do not present a source grep as runtime proof.

----------------------------------------------------
PART 11 — OPTIONAL FULL REPOSITORY RUN
----------------------------------------------------

Run the full repository test suite if practical.

If run:

- report exact command
- total pass/fail count
- every failure
- whether each failure reproduces on the parent commit

If not run:

state:

Full repository regression not run.

Do not relabel the focused suite as full regression.

----------------------------------------------------
PART 12 — COMMIT
----------------------------------------------------

Only after all required Phase 6A validation passes:

Run:

git diff --check
git status --short

Commit only the Phase 6A files.

Expected files may include:

- artifacts/tradingview-webhook/backtest_engine.py
- artifacts/tradingview-webhook/app.py
- artifacts/tradingview-webhook/test_backtest_orb_adapter.py
- artifacts/tradingview-webhook/test_backtest_coverage_audit.py

Do not commit pasted task attachments.

Suggested commit message:

Add Opening Range Breakout historical adapter

After committing, run:

git rev-parse HEAD
git log -1 --oneline
git status --short

Return:

- commit hash
- exact committed files
- final working-tree status

----------------------------------------------------
PART 13 — FINAL CLOSURE REPORT
----------------------------------------------------

Return:

1. Branch
2. Starting HEAD
3. Audit checkpoint hash
4. Ending commit hash
5. Working tree before
6. Working tree after
7. Exact files changed
8. Exact files committed
9. Live ORB rules
10. Historical reconstruction
11. Opening-range time window
12. Timezone handling
13. Signal timing
14. Entry timing
15. Same-bar conflict policy
16. ORB registry changes
17. Dashboard changes
18. Coverage before and after
19. Look-ahead prevention evidence
20. Session-reset evidence
21. Existing four-strategy metric parity
22. Representative historical replay results
23. ORB test count
24. Coverage test count
25. DQ test count
26. Strategy Scan test count
27. Stop Parity test count
28. Other backtest test results
29. Golden workflow results
30. Full repository result, if actually run
31. EXHAUSTION_FADE confirmation
32. Research strategy exclusion confirmation
33. Live scoring unchanged confirmation
34. Decision Quality unchanged confirmation
35. Execution unchanged confirmation
36. Learning unchanged confirmation
37. Databento unchanged confirmation
38. Remaining historical-versus-live limitations
39. Recommendation for Phase 6B

After returning the report, stop.

DO NOT START PHASE 6B.