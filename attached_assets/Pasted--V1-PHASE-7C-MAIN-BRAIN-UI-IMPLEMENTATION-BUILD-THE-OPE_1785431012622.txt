# V1 PHASE 7C — MAIN BRAIN UI IMPLEMENTATION
# BUILD THE OPERATOR CONSOLE FROM THE APPROVED MOCKUP
# USE THE EXISTING /main-brain PAYLOAD
# DO NOT CHANGE TRADING LOGIC
# DO NOT DEPLOY UNTIL VALIDATION IS COMPLETE

Accepted backend endpoint:

72c8141 — V1-P7B Main Brain read-only route

Canonical dashboard data source:

GET /main-brain

Approved visual target:

The attached Main Brain dashboard mockup is the North Star for layout, visual
hierarchy, styling, panel organization, and operator experience.

Phase objective:

Build the new Main Brain Operator Console using the existing `/main-brain`
payload.

This is a UI implementation phase.

Do not redesign the trading engine.

Do not invent new analytics.

Every displayed live value must come from `/main-brain` or be an explicitly
labeled display-only placeholder.

=========================================================
PRIMARY DELIVERABLE
=========================================================

Create a polished Main Brain dashboard that visually follows the attached
mockup as closely as practical.

The page must present the platform as one unified AI trading system rather than
a collection of unrelated modules.

The dashboard should let the operator understand within approximately ten
seconds:

- what the market is doing
- what the Main Brain believes
- which strategy is favored
- whether a trade is active
- whether execution is safe
- what Coach is learning
- whether any subsystem is unavailable
- what happened most recently

=========================================================
NON-NEGOTIABLE RULES
=========================================================

Do not change:

- Databento ingestion
- Databento subscriptions
- market-data calculations
- thesis generation
- thesis resolution
- verdict generation
- confidence
- edge score
- edge grade
- actionability
- strategy selection
- scanner rankings
- strategy universe
- learning weights
- Coach event semantics
- Journal persistence
- execution gateway behavior
- gateway status
- gateway outcome
- duplicate suppression
- risk
- sizing
- entries
- stops
- targets
- active-trade management
- broker routing
- broker payloads
- database schema
- authentication policy
- deployment configuration

Do not add fake trading values merely to match the mockup.

Do not expose the 16 paper-research strategies as live production strategies.

Do not replace the existing `/main-brain` data contract with UI-specific logic.

=========================================================
DOCUMENT PRECEDENCE
=========================================================

Read completely before editing:

1. SYSTEM_ARCHITECTURE_V1.md
2. IMPLEMENTATION_ROADMAP_V1.md
3. PHASE_7_MAIN_BRAIN_ROUTING_BRIEF.md
4. V1_PHASE_7B_MAIN_BRAIN_ROUTE_VALIDATION.md
5. PRODUCT_SPEC_V1.md
6. PLATFORM_BLUEPRINT.md
7. V1_PHASE_6_VALIDATION.md
8. V1_PHASE_5_VALIDATION.md
9. V1_PHASE_4_VALIDATION.md

Use this precedence:

1. SYSTEM_ARCHITECTURE_V1.md
2. IMPLEMENTATION_ROADMAP_V1.md
3. PHASE_7_MAIN_BRAIN_ROUTING_BRIEF.md
4. V1_PHASE_7B_MAIN_BRAIN_ROUTE_VALIDATION.md
5. PRODUCT_SPEC_V1.md
6. PLATFORM_BLUEPRINT.md

If the documents conflict on:

- route ownership
- authentication
- dashboard entry path
- field meaning
- strategy count
- timeline semantics
- Coach semantics
- execution state
- active-trade ownership

STOP and report the conflict.

Do not invent a compromise.

=========================================================
STAGE 0 — PRODUCTION CHECKPOINT AND BASELINE FREEZE
=========================================================

Before editing, record:

- branch
- HEAD
- git status
- tracked modifications
- untracked files
- git diff --stat
- git diff --check
- recent commits
- whether 72c8141 is an ancestor of HEAD
- every commit after 72c8141
- classification of each intervening commit

Classify each intervening commit as:

- production implementation
- dashboard/UI
- test-only
- documentation-only
- platform checkpoint
- attached asset
- unexplained

Inspect every production or dashboard commit after 72c8141.

Stop on unexplained changes.

Confirm the latest republished deployment is healthy before beginning:

- production boot succeeded
- existing dashboard loads
- authentication works
- `/status` is healthy
- `/main-brain` returns the documented payload
- no startup exceptions
- no unexpected broker activity
- no Databento regression

Do not republish again during implementation.

=========================================================
STAGE 1 — BASELINE VALIDATION
=========================================================

Run all accepted backend suites before UI work:

python3 artifacts/tradingview-webhook/test_phase7b_main_brain_route.py

python3 artifacts/tradingview-webhook/test_phase6_journal_coach.py

python3 artifacts/tradingview-webhook/test_phase5_execution_safety.py

python3 artifacts/tradingview-webhook/test_phase4_operator_explanation.py

python3 artifacts/tradingview-webhook/test_phase3_thesis_verdict_pipeline.py

python3 artifacts/tradingview-webhook/test_v1_interface_versions.py

python3 artifacts/tradingview-webhook/test_phase2_market_data_reliability.py

bash artifacts/tradingview-webhook/checks/run_phase2_smoke.sh

Also run:

- Phase 2 smoke from /tmp
- parity
- scalp_golden
- dual_sim
- breakout_mode
- Python syntax validation
- current frontend/Node syntax validation
- git diff --check

Expected minimum:

- Phase 7B: 56 passing
- all accepted earlier suites passing
- all primary regressions passing
- no changed golden output

If baseline validation fails:

STOP.

Do not alter production logic or weaken tests to proceed.

=========================================================
STAGE 2 — DISCOVER THE CURRENT DASHBOARD ARCHITECTURE
=========================================================

Before writing UI code, identify:

- current main dashboard route
- current legacy analysis dashboard route
- current authentication middleware
- current HTML entry files
- current CSS files
- current JavaScript files
- current navigation links
- current asset paths
- current Flask or Express proxy behavior
- current dashboard polling logic
- current mobile behavior
- current error and loading states
- current selected-instrument behavior

Document the exact files and functions that will change.

Do not create a second disconnected dashboard application unless the existing
architecture makes that necessary.

Prefer integrating the Main Brain page into the current authenticated dashboard
shell.

=========================================================
ROUTE AND NAVIGATION CONTRACT
=========================================================

Use the route approved by the current dashboard architecture.

Preferred behavior:

- authenticated home route opens Main Brain
- existing analysis dashboard remains accessible through navigation
- existing diagnostics pages remain accessible
- no existing page is deleted
- no existing URL breaks without an explicit redirect
- no authentication bypass is introduced

The Main Brain should become the primary operator landing page only if the
existing route structure can support this safely.

If changing the default landing route would break existing links:

- retain the old route
- create a new authenticated Main Brain route
- add a clear Main Brain navigation link
- document the deferred landing-page switch

=========================================================
VISUAL DESIGN TARGET
=========================================================

Follow the attached mockup for:

- dark futuristic appearance
- dense but readable operator layout
- blue/cyan primary accents
- green success states
- orange warning states
- red danger states
- purple learning/Coach accents
- subtle glow treatment
- rounded panels
- strong visual hierarchy
- compact status badges
- clear spacing
- professional chart styling
- left navigation rail
- top system header
- multi-column desktop layout
- responsive stacking on smaller screens

Do not copy fake values from the image.

The image defines presentation, not data.

=========================================================
DESIGN SYSTEM
=========================================================

Create reusable CSS variables or design tokens for:

- page background
- secondary background
- panel background
- elevated panel background
- border
- muted border
- primary cyan
- secondary blue
- success green
- warning orange
- danger red
- Coach purple
- primary text
- secondary text
- muted text
- spacing scale
- border-radius scale
- shadow scale
- glow scale
- typography scale
- transition speed

Suggested hierarchy:

- page title
- large metric
- section heading
- card heading
- body
- secondary text
- label
- badge

Use accessible contrast.

Do not use color alone to indicate status.

Include text labels or icons with color.

=========================================================
MAIN BRAIN PAGE STRUCTURE
=========================================================

Build the following layout.

=========================================================
1. APPLICATION SHELL
=========================================================

Include:

- left navigation rail
- Main Brain brand/logo area
- navigation links
- selected-page state
- compact system-health indicator
- desktop and mobile navigation behavior

Navigation should include existing destinations where available:

- Main Brain
- Left Brain / Analysis
- Strategy Scanner
- Active Trades
- Journal
- Coach
- Diagnostics
- Settings

Do not create dead links.

For destinations not implemented:

- render disabled or “coming later”
- do not route to blank pages

=========================================================
2. TOP HEADER
=========================================================

Display from `/main-brain`:

- selected instrument
- market session state
- date/time
- trading mode
- execution mode
- auto-trade status
- overall system availability

Include an AI summary region.

Only display real summary text if an existing canonical field supports it.

Otherwise display a neutral state such as:

“Main Brain data is current.”

Do not create new generated commentary in the frontend.

=========================================================
3. MARKET STATE STRIP
=========================================================

Display compact cards for:

- market regime
- primary driver
- risk state
- VWAP state
- volatility
- selected instrument
- session status
- Databento/system readiness

Use:

payload.market
payload.market_state
payload.system_status
payload.availability

Every card needs:

- label
- value
- availability state
- stale or unavailable treatment
- tooltip or expandable details where useful

=========================================================
4. MAIN BRAIN THESIS PANEL
=========================================================

Display:

- thesis direction
- thesis confidence
- thesis narrative
- thesis status
- thesis age
- generated timestamp
- last resolved timestamp
- supporting intelligence where available

Source:

payload.left_brain

Visually emphasize:

- bullish
- bearish
- conflicted
- neutral
- unavailable

Do not translate “conflicted” into bullish or bearish.

When unavailable, show:

“Thesis unavailable”

rather than an invented neutral thesis.

=========================================================
5. VERDICT AND CONFIDENCE PANEL
=========================================================

Display:

- readiness
- edge score
- edge grade
- actionability
- failed conditions
- component breakdown
- direction if canonical
- confidence if canonical

Source:

payload.verdict

Build a prominent confidence/edge visualization similar to the mockup.

The UI must correctly support the documented edge-score range.

Do not assume a 0–100 scale if production returns 0–110.

Clearly label:

- edge score
- confidence
- readiness

as separate concepts where they are separate fields.

Do not combine them into one misleading gauge.

=========================================================
6. STRATEGY SCANNER PANEL
=========================================================

Display the five canonical main-engine strategies:

- OPENING_DRIVE
- LIQUIDITY_SWEEP_REVERSAL
- VWAP_TREND_CONTINUATION
- RANGE_EXPANSION_BREAKOUT
- OPENING_RANGE_BREAKOUT

For each strategy display only available fields such as:

- name
- direction
- readiness
- selected status
- canonical score/confidence
- failed conditions
- availability
- mode compatibility

Highlight the selected strategy.

Clearly mark unavailable fields.

Do not show fabricated win rates or expectancy.

Do not include paper-research strategies in this primary panel.

Include a later-drilldown affordance only if a real scanner page exists.

=========================================================
7. TRADE PLAN PANEL
=========================================================

Where a canonical trade plan exists, display:

- entry
- stop
- targets
- risk/reward
- direction
- setup
- timeframe
- status

Where no plan exists, show:

“No actionable trade plan”

Do not display zero values as real prices.

Treat absent, null, malformed, and unavailable values distinctly.

=========================================================
8. ACTIVE TRADES PANEL
=========================================================

Display:

- instrument
- direction
- strategy
- entry
- current price
- quantity/contracts
- unrealized P&L
- current R
- stop
- targets
- opened time
- management state

Source:

payload.active_trades

Support:

- zero active trades
- one active trade
- multiple active trades
- partial data
- unavailable source

Do not assume only one active trade.

Do not calculate alternate P&L or R in the browser when canonical values exist.

=========================================================
9. EXECUTION STATUS PANEL
=========================================================

Display:

- execution mode
- gateway status
- gateway outcome where available
- last sent timestamp
- broker readiness
- duplicate suppression state if exposed
- verification-required state if exposed
- failure reason if exposed

Source:

payload.execution_gateway
payload.manager
payload.system_status

Phase 7B deferred `last_outcome`.

Do not fabricate it.

If unavailable, show:

“Last outcome not available”

Do not infer a successful execution solely from a timestamp.

=========================================================
10. COACH PANEL
=========================================================

Display:

- Coach interface version
- weight_updated
- thesis_resolved
- thesis_last_resolved_at
- learning_influence
- rule_engine_eligibility
- performance summary
- win rate
- average R
- sample count
- best setup if available

Sources:

payload.coach
payload.performance

Protect accepted semantics:

- eligibility does not mean an update occurred
- readiness does not mean weight_updated
- database availability does not mean thesis_resolved
- feature enabled does not mean learning influence occurred

Use explanatory labels or tooltips to avoid misinterpretation.

Do not create personalized advice unless a canonical field exists.

=========================================================
11. JOURNAL SUMMARY PANEL
=========================================================

Display:

- today’s trade count
- recent closed trades
- total or recent win rate if available
- average R
- last ten closed trades
- setup
- instrument
- direction
- result
- R multiple
- closed time

Source:

payload.journal
payload.performance

Support:

- empty Journal
- database unavailable
- partial history
- malformed record
- no trades today

Never expose raw database errors.

=========================================================
12. DECISION TIMELINE
=========================================================

Display the partial derived timeline from:

payload.decision_timeline

Each row should show:

- timestamp
- event label
- source/subsystem
- status
- details
- derived marker where applicable

Clearly show the timeline completeness state.

Do not visually imply that absent event types occurred.

If timeline is partial, label it:

“Partial timeline — additional event capture is planned.”

Do not manufacture missing order, Coach, Journal, or strategy events.

=========================================================
13. ALERTS / LIVE FEED
=========================================================

Display the bounded alerts list from:

payload.alerts

Include:

- timestamp
- instrument
- alert type
- message
- severity if available

Support:

- no alerts
- unavailable alert source
- stale alerts
- malformed alert

Do not poll or expose raw application logs.

=========================================================
14. SYSTEM HEALTH
=========================================================

Display compact health indicators for:

- database
- learning
- Databento
- broker readiness
- Left Brain
- scanner
- Manager
- Coach
- Journal
- timeline

Sources:

payload.system_status
payload.availability
payload.errors

Each status needs:

- available/unavailable
- stale/current if provided
- safe error label
- no secret or exception text

=========================================================
DATA FETCHING
=========================================================

Fetch:

GET /main-brain

through the existing authenticated application boundary.

Requirements:

- do not bypass the current proxy/authentication path
- do not place credentials in frontend code
- use one shared fetch function
- handle non-200 responses
- handle invalid JSON
- handle partial schema
- handle authentication expiration
- handle slow responses
- avoid concurrent duplicate polling
- cancel or ignore stale requests
- do not create an aggressive request loop

Use the polling cadence approved in the Phase 7A routing brief.

If no cadence is explicitly approved, use a conservative interval such as
5–10 seconds and document it.

Include a manual refresh control.

Pause or reduce polling when the page is hidden where practical.

=========================================================
FRONTEND STATE MODEL
=========================================================

Implement clear UI states:

- initial loading
- loaded
- refreshing
- partial availability
- stale
- authentication failure
- total route failure
- individual panel failure
- empty state

Do not blank the entire dashboard because one subsystem is unavailable.

Preserve the previous successful payload during transient refresh failure and
show a stale-warning banner.

Do not silently show outdated data as current.

=========================================================
SAFE RENDERING
=========================================================

Frontend rendering must safely handle:

- null
- missing fields
- empty arrays
- empty dictionaries
- string numbers
- malformed numbers
- unexpected booleans
- unexpected lists
- unexpected objects
- invalid timestamps
- very long text
- negative values
- zero values
- edge score over 100
- unknown status strings

Never inject untrusted values with unsafe HTML.

Prefer textContent or safe framework rendering.

Do not use innerHTML for live backend text unless escaped.

=========================================================
RESPONSIVE BEHAVIOR
=========================================================

Desktop:

- multi-column operator console
- persistent navigation rail
- large thesis/verdict area
- side panels for Coach, execution, and health
- full timeline and scanner

Tablet:

- collapsible navigation
- two-column card layout
- reduced chart/gauge size
- timeline below major cards

Mobile:

- single-column layout
- compact header
- collapsible sections
- readable active-trade cards
- no horizontal page overflow
- touch-friendly controls
- no critical data hidden only on hover

Test at representative widths such as:

- 1440 px
- 1280 px
- 1024 px
- 768 px
- 430 px
- 390 px

=========================================================
ACCESSIBILITY
=========================================================

Implement:

- semantic landmarks
- keyboard navigation
- visible focus states
- accessible button labels
- sufficient contrast
- status text in addition to color
- reduced-motion support
- screen-reader labels for gauges
- tables or lists with semantic structure
- no flashing effects
- no critical hover-only information

=========================================================
ANIMATION
=========================================================

Use animation sparingly.

Allowed:

- subtle panel entrance
- gentle refresh indicator
- status-dot pulse for live/current systems
- smooth gauge updates
- mild hover transitions

Do not use:

- distracting constant motion
- large particle effects
- flashing alerts
- animations that obscure values
- heavy GPU effects that reduce reliability

Respect:

prefers-reduced-motion

=========================================================
AUTHORIZED FILES
=========================================================

Use the exact current dashboard architecture discovered in Stage 2.

Expected authorized changes may include:

- current authenticated dashboard HTML/template
- current dashboard CSS
- current dashboard JavaScript
- shared dashboard components
- new Main Brain-specific frontend files
- narrowly scoped route/template wiring if required
- new Phase 7C frontend test file
- screenshot or visual-validation artifacts
- Phase 7C validation document

Do not modify app.py business logic.

A minimal route that serves the UI shell may be allowed only when required by
the existing architecture and must not change `/main-brain` semantics.

If app.py requires more than a narrowly scoped page-serving route:

STOP and report.

=========================================================
TESTS
=========================================================

Create a dedicated Phase 7C test file consistent with the existing frontend
test architecture.

Suggested file:

artifacts/tradingview-webhook/test_phase7c_main_brain_ui.py

Test groups should cover:

- page route
- authentication
- Main Brain fetch behavior
- full payload rendering
- partial payload rendering
- empty payload rendering
- malformed payload handling
- subsystem failure handling
- stale state
- polling behavior
- manual refresh
- no duplicate request loop
- strategy count
- active trade rendering
- Coach semantics
- execution fallback
- timeline partial label
- alerts bounds
- safe text rendering
- responsive structure
- keyboard accessibility
- design-system tokens
- existing dashboard route compatibility
- no backend mutation
- no broker call
- no gateway invocation
- no database write

Use runtime or browser-level evidence where the project environment supports it.

Static source inspection may supplement but not replace behavior tests.

=========================================================
SCREENSHOT VALIDATION
=========================================================

Capture screenshots using deterministic mocked `/main-brain` fixtures for:

1. Full healthy desktop payload
2. Partial subsystem failure
3. No active trades
4. Active trade present
5. Mobile layout
6. Loading state
7. Stale state
8. Total API failure

Compare visually against the approved mockup for:

- page hierarchy
- overall density
- panel placement
- typography hierarchy
- color usage
- navigation treatment
- verdict emphasis
- scanner readability
- Coach visibility
- timeline clarity
- responsive behavior

Do not require pixel-perfect identity where the existing application stack makes
that impractical.

Document material visual differences honestly.

=========================================================
NO-HARDCODED-LIVE-VALUE TEST
=========================================================

Audit the frontend for hardcoded trading values.

Prohibited hardcoded production values include:

- instrument prices
- edge scores
- confidence
- strategy results
- P&L
- R multiples
- stop levels
- target levels
- win rate
- sample counts
- broker outcomes
- thesis direction
- market regime

Static placeholder fixtures may exist only in test files.

Production UI must source them from `/main-brain`.

=========================================================
REGRESSION CONTRACT
=========================================================

Run:

python3 artifacts/tradingview-webhook/test_phase7c_main_brain_ui.py

python3 artifacts/tradingview-webhook/test_phase7b_main_brain_route.py

python3 artifacts/tradingview-webhook/test_phase6_journal_coach.py

python3 artifacts/tradingview-webhook/test_phase5_execution_safety.py

python3 artifacts/tradingview-webhook/test_phase4_operator_explanation.py

python3 artifacts/tradingview-webhook/test_phase3_thesis_verdict_pipeline.py

python3 artifacts/tradingview-webhook/test_v1_interface_versions.py

python3 artifacts/tradingview-webhook/test_phase2_market_data_reliability.py

bash artifacts/tradingview-webhook/checks/run_phase2_smoke.sh

Also run:

- Phase 2 smoke from /tmp
- parity
- scalp_golden
- dual_sim
- breakout_mode
- Python syntax validation
- Node/JavaScript syntax validation
- frontend build if applicable
- authentication tests
- existing dashboard route tests
- git diff --check

Required:

- no backend regression
- no golden-output change
- no trading behavior change
- no new broker request
- no database write from UI load
- no learning update from UI load
- no Journal write from UI load
- no Databento mutation from UI load
- no authentication weakening

=========================================================
VALIDATION DOCUMENT
=========================================================

Create:

artifacts/tradingview-webhook/V1_PHASE_7C_MAIN_BRAIN_UI_VALIDATION.md

Required sections:

1. Baseline State
2. Intervening Commit Audit
3. Phase 7C Scope
4. Existing Dashboard Architecture
5. Files Changed
6. Final Page Route
7. Navigation Changes
8. Design-System Implementation
9. Panel Inventory
10. Main Brain API Integration
11. Field-to-Component Mapping
12. Loading State
13. Empty State
14. Partial Availability State
15. Stale State
16. Total Failure State
17. Strategy Scanner Rendering
18. Active Trade Rendering
19. Execution Status Rendering
20. Coach Semantic Rendering
21. Journal Rendering
22. Timeline Rendering
23. Alerts Rendering
24. System Health Rendering
25. Responsive Results
26. Accessibility Results
27. Authentication Results
28. Safe-Rendering Results
29. Polling and Refresh Results
30. No-Hardcoded-Value Audit
31. Screenshot Evidence
32. Backend Non-Mutation Evidence
33. Regression Evidence
34. Deferred UI Items
35. Known Visual Differences
36. Deployment Status
37. Honest Final Status

=========================================================
DIFF CONTROL
=========================================================

Before committing:

git status
git diff --stat
git diff --check
git diff

For every changed file document:

- purpose
- owning component
- authorized scope
- runtime behavior
- backend effect
- regression risk

Expected changes should be limited to:

- Main Brain UI/template files
- Main Brain styles
- Main Brain JavaScript/components
- narrowly scoped page-route wiring if necessary
- Phase 7C tests
- Phase 7C validation document
- intentionally tracked design asset if required

Do not commit:

- temporary screenshots unless used as validation artifacts
- attached prompt text
- logs
- caches
- local databases
- credentials
- webhook URLs
- unrelated agent outputs

=========================================================
STOP CONDITIONS
=========================================================

Stop immediately if:

- baseline tests fail
- HEAD changes unexpectedly
- the UI requires trading-logic changes
- the UI requires new strategy calculations
- the UI requires a schema migration
- `/main-brain` must be substantially changed
- authentication cannot be preserved
- production trading values must be hardcoded
- browser load invokes the gateway
- browser load sends a broker request
- browser load writes to Journal
- browser load updates learning
- browser load mutates Databento
- existing dashboard routes break
- existing analysis dashboard becomes unreachable
- production secrets appear in frontend code
- primary regressions change
- the attached mockup cannot be accessed or inspected

Do not silently broaden scope.

=========================================================
IMPLEMENTATION ORDER
=========================================================

Implement in this order:

1. Baseline and architecture audit
2. Main Brain route/page shell
3. Design tokens and application shell
4. Shared `/main-brain` fetch/state layer
5. Header and market-state strip
6. Thesis and verdict
7. Strategy scanner and trade plan
8. Active trades and execution
9. Coach and Journal
10. Timeline, alerts, and system health
11. Empty/error/stale states
12. Responsive layout
13. Accessibility
14. Screenshot validation
15. Full regression
16. Validation document
17. Commit

Checkpoint after each major group.

Do not wait until the end to test all panels.

=========================================================
COMMIT CONTROL
=========================================================

If implementation is complete and all tests pass, create one commit:

V1-P7C Main Brain operator console

Do not amend 72c8141.

Do not squash.

Do not deploy automatically.

Do not publish automatically.

Do not begin Phase 7D.

=========================================================
FINAL RESPONSE FORMAT
=========================================================

Report:

1. Phase title
2. Starting branch and HEAD
3. Accepted Phase 7B endpoint
4. Intervening commits
5. Ending branch and HEAD
6. Baseline test results
7. Main Brain page route
8. Existing dashboard routes preserved
9. Authentication boundary
10. Files created
11. Files modified
12. Files deleted
13. Navigation implementation
14. Design tokens implemented
15. UI components implemented
16. Main Brain API integration
17. Polling interval
18. Manual refresh result
19. Loading-state result
20. Empty-state result
21. Partial-availability result
22. Stale-state result
23. Total-failure result
24. Market-state result
25. Thesis result
26. Verdict result
27. Strategy scanner result
28. Strategy count displayed
29. Research-strategy handling
30. Trade-plan result
31. Active-trades result
32. Execution-status result
33. Coach result
34. Journal result
35. Timeline result
36. Timeline partial-label result
37. Alerts result
38. System-health result
39. Safe-rendering result
40. Hardcoded-live-value audit
41. Desktop screenshot result
42. Tablet result
43. Mobile result
44. Accessibility result
45. Authentication result
46. Frontend syntax/build result
47. Phase 7C test count
48. Phase 7B result
49. Phase 6 result
50. Phase 5 result
51. Phase 4 result
52. Phase 3 result
53. Interface result
54. Phase 2 result
55. Smoke results
56. Primary regressions
57. Gateway invocation count from UI tests
58. Broker-call count from UI tests
59. Journal-write count from UI tests
60. Learning-write count from UI tests
61. Database-write count from UI tests
62. Databento mutation count
63. Backend production logic changes
64. Known visual differences from the mockup
65. Deferred UI items
66. Validation document
67. Commit created
68. Confirmation no deployment occurred
69. Whether Phase 7C is officially complete
70. Whether the project is ready for controlled preview deployment

Do not begin Phase 7D.

Stop after reporting.