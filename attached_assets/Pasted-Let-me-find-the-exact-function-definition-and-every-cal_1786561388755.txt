Let me find the exact function definition and every caller:
The function is fully visible. Let me now read every critical caller in context to produce a complete trace, and run all tests simultaneously:
49/49 pass. Now rerun all 4 smokes in parallel:
All 4 smokes pass. No code was changed — the function was already correct. Here is the complete semantic verification.

_swing_htf_enabled() — Full Semantic Audit

Exact function, unchanged (lines 1235–1253)

def _swing_htf_enabled(mode=None):
    """Single master gate for the SWING higher-timeframe overhaul.
    Returns True ONLY when the active (or explicitly given) trading mode is SWING
    AND the SWING_HTF_ENABLED flag is on. An env kill-switch
    (SWING_HTF_ENABLED=0) force-disables it everywhere ...
    """
    m = (mode or TRADING_MODE)
    if m != "SWING":
        return False          # ← line 1246: INTRADAY_TREND hits here → False
    if os.environ.get("SWING_HTF_ENABLED", "").strip() == "0":
        return False
    try:
        return bool(cfg_for(m, "SWING_HTF_ENABLED"))
    except KeyError:
        return False
Clarification on the prior report's phrasing. The session summary said "the function returns m != 'SWING'" — that was ambiguous shorthand. The function does not return the boolean expression m != "SWING". It uses that condition as an early-exit guard: when the mode is anything other than SWING (including INTRADAY_TREND), it immediately return False. Only SWING reaches the flag check and can return True. The function was correct all along. No fix was needed.

Every caller — INTRADAY_TREND trace

Line	Call form	IT evaluates to	Outcome for INTRADAY_TREND
1246	if m != "SWING": return False	hits early return	Function returns False for IT
8750	if not _swing_htf_enabled():	not False = True	Enters block — but this block is a SWING-off guard (it runs ONLY when flag-off/SCALP/IT, blocking a check that would otherwise require the flag)
10246	if _swing_htf_enabled(mode): in _dynamic_stop_plan	False → else branch	Uses legacy STOP_ATR_MULT, not SWING's wider SWING_STOP_ATR_MULT. IT never enters the SWING stop-multiplier path.
10433	swing_htf_plan = _swing_htf_enabled(mode) in build_strict_trade_plan	False	swing_htf_plan = False → entire SWING structure-target block at lines 10435–10500 is skipped. IT exited via routing block before reaching this line anyway.
10629	if _swing_htf_enabled(): in ORB retarget function	False → returns early	ORB 1:4 retarget skipped for IT (correct — SWING with flag-on skips it; IT also skips it)
27873	if _swing_htf_enabled(TRADING_MODE) or TRADING_MODE == "INTRADAY_TREND":	False OR True = True	swing_ctx IS computed for IT — intentional and correct. swing_ctx supplies daily_levels (prior highs/lows) consumed by compute_intraday_trend_context. This is not a SWING HTF path — it is session-level data shared between modes. All downstream SWING HTF logic is gated separately on _swing_htf_enabled().
28050	if _swing_htf_enabled() and is_actionable(verdict) ...	False AND ... = False	SWING entry veto (_swing_entry_veto_reasons) never runs for IT
28125	if _swing_htf_enabled() and _swing_strategy_filter_enabled():	False AND ... = False	SWING strategy filter never runs for IT
29216, 29290, 29299	Various _swing_htf_enabled() guards in full_analysis	False	All SWING-specific blocks in full_analysis skipped for IT
33155, 33184, 33221, 33230	Managed trades SWING guards	False	SWING thesis / managed-trade swing logic never touched by IT
33596	bool(mt.get("is_swing")) and _swing_htf_enabled()	... and False = False	IT trades never classified as is_swing
35247, 35593	Swing-thesis DB functions	False	Never called for IT
48945, 48989, 49178	Shadow-path callers (no it_ctx passed)	False	Shadow paths return IT_UNAVAILABLE → WAIT (correct)
61950, 83482	Display/boot guards	False	SWING-only boot paths skipped for IT
The one intentional OR at line 27873

if _swing_htf_enabled(TRADING_MODE) or TRADING_MODE == "INTRADAY_TREND":
    swing_ctx = compute_swing_context(active_ticker, current_price)
This is not a SWING HTF path. compute_swing_context returns session-level geometry (prior day high/low, opening range, daily bias) that compute_intraday_trend_context needs for its daily_levels target scan. Every SWING HTF-specific behaviour downstream — stop multiplier, structure target, entry veto, strategy filter, managed trade classification — is gated independently on _swing_htf_enabled(), which returns False for IT. The OR is an explicit carve-out for IT data needs, not an admission into the SWING HTF pipeline.

Proof summary

INTRADAY_TREND cannot enter any SWING-specific HTF logic because:

_swing_htf_enabled(mode) returns False at line 1246 for any mode != "SWING" — before the flag is even consulted.
Every SWING HTF block (stop multiplier, structure target, entry veto, strategy filter, thesis, managed-trade classification) is individually guarded by a direct _swing_htf_enabled() call that evaluates to False for IT.
The one place IT deliberately shares data with the SWING computation (swing_ctx at line 27873) is a data-fetch, not a logic gate — and all SWING HTF logic gates that consume swing_ctx still require _swing_htf_enabled() to be True.
Test results

Suite	Result
test_intraday_engine.py	49 / 49 passed
parity	✅ PARITY OK
scalp_golden	✅ SCALP GOLDEN OK
dual_sim	✅ DUAL-SIM SMOKE OK
breakout_mode	✅ BREAKOUT SMOKE OK
No code was changed. The function was correct as written.