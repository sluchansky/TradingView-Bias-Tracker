TASK: Finalize and Freeze Phase 6B.1

DO NOT add features.

DO NOT optimize strategies.

DO NOT generate another baseline.

DO NOT start Phase 6C.

The first official real-data baseline was reported as:

BL-20260726-042830-0cc8364

Complete the final verification and return the full closure report.

----------------------------------------------------
PART 1 — FINAL GIT STATE
----------------------------------------------------

Run:

git branch --show-current
git rev-parse HEAD
git log -7 --oneline
git status --short
git diff --check
git show --stat --oneline HEAD

Return:

- branch
- starting Phase 6B.1 commit
- final Phase 6B.1 commit
- current HEAD
- exact commit message
- exact files committed
- exact untracked files
- confirmation no task attachment is committed
- confirmation no historical market data is committed
- confirmation no secrets are committed

If Phase 6B.1 code has not yet been committed, commit it now.

Suggested commit message:

Add official real-data baseline system

Do not commit task files or market data.

----------------------------------------------------
PART 2 — DATABASE SCHEMA VERIFICATION
----------------------------------------------------

Return the complete schema for:

- baseline_runs
- baseline_matrix_results
- baseline_trades
- baseline_breakdowns

Use the actual table names if different.

Confirm the presence and data type of:

- initial_risk_pts
- initial_risk_r
- realized_r
- dataset_fingerprint
- config_snapshot_hash
- reliability_label
- warnings
- error_details

Return the exact migration or schema code that adds initial_risk_r.

Confirm this schema change is represented in committed source code and is not only
a manual change made directly to the current database.

If the column was added manually but no repeatable migration exists, add the
smallest safe idempotent migration before closure.

The application must be able to initialize the schema correctly in a fresh
environment.

----------------------------------------------------
PART 3 — BASELINE RECORD INTEGRITY
----------------------------------------------------

Verify baseline:

BL-20260726-042830-0cc8364

Return database counts for:

- baseline run records
- matrix result records
- trade records
- breakdown records

Expected:

- one baseline run
- 40 matrix results
- 7,909 trade records
- reported breakdown count

Confirm every trade record has:

- baseline ID
- matrix result ID
- dataset ID
- instrument
- mode
- strategy
- direction
- entry timestamp
- exit timestamp
- entry price
- stop price
- target price
- exit price
- realized R

Return null counts for every required trade field.

Do not print all trade records.

----------------------------------------------------
PART 4 — REMOVE INCOMPLETE BASELINES
----------------------------------------------------

The progress log reports that an initial baseline generation succeeded before
trade storage failed because initial_risk_r was missing.

Verify that no incomplete or orphaned baseline remains.

Return all baseline IDs currently stored with:

- creation time
- matrix result count
- trade count
- breakdown count
- status

Confirm that only valid completed baseline versions remain.

Check for orphan rows in:

- matrix results
- trades
- breakdowns

Return orphan counts.

Expected:

0 orphan matrix rows
0 orphan trade rows
0 orphan breakdown rows

Do not delete any valid completed baseline.

----------------------------------------------------
PART 5 — DATASET SNAPSHOT
----------------------------------------------------

For every official dataset used by the baseline return:

- dataset ID
- instrument
- Databento dataset
- schema
- start timestamp
- end timestamp
- row count
- gap count
- SHA-256 fingerprint
- quality classification
- real-data confirmation
- synthetic flag

Confirm the baseline stored the exact dataset IDs and fingerprints.

Confirm the baseline does not dynamically select “latest” datasets when viewed
later.

----------------------------------------------------
PART 6 — CONFIGURATION SNAPSHOT
----------------------------------------------------

Return the stored configuration snapshot for the baseline, including:

- source commit
- engine version
- dataset IDs
- dataset fingerprints
- instrument list
- modes
- strategies
- disabled strategies
- management settings
- commission settings
- slippage settings
- tick sizes
- ATR settings
- stop settings
- ORB 4R override
- same-bar conflict policy
- next-bar-open entry policy
- session definitions
- timezone policy
- contract/continuous-symbol policy
- news-blackout settings

Return:

- configuration snapshot hash
- method used to serialize it
- method used to produce the hash

Confirm the snapshot is immutable.

----------------------------------------------------
PART 7 — MATRIX COMPLETION
----------------------------------------------------

Return all 40 combinations in a compact table containing:

- instrument
- mode
- strategy
- status
- completed trades
- wins
- losses
- win rate
- net R
- average R
- expectancy
- profit factor
- maximum drawdown
- reliability label
- warnings

Do not omit zero-trade combinations.

Statuses must reconcile to exactly 40:

- COMPLETE
- COMPLETE_ZERO_TRADES
- FAILED
- DATA_UNAVAILABLE

Return totals for each status.

----------------------------------------------------
PART 8 — BASELINE SUMMARY
----------------------------------------------------

Return:

- baseline ID
- generation timestamp
- source commit
- total combinations
- completed combinations
- zero-trade combinations
- failed combinations
- unavailable combinations
- total bars processed
- total trades
- total wins
- total losses
- breakeven trades
- aggregate gross positive R
- aggregate gross negative R
- aggregate net R
- aggregate average R
- aggregate expectancy
- aggregate profit factor
- aggregate maximum drawdown
- reliability distribution
- warnings
- failures

Clearly state the aggregation method.

Confirm that aggregate maximum drawdown was not calculated by incorrectly
concatenating unrelated strategies or instruments unless explicitly labeled as
a synthetic aggregate sequence.

----------------------------------------------------
PART 9 — BREAKDOWN RECONCILIATION
----------------------------------------------------

Return available breakdown groups:

- instrument
- strategy
- mode
- direction
- session
- ET hour
- weekday
- month
- volatility regime
- trend regime
- contract
- sample-size band

For each grouping type return:

- number of groups
- total trades represented
- whether totals reconcile to 7,909

Explain any grouping that legitimately does not reconcile because a source field
is unavailable or null.

----------------------------------------------------
PART 10 — API VERIFICATION
----------------------------------------------------

Verify these routes:

- GET /backtest/baselines
- GET /backtest/baselines/<baseline_id>
- GET /backtest/baselines/<baseline_id>/trades
- GET /backtest/baselines/<baseline_id>/breakdowns
- POST /backtest/baselines/generate

For each route return:

- HTTP method
- owner protection
- success status
- major response keys
- read/write behavior

Confirm:

- detail endpoint returns matrix_results
- dashboard expects matrix_results
- trade filters work
- breakdown filters work
- POST requires explicit dataset IDs
- POST does not download Databento data
- GET routes do not modify state

----------------------------------------------------
PART 11 — DASHBOARD VERIFICATION
----------------------------------------------------

Verify the dashboard panel renders without JavaScript syntax errors.

Return proof from:

- extracted dashboard script
- node --check result
- server response check

Confirm the panel shows:

- baseline ID
- source commit
- generation time
- dataset coverage
- 40-combination matrix
- total trade count
- net R
- expectancy
- profit factor
- maximum drawdown
- reliability labels
- warnings
- filters
- trade explorer
- data-quality information

Confirm these labels are visible:

HISTORICAL RESEARCH ONLY
NOT LIVE PERFORMANCE
NOT FINANCIAL ADVICE

----------------------------------------------------
PART 12 — TEST REPORT
----------------------------------------------------

Return exact pass/fail counts separately for:

- Phase 6B.1 focused tests
- Phase 6B.0 acquisition tests
- Phase 6A.1 parity tests
- historical import tests
- ORB adapter tests
- coverage audit tests
- Stop Parity tests
- Decision Quality tests
- Strategy Scan tests
- every other backtest-related suite
- each of the four golden workflows

Explain the reported:

88/88

State exactly which test file or files comprise that count.

Do not call this the full repository suite unless the entire repository suite was
run.

----------------------------------------------------
PART 13 — DETERMINISM PROOF
----------------------------------------------------

Do not generate another official baseline.

Using the same four datasets and frozen configuration, run the engine in a
non-persistent verification mode or compare stored deterministic artifacts.

Verify:

- same 40 matrix keys
- same status per matrix result
- same completed trade counts
- same trade ordering
- same serialized metric output
- same configuration hash

Do not insert another baseline record.

Return whether deterministic parity is:

PASS
FAIL
NOT_TESTED

If not tested, explain why.

----------------------------------------------------
PART 14 — PROTECTED BOUNDARIES
----------------------------------------------------

Confirm Phase 6B.1 did not change:

- live Databento ingestion
- live subscriptions
- Databento normalization
- live strategy scorers
- Edge Score
- verdicts
- gates
- Decision Quality
- learning
- sizing
- trade management
- execution
- TradersPost
- prop controls
- research strategy registry

Return source-diff evidence and test evidence.

Pay special attention to app.py because routes and dashboard code were added
there.

Confirm that no baseline function is called from the live alert-processing path.

----------------------------------------------------
PART 15 — FIRST BASELINE INTERPRETATION BOUNDARY
----------------------------------------------------

Do not recommend parameter changes.

Do not modify strategy rankings.

Do not feed results to learning.

Return only descriptive observations such as:

- sample-size distribution
- combinations with zero trades
- failed combinations
- extreme values that require audit
- suspiciously high trade counts
- suspiciously high profit factors
- combinations with no losing trades
- combinations where metrics are undefined

Flag anything that may indicate:

- duplicate trades
- overlapping trades
- session leakage
- continuous-contract roll distortion
- commission omission
- slippage omission
- unrealistic fill assumptions
- excessively permissive signal dispatch

Do not claim any strategy has a proven edge yet.

----------------------------------------------------
PART 16 — FINAL STATUS
----------------------------------------------------

Return exactly one:

PHASE_6B_BASELINE_COMPLETE

PHASE_6B_BASELINE_COMPLETE_WITH_WARNINGS

PHASE_6B_BASELINE_BLOCKED

Use COMPLETE_WITH_WARNINGS if the baseline is stored correctly but includes
sample-size, data, modeling, or interpretation warnings.

----------------------------------------------------
PART 17 — FINAL CLOSURE
----------------------------------------------------

Return:

1. Branch
2. Starting commit
3. Ending commit
4. Current HEAD
5. Files committed
6. Schema migration status
7. Baseline ID
8. Dataset IDs and fingerprints
9. Matrix record count
10. Trade record count
11. Breakdown record count
12. Orphan counts
13. Aggregate metrics
14. Reliability distribution
15. API status
16. Dashboard status
17. Test totals
18. Golden results
19. Determinism result
20. Protected-boundary proof
21. Remaining warnings
22. Final status

After returning the report, stop.

DO NOT START PHASE 6C.