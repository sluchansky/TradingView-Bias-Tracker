PROCEED WITH OPTION A.

PHASE 2 IS TEMPORARILY PAUSED.

FIRST IMPLEMENT THE UPGRADED FOUR-INSTRUMENT 9:30 ALGORITHMIC ORB SYSTEM.

The authoritative implementation specification already exists at:

attached_assets/Pasted-FULL-REPLIT-IMPLEMENTATION-INSTRUCTIONS-Upgrade-the-Exi_1786044408647.txt

Use that document as the controlling implementation specification.

DO NOT substitute compute_breakout_mode() for this system.

DO NOT modify or replace the existing live 08:00–08:30 OPENING_RANGE_BREAKOUT strategy.

The 08:00 ORB and the new 09:30 ORB are separate strategies.

==================================================
1. SAFETY / SCOPE
==================================================

The upgraded 09:30 ORB must initially finish in:

SHADOW

It must NOT transmit live orders.

Do not change:

- existing 08:00 live ORB
- existing strategy scoring
- READY / EARLY / WAIT
- existing live trade plans
- TradersPost
- execution gateway
- prop protection
- arm-state system
- daily-loss protection
- canonical Databento ingestion
- learning weights
- existing native journal behavior

The new ORB must use the existing production infrastructure rather than creating parallel broker/risk systems.

==================================================
2. AUTHORITATIVE 9:30 ORB CONTRACT
==================================================

Implement the system described in the existing 1,794-line specification.

At minimum the resulting engine must support:

INSTRUMENTS:

MGC
MNQ
MES
MYM

Each instrument operates independently.

DEFAULT RANGE:

09:30–09:39 ET

DEFAULT RANGE LOCK:

09:40 ET

DEFAULT ENTRY WINDOW:

09:40–10:30 ET

CONFIGURABLE RANGE DURATIONS:

5 minutes
10 minutes
15 minutes
30 minutes

Corresponding lock times must derive correctly from the selected range duration.

==================================================
3. CONFIRMATION MODES
==================================================

Implement:

TOUCH
CLOSE_OUTSIDE
CLOSE_AND_RETEST

Default:

CLOSE_OUTSIDE

Definitions must be deterministic and based on actual Databento market data.

Do not use an LLM to determine whether confirmation occurred.

==================================================
4. BREAKOUT BUFFERS
==================================================

Use instrument-specific tick buffers defined by the controlling spec.

Initial expected defaults:

MGC = 2 ticks
MNQ = 4 ticks
MES = 2 ticks
MYM = 4 ticks

Verify these against the authoritative spec before coding.

Do not replace these with the old shared 0.05 × ATR buffer.

==================================================
5. MAXIMUM CHASE
==================================================

Implement maximum-chase protection.

Default from the specification:

25% of opening-range width

A breakout that has moved beyond the permitted chase distance must transition to the appropriate blocked/missed state instead of being entered late.

==================================================
6. PER-INSTRUMENT STATE MACHINE
==================================================

Implement the full state machine required by the controlling spec.

Including at minimum:

DISABLED
WAITING_FOR_SESSION
WAITING_FOR_RANGE
BUILDING_RANGE
RANGE_LOCKED
WATCHING_BREAKOUT
BREAKOUT_DETECTED
CONFIRMATION_PENDING
QUALIFIED
BREAKOUT_MISSED
RISK_PENDING
RISK_RESERVED

BLOCKED_BY_DATA
BLOCKED_BY_RANGE_WIDTH
BLOCKED_BY_CONFIRMATION
BLOCKED_BY_MAXIMUM_CHASE
BLOCKED_BY_INSTRUMENT_RISK
BLOCKED_BY_GROUP_RISK
BLOCKED_BY_PORTFOLIO_RISK
BLOCKED_BY_PROP_RULE
BLOCKED_BY_DAILY_LOSS
BLOCKED_BY_POSITION_LIMIT
BLOCKED_BY_DUPLICATE_GUARD
BLOCKED_BY_EXECUTION_MODE
BLOCKED_BY_ARM_STATE
BLOCKED_BY_SAFETY_LOCK

ENTRY_REQUESTED
ORDER_ACCEPTED
ORDER_REJECTED
POSITION_ACTIVE
POSITION_MANAGING
COMPLETED
EXPIRED
DATA_INVALID
RECOVERY_REQUIRED

Use the exact authoritative state list if it differs.

State transitions must be deterministic and auditable.

==================================================
7. UNIQUE INSTANCE IDENTITY
==================================================

Each ORB instance must have immutable identity including:

trading date
instrument
strategy_version
config_version

No state leakage between:

MGC
MNQ
MES
MYM

No state leakage between trading dates.

==================================================
8. RANGE IMMUTABILITY
==================================================

Once the range is locked:

or_high
or_low
range width
lock timestamp

must become immutable for that ORB instance.

Subsequent price movement may not rewrite the opening range.

Add tests explicitly proving this.

==================================================
9. DATABENTO SOURCE
==================================================

Use the existing canonical Databento market data pipeline.

DO NOT create another Databento connection.

DO NOT alter proven Databento ingestion.

Reuse:

normalized bars
bar-close callbacks
canonical market state
existing instrument definitions

where appropriate.

The new ORB should be Databento-native.

Do not make Databento pretend to be TradingView.

==================================================
10. STOP MODEL
==================================================

Default stop:

OPPOSITE_RANGE_SIDE

Use the controlling specification's per-instrument stop buffers.

Also implement the configured stop-model options from the specification if required:

BREAKOUT_CANDLE_INVALIDATION
ATR_BASED
STRUCTURE_BASED
CUSTOM_TICKS

But do not change the default away from OPPOSITE_RANGE_SIDE.

==================================================
11. TARGET MODEL
==================================================

Implement the intended ORB target management from the controlling spec.

Expected defaults:

TP1 = 1R
TP2 = 2R

For multi-contract trades:

exit approximately 50% at TP1
hold runner for TP2

For one-contract trades:

use the specification's single-contract fallback, expected approximately 1.5R.

Verify exact values against the authoritative spec.

Do NOT use the old 08:00 ORB's 4R target override.

Do NOT use compute_breakout_mode()'s adaptive 1–5R target model.

==================================================
12. RISK / CORRELATION
==================================================

Reuse the existing canonical risk engine.

Do not invent a second account-risk system.

Implement ORB-specific reservation/accounting required by the spec.

The equity-index group:

MNQ
MES
MYM

must share correlation/risk budgeting.

MGC belongs to the metals group separately.

Default correlation handling should follow:

SHARED_GROUP_BUDGET

if confirmed by the controlling spec.

Support the other configured modes only as specified.

Implement atomic reservation lifecycle:

PENDING
RESERVED
CONVERTED_TO_ACTIVE
RELEASED
EXPIRED
RECONCILIATION_REQUIRED

Risk reservation must prevent race conditions when multiple instruments qualify nearly simultaneously.

==================================================
13. POSITION / TRADE LIMITS
==================================================

Implement the configuration limits from the spec, including:

MAX_TRADES_PER_INSTRUMENT
MAX_SIMULTANEOUS_ORB_POSITIONS
MAX_ORB_TOTAL_RISK_DOLLARS
MAX_INDEX_GROUP_RISK_DOLLARS

and any other canonical limits specified.

Default:

one ORB trade per instrument per session unless the authoritative specification says otherwise.

Re-entry default should remain disabled unless explicitly configured.

==================================================
14. EXECUTION MODES
==================================================

Implement:

DISABLED
SHADOW
PAPER
LIVE_ALGORITHMIC

Missing/unknown mode must fail safe to:

DISABLED

FOR THIS IMPLEMENTATION:

production default = SHADOW

Do not enable LIVE_ALGORITHMIC.

==================================================
15. SAFETY GATES
==================================================

Even when eventually promoted to LIVE_ALGORITHMIC, an ORB order must still pass the existing independent execution protections.

At minimum:

execution mode
manual arm state
safety lock
emergency stop
final validation
duplicate guard
daily loss
prop protection
position limits
risk reservation

Do not build a direct ORB → TradersPost bypass.

Required architecture:

ORB qualification
    ↓
canonical risk
    ↓
existing execution gateway
    ↓
existing final safety validation
    ↓
broker path

==================================================
16. JOURNAL / PROVENANCE
==================================================

Add ORB-specific provenance without replacing the existing journal.

Record:

instrument
date
strategy_version
config_version

range duration
range high
range low
range width
lock time

breakout direction
breakout timestamp
confirmation mode
confirmation timestamp

breakout buffer
max chase

entry
stop
TP1
TP2
contracts

risk requested
risk reserved

15m trend
4H trend
VWAP context
CVD context
VIX context where available

state transitions

MFE
MAE
final R
final outcome

Execution/journal ownership must remain consistent with the existing system.

==================================================
17. DASHBOARD
==================================================

Upgrade the existing 9:30 Breakout panel in place.

Do NOT create an unrelated new dashboard/project.

Show all four instruments simultaneously:

MGC
MNQ
MES
MYM

For each show:

current ORB state
range high
range low
range width
range status
lock time

breakout direction
confirmation mode
confirmation progress

current price
breakout threshold
maximum chase threshold

15m trend
4H trend
VWAP
CVD
VIX/filter context where available

risk status
risk reservation
block reason

planned entry
stop
TP1
TP2

mode:
DISABLED / SHADOW / PAPER / LIVE_ALGORITHMIC

The operator must be able to tell exactly WHY an instrument is:

waiting
qualified
blocked
missed
active
completed

==================================================
18. FILTER MODE CONTRACT
==================================================

Where the specification defines optional contextual filters, support:

OFF
INFORMATIONAL
REQUIRED

This is particularly important for:

trend alignment
VWAP
CVD
VIX
other contextual confirmation

Default these exactly as required by the controlling spec.

An INFORMATIONAL filter may display context but may NOT block the trade.

==================================================
19. SHADOW MODE
==================================================

SHADOW is critical.

In SHADOW:

the complete algorithm must run as if preparing a real trade.

It should calculate:

range
breakout
confirmation
qualification
risk request
planned contracts
entry
stop
targets
management plan

but MUST NOT transmit an order.

Shadow outcomes should be recorded so Phase 2 can later use the exact engine as its research baseline.

==================================================
20. DO NOT CONNECT PHASE 2 YET
==================================================

Do not build:

ghost_experiment_engine.py
ghost_opportunities
ghost_experiments
ghost_experiment_results

during this task.

First finish and prove the actual 9:30 ORB baseline.

Phase 2 will attach afterward.

==================================================
21. TESTS
==================================================

Add comprehensive deterministic tests.

At minimum:

09:30 range starts correctly
10-minute default range
5-minute range
15-minute range
30-minute range

correct lock times

range immutable after lock

MGC isolation
MNQ isolation
MES isolation
MYM isolation

TOUCH qualification
CLOSE_OUTSIDE qualification
CLOSE_AND_RETEST qualification

failed confirmation
breakout missed
maximum chase block

long breakout
short breakout

opposite-range-side stop

TP1 calculation
TP2 calculation
single-contract target

index correlation group
MGC independent risk group

risk reservation concurrency
risk reservation release

daily loss block
prop block
position-limit block
duplicate block

execution-mode block
arm-state block
safety-lock block

SHADOW never transmits
PAPER never reaches live broker
DISABLED performs no qualification/execution
LIVE_ALGORITHMIC still requires gateway safety

restart recovery

strategy_version/config_version identity

journal provenance

no live 08:00 ORB behavior change

==================================================
22. REGRESSION / PARITY
==================================================

Run all relevant existing tests.

At minimum:

canonical market state
scoring parity
scalp golden
risk
execution gateway
prop protection
arm state
safety lock
existing 08:00 ORB
Left Brain
dashboard/API

The existing 08:00 OPENING_RANGE_BREAKOUT must remain byte/behavior equivalent.

The new 09:30 ORB in SHADOW must not affect:

verdict
edge_score
direction
blockers
entry
stop
targets
quantity

of the existing live trading engine.

==================================================
23. FAIL-OPEN
==================================================

Because SHADOW research/advisory functionality must never stop live trading:

If the 09:30 ORB engine throws:

log the error
mark that instrument DATA_INVALID/RECOVERY_REQUIRED as appropriate
continue existing production processing

One instrument failure must not affect the others.

==================================================
24. COMPLETION REPORT
==================================================

Return:

AUTHORITATIVE SPEC USED:

NEW FILES:

MODIFIED FILES:

DATABENTO INGESTION MODIFIED: YES/NO

08:00 LIVE ORB MODIFIED: YES/NO

09:30 ORB ENGINE IMPLEMENTED: YES/NO

INSTRUMENTS:
MGC =
MNQ =
MES =
MYM =

DEFAULT RANGE:
DEFAULT LOCK:
DEFAULT ENTRY WINDOW:

CONFIRMATION MODES:

MGC BUFFER:
MNQ BUFFER:
MES BUFFER:
MYM BUFFER:

DEFAULT STOP:

TP1:
TP2:
ONE-CONTRACT TARGET:

STATE MACHINE IMPLEMENTED: YES/NO

RISK RESERVATION IMPLEMENTED: YES/NO

INDEX SHARED BUDGET IMPLEMENTED: YES/NO

EXECUTION MODES IMPLEMENTED: YES/NO

CURRENT PRODUCTION MODE:

DASHBOARD IMPLEMENTED: YES/NO

JOURNAL PROVENANCE IMPLEMENTED: YES/NO

SHADOW ORDERS TRANSMITTED:
MUST = 0

NEW TESTS:
TOTAL TESTS:
PASSED:
FAILED:

08:00 ORB REGRESSION:
SCORING PARITY:
SCALP GOLDEN:
EXECUTION PARITY:

LIVE TRADING BEHAVIOR CHANGED: YES/NO

NEW 09:30 ORB LIVE EXECUTION ENABLED:
MUST = NO

SAFE TO USE AS PHASE 2 GHOST BASELINE: YES/NO

GIT COMMIT: