TASK: Dashboard Cleanup — Phase 1.5

OBJECTIVE

Remove one clear duplicate interface from the dashboard:

Ask the AI panel
ID: mod-assistant

The Main Brain already contains a chat interface using the same /assistant backend. The separate Ask the AI panel is therefore redundant.

MAKE ONLY THIS ONE CHANGE.

REQUIREMENTS

1. Remove mod-assistant from the rendered dashboard interface.

2. Do not remove or modify:
- The /assistant endpoint
- The chat interface inside mod-brain
- Any assistant backend logic
- Quick-question functionality used by the Main Brain
- Any shared JavaScript functions required by mod-brain

3. Before deleting code, trace every function, event handler, selector, and CSS rule associated with mod-assistant.

4. If code is shared with the Main Brain chat, preserve the shared code.

5. Remove only code that is exclusively required by the standalone mod-assistant panel.

6. Remove mod-assistant from:
- Dashboard markup
- Panel category lists
- Advanced View visibility rules
- Navigation or panel registries, if present

7. Do not remove any other duplicate panel during this phase.

8. Do not:
- Move panels
- Resize panels
- Change layout styling
- Change navigation
- Change Brain content
- Change trading logic
- Rename anything
- Perform general cleanup

BEFORE EDITING

Report:

- Exact file or files to modify
- Whether mod-assistant shares functions with the Main Brain chat
- The exact standalone code that can be safely removed
- Any code that must remain because it is shared

Then make the change.

SUCCESS CRITERIA

- The standalone Ask the AI panel no longer appears.
- Main Brain chat still opens and accepts questions.
- Quick questions inside Main Brain still work.
- The /assistant endpoint still responds normally.
- Advanced View no longer references mod-assistant.
- No empty gap or broken panel container remains.
- No other panel changes.
- No new browser-console errors.

TESTING

1. Load dashboard with Advanced View OFF.
2. Confirm Ask the AI is absent.
3. Enable Advanced View.
4. Confirm Ask the AI remains absent.
5. Open Main Brain chat.
6. Submit “Why WAIT?”
7. Confirm a response appears.
8. Test one Main Brain quick-question button.
9. Switch dashboard sections.
10. Check browser console.

FINAL RESPONSE

Return:

- Files changed
- Markup removed
- Standalone JavaScript removed
- Shared JavaScript preserved
- CSS or registry references removed
- Main Brain chat test result
- Browser-console result
- Confirmation that no other panel was changed

Do not begin Phase 1.6.