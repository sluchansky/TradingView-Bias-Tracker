PHASE 4.2 — FVG RESEARCH SEMANTICS + RESTART INTEGRITY

Do NOT add another research family.

Do NOT modify live trading.

Three targeted items only.

==================================================
1. REMOVE DUPLICATE FVG VARIANT
==================================================

Audit:

BASELINE
SECOND_TOUCH_ALLOWED

Current completion report states:

BASELINE = all revisits eligible

SECOND_TOUCH_ALLOWED = any revisit_n

If true, SECOND_TOUCH_ALLOWED is equivalent to BASELINE.

That violates the one-variable/distinct-variant contract.

Replace it with:

SECOND_TOUCH_ONLY

Exact gate:

revisit_n == 2

unless the code audit shows the baseline semantics were reported incorrectly.

Required resulting experiment set should still be <= 10.

Add tests proving:

BASELINE enters eligible first and later revisits

FIRST_TOUCH_ONLY permits revisit_n == 1 only

SECOND_TOUCH_ONLY permits revisit_n == 2 only

SECOND_TOUCH_ONLY rejects revisit_n == 1

SECOND_TOUCH_ONLY rejects revisit_n >= 3

No non-baseline variant is behaviorally identical to BASELINE.

==================================================
2. FIX / CLARIFY MIDPOINT_ENTRY
==================================================

Current report says:

MIDPOINT_ENTRY = depth >= 60%
"must reach midpoint"

Audit _fvg_depth_pct().

If:

0% = near edge
50% = midpoint
100% = full fill

then MIDPOINT_ENTRY must use:

depth >= 50%

If the internal depth scale differs, document it precisely.

Do not retain a strategy called MIDPOINT_ENTRY if its threshold does not represent the actual midpoint.

Add boundary tests:

49.99% = not midpoint
50.00% = midpoint
50.01% = midpoint

using tick-valid prices.

==================================================
3. RESTART REVISIT SEQUENCE
==================================================

Prove revisit identity survives a restart AFTER prior experiments are already completed.

Required deterministic sequence:

A. Create one FVG.

B. First physical revisit:
   revisit_n = 1
   opportunity A created.

C. Complete all experiments belonging to opportunity A.

D. Restart GhostResearchEngine.

E. Feed market state while price is still inside the SAME physical revisit.

Expected:
NO new opportunity.
NO duplicate revisit.
revisit_n remains logically 1.

F. Price leaves the FVG.

G. Price later re-enters the same FVG.

Expected:

revisit_n = 2
new opportunity B
new revisit_id
same research_fvg_id
same logical FVG

H. Restart again.

I. Confirm another callback does not relabel the next revisit as #1.

If current restore only reconstructs revisit count from ACTIVE experiments and loses completed revisit history, FIX IT.

Restore the latest persisted revisit state from authoritative Ghost opportunity/history data.

Do not depend exclusively on currently open experiments.

==================================================
4. OPPORTUNITY / TOUCH SEMANTICS
==================================================

Confirm touch/revisit numbering is based on physical FVG interactions rather than number of Ghost experiments.

Ten variants from one revisit = ONE revisit.

Do not increment revisit count per experiment.

==================================================
5. TARGET FORMULA REPORT
==================================================

The previous completion report truncated the baseline target line.

Return the exact executable formula for:

BULLISH TP1
BEARISH TP1

Confirm BASELINE = exactly 2R if that is the implemented rule.

==================================================
6. REGRESSION
==================================================

Run:

FVG research tests
GRE tests
ORB tests

PARITY
SCALP GOLDEN
DUAL-SIM
BREAKOUT

No new failures.

==================================================
7. SAFETY
==================================================

FVG broker calls = 0
ORB broker calls = 0
09:30 ORB = SHADOW
Research money-path authority = NO
Live trading behavior changed = NO

==================================================
RETURN
==================================================

DUPLICATE SECOND_TOUCH_ALLOWED:
CONFIRMED/FALSE ALARM

REPLACED WITH SECOND_TOUCH_ONLY:
YES/NO

MIDPOINT DEPTH SCALE:

MIDPOINT THRESHOLD:

RESTART AFTER COMPLETED REVISIT:
PASS/FAIL

REVISIT #1 SURVIVES RESTART:
YES/NO

NEXT LEGITIMATE REVISIT = #2:
YES/NO

SAME FVG research_fvg_id:
PASS/FAIL

NEW revisit_id:
PASS/FAIL

BASELINE BULLISH TP FORMULA:

BASELINE BEARISH TP FORMULA:

FVG TESTS:

ORB REGRESSION:
PASS/FAIL

PARITY:
PASS/FAIL

SCALP GOLDEN:
PASS/FAIL

DUAL-SIM:
PASS/FAIL

BREAKOUT:
PASS/FAIL

LIVE BEHAVIOR CHANGED:
MUST = NO

SAFE TO ACCUMULATE REAL ORB + FVG EVIDENCE:
YES/NO