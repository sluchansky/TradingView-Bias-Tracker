ARM CONTROL — CLOSURE VALIDATION AND SAFETY-GAP FIXES

This is a closure task for the Auto-Trading Arm/Disarm Control.

Do not add new notifications, dashboards, strategies, trading logic, or unrelated features.

Do not enable live execution.

Keep the system in paper or disabled mode throughout validation.

1. Reconcile the commit history

Report the exact commit chain containing:

* Original safety remediation
* Arm-control backend implementation
* Dashboard implementation
* Current HEAD
* Published revision

Explain why the earlier remediation was reported as commit 569018c, but the arm-control summary says it builds on 3408255.

Prove whether the same remediation code is present in both histories or whether commits were replaced, rebased, cherry-picked, or recreated.

Report:

* Branch
* Current HEAD
* Published revision
* git log --oneline --decorate -15
* Working-tree status
* Modified and untracked files
* git diff --check
* Whether a76b11a is included in production

Do not publish until the test issues below are resolved.

2. Resolve the 10 remediation-test failures

The prior report stated:

* test_auto_trading_high_findings_remediation.py: 77/77 passed

The arm-control report now states:

* 77 remediation tests
* 10 failures
* Claimed pre-existing

This contradiction must be resolved.

Run the remediation suite against:

1. The commit immediately before arm-control changes
2. The current arm-control HEAD

For every failure report:

* Exact test name
* Exact assertion or exception
* Whether it fails at both commits
* Whether global state or test-order contamination is involved
* Whether the failure touches execution mode, Databento health, candidate freshness, LRE safety, duplicate prevention, or outbound transport

If the failures occur only after the arm-control work, fix them.

If they occur at the pre-arm baseline, explain why the earlier 77/77 report was incorrect.

Do not declare closure while execution-safety tests are failing.

Required result:

* Original 97 audit tests pass
* All 77 remediation tests pass
* All 74 arm-control tests pass
* No test-order-dependent failures
* No global-state pollution

Run the three suites:

* individually
* together in one process
* in randomized order, if supported
* twice consecutively in the same process or environment

3. Add the mandatory in-flight race tests

Implement the proposed in-flight race test now. It is a required safety test, not a future enhancement.

Test these exact sequences:

Disarm race

1. Candidate passes first arm check
2. Candidate is claimed
3. Operator disarms
4. Final pre-send arm check executes
5. Outbound transport must not be called

Expiration race

1. Candidate passes first arm check
2. Arm session expires
3. Final pre-send check executes
4. Outbound transport must not be called

Kill-switch race

1. Candidate passes first arm check
2. Kill switch activates
3. Final pre-send check executes
4. Outbound transport must not be called

Session replacement race

1. Candidate is authorized under arm-session A
2. Session A is disarmed
3. A new arm-session B is created
4. Candidate from session A reaches the final check
5. It must be blocked because its session ID does not match session B

Limit-change race

1. Candidate is approved for two contracts
2. Operator disarms and rearms with a one-contract limit
3. Old two-contract candidate reaches final check
4. It must be blocked

For every case assert:

* Zero outbound transport calls
* Stable reason code
* Candidate state is not left permanently claimed
* Duplicate guard remains valid
* Structured audit event exists

4. Verify startup and deployment disarming

Prove with real process restarts:

* EXECUTION_MODE=traderspost starts as live_available_disarmed
* Restart clears an active arm session
* Deployment clears an active arm session
* Crash recovery clears an active arm session
* Arm-session ID changes after every new arm
* A pre-restart candidate cannot use a post-restart arm session
* Missing mode remains disabled
* Paper mode cannot be armed for live execution

Do not test this only by resetting Python variables.

Use actual controlled application process restarts.

5. Verify route authentication and direct Flask protection

Test the published architecture and local direct backend separately.

Required routes:

* POST /execution/arm
* POST /execution/disarm
* POST /execution/kill-switch
* POST /execution/reset-safety-lock
* GET /execution/state
* GET /execution/audit-log

Confirm:

* Anonymous requests cannot arm
* Anonymous requests cannot disarm
* Anonymous requests cannot activate or reset the kill switch
* GET cannot perform state changes
* Direct Flask access cannot bypass Express authentication
* CSRF or equivalent protection exists for browser state changes
* Arm requests are rate-limited
* Failed arm attempts are audited
* Audit-log responses do not expose secrets, auth headers, webhook URLs, or confirmation phrases

If direct production Flask access can change arm state anonymously, classify it CRITICAL and fix it before continuing.

6. Verify the Main Brain interface location

Report exactly where the operator controls are rendered:

* Legacy Flask dashboard
* React Main Brain page
* Both

The user needs to arm the system from the actual Main Brain home interface.

If the controls are only in the legacy app.py dashboard, add the same authenticated backend-driven control panel to the React Main Brain page without duplicating arm state.

The backend remains the sole source of truth.

The Main Brain interface must display:

* Configured mode
* Effective state
* ARMED / DISARMED / SAFETY LOCKED
* Expiration countdown
* Allowed instruments
* Maximum contracts
* Trades used and limit
* Session loss and limit
* Databento status
* Prop-guard status
* Last disarm reason

It must include:

* ARM AUTO-TRADING
* DISARM NEW ENTRIES
* EMERGENCY KILL SWITCH
* RESET SAFETY LOCK

Do not rely on localStorage for arm state.

7. Verify existing-position behavior

Using a mocked protected active trade, prove:

Ordinary disarm

* Blocks new entries immediately
* Does not cancel the existing protective stop
* Does not cancel valid targets
* Does not falsely mark the position closed
* Allows the existing trade-management loop to continue
* Does not allow pyramiding or reversal

Kill switch — lock only

* Blocks new entries
* Locks rearming
* Preserves existing protective orders
* Does not automatically market-close a position

Emergency close

If implemented, it must:

* Be a separate route and button
* Require a separate exact confirmation phrase
* Reconcile the broker response
* Preserve lock state after close
* Never be triggered by the ordinary kill-switch button

If emergency close is not implemented, state that clearly. Do not imply the kill switch closes positions.

8. Validate automatic disarm triggers immediately

The watcher runs every 30 seconds, but execution safety cannot depend on waiting for that polling interval.

For each trigger, prove both:

1. The final pre-send safety check blocks immediately
2. The watcher later updates the visible arm state and reason

Test:

* Databento disconnect
* Stale tick
* Stale bar
* Stale VWAP
* Daily loss limit
* Session loss limit
* Drawdown limit
* LRE blocking error
* Broker state unknown
* Protective-order failure
* Execution-mode change
* Account or prop-profile change
* Unhandled execution-path exception
* Arm expiration

An unsafe condition must block transmission immediately, even if _ARM_STATE["armed"] has not yet been changed by the watcher.

9. Validate limits and counters

Prove backend enforcement of:

* Allowed instruments
* Allowed strategies
* Direction restriction
* Maximum contracts per instrument
* Maximum trades per arm session
* Maximum session loss
* One-position maximum
* No pyramiding
* No automatic reversal
* Maximum arm duration
* Expiration

Confirm:

* Limits can only tighten global risk rules
* Rearming cannot reset actual session losses improperly
* Failed and blocked candidates do not consume a completed-trade count unless deliberately designed and documented
* A paper candidate cannot increment live arm-session counters
* Trade count increments atomically
* Two simultaneous candidates cannot both consume the final available trade slot

10. Audit persistence and bounded storage

Report whether execution-control audit events are:

* Memory only
* File-backed
* Database-backed

If memory only, do not call them persistent.

At minimum, ensure critical arm-state changes and execution authorizations survive normal application restart in an append-only audit store, while the armed state itself resets to disarmed.

Verify audit records for:

* Arm success
* Arm failure
* Manual disarm
* Expiration
* Automatic safety disarm
* Kill switch
* Reset safety lock
* Candidate blocked while disarmed
* Candidate blocked after session mismatch
* Candidate authorized
* Candidate blocked at final pre-send check

Set a bounded retention policy so an in-memory list cannot grow indefinitely.

11. Full validation

Run:

* test_auto_trading_audit.py
* test_auto_trading_high_findings_remediation.py
* test_auto_trading_arm_control.py
* New in-flight race tests
* Execution safety tests
* Duplicate and idempotency tests
* Databento health tests
* LRE fail-closed tests
* Prop-guard tests
* TradersPost payload tests
* Restart tests
* Authentication tests
* Parity
* Scalp golden
* Dual-simulation smoke
* Production build
* Main Brain TypeScript validation

Required results:

* All targeted audit, remediation, and arm tests pass
* Zero live outbound broker calls
* Zero test-order pollution
* Zero unauthenticated state changes
* Zero disarm-race transmissions
* Zero expired-session transmissions
* Zero old-session candidate transmissions
* Existing protected-position tests pass

12. Publish and production validation

Only after all required tests pass:

* Commit the closure fixes
* Restart
* Publish
* Confirm the deployed revision matches HEAD
* Confirm production starts disarmed
* Confirm Main Brain shows the same backend state
* Arm in a transport-mocked or paper-only validation mode
* Confirm countdown and limits
* Disarm and confirm immediate backend state
* Restart and confirm disarmed
* Confirm zero live outbound calls

Capture authenticated evidence of:

1. Live-capable mode showing DISARMED
2. Arm confirmation modal
3. Armed state with countdown and limits
4. Manual disarm
5. Safety-locked state
6. Restarted application showing DISARMED

Do not display private credentials or webhook information.

13. Final report

Return:

* Branch
* Starting HEAD
* Final HEAD
* Published revision
* Commit-history reconciliation
* Configured execution mode
* Startup effective state
* Arm-control interface location
* Audit suite result
* Remediation suite result
* Arm-control suite result
* Race-test result
* Restart-test result
* Authentication-test result
* Existing-position test result
* Total tests passed
* Total tests failed
* Live outbound calls
* CRITICAL findings remaining
* HIGH findings remaining
* MEDIUM findings remaining
* Final verdict

Use one verdict:

* ARM CONTROL SAFE FOR PAPER VALIDATION
* ARM CONTROL SAFE FOR LIMITED LIVE PILOT
* ARM CONTROL NOT SAFE

Do not use ARM CONTROL SAFE FOR LIMITED LIVE PILOT unless:

* All 97 audit tests pass
* All 77 remediation tests pass
* All 74 arm-control tests pass
* All new race tests pass
* Direct execution-control routes are authenticated
* Production starts disarmed
* Old arm sessions cannot authorize new candidates
* Existing protective orders survive ordinary disarm
* Zero unresolved CRITICAL findings
* Zero unresolved HIGH execution-control findings

Do not enable real live execution during this task.