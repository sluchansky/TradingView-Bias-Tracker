TASK: Audit and fix the Main Brain Risk State field showing unavailable or blank.

PROBLEM

The Main Brain market-state strip displays:

RISK STATE: —

The operator needs to know whether risk is normal, reduced, blocked, daily-limit constrained, drawdown constrained, or unavailable.

Do not invent a new risk model.

Reuse the canonical production risk and prop-guard state.

STRICT INITIAL SCOPE

Begin audit-first.

DO NOT modify:

- risk thresholds
- daily loss limits
- drawdown calculations
- position sizing
- contract calculations
- prop rules
- strategy scoring
- readiness
- alerts
- TradersPost
- execution
- Databento
- polling

PART 1 — IDENTIFY THE CANONICAL SOURCE

Trace all existing risk-state sources, including:

- prop guard
- daily P&L
- daily loss limit
- trailing drawdown
- max contracts
- current exposure
- active-trade risk
- broker readiness
- execution lock
- emergency stop
- session risk controls

Determine which existing field should populate Main Brain Risk State.

Do not derive it solely from UI text.

PART 2 — TRACE THE DATA PATH

Trace:

risk engine / prop guard
→ backend state
→ API response
→ Main Brain adapter
→ frontend normalizer
→ Market State Strip

Provide exact file names, functions, and JSON paths.

Determine whether the problem is:

- source absent
- wrong field name
- null mapping
- stale state
- unsupported account mode
- frontend normalizer omission
- fallback hiding the reason

PART 3 — DEFINE DISPLAY STATES

Use existing canonical semantics to map into a concise operator state.

Examples only:

NORMAL
REDUCED
CAUTION
BLOCKED
DAILY LIMIT
DRAWDOWN RISK
NO ACCOUNT DATA
UNAVAILABLE

Do not create a new trading rule.

PART 4 — ADD READ-ONLY DIAGNOSTICS

Expose enough supporting information to explain the state:

{
  "state": "CAUTION",
  "source": "prop_guard",
  "daily_pnl": -350,
  "daily_loss_limit": -1500,
  "remaining_daily_loss": 1150,
  "drawdown_remaining": 2800,
  "current_exposure": 0,
  "max_contracts": 3,
  "execution_allowed": true,
  "blocked_reason": null,
  "updated_at": "..."
}

Use actual existing fields only.

Do not fabricate missing account data.

PART 5 — MAIN BRAIN DISPLAY

The top strip should show the concise state.

The detailed risk information should appear in either:

- the Trade Plan panel,
- Execution Status,
- or a compact tooltip/detail drawer.

Examples:

RISK STATE
NORMAL

or:

RISK STATE
CAUTION
$420 remaining before daily lock

If unavailable, show:

RISK DATA UNAVAILABLE

not merely `—`.

PART 6 — TESTS

Verify:

- normal state
- reduced-risk state
- daily-limit warning
- hard block
- missing account data
- stale risk state
- execution disabled
- active trade exposure
- no frontend recalculation
- other Main Brain fields unchanged

VALIDATION

Run:

- risk/prop-guard tests
- sizing tests
- Main Brain tests
- execution-status tests
- TypeScript typecheck
- production build
- git diff --check

FINAL RESPONSE REQUIRED

Provide:

- exact root cause
- canonical risk source
- files changed
- states implemented
- current live risk state
- tests
- TypeScript/build results
- screenshot
- confirmation that no risk rule or threshold was changed