# Memory Index

- [api-server proxy route whitelist](proxy-route-whitelist.md) — Flask routes must be added to the Express `/api` proxy whitelist or they 404 before reaching Flask; how to debug 404s on this stack.
- [Express /api proxy must forward RAW body](api-proxy-raw-body.md) — proxy must buffer raw bytes + forward client's original content-type; express.json() drops TradingView text/plain webhooks → Flask "BODY: <empty>"/"Unrecognized alert type: ''" → dashboard "0 evaluations" (looks like a broken publish).
- [SCALP/SWING trading mode](trading-mode-scalp-swing.md) — webhook scoring has two sensitivity profiles via cfg(); invariants (SWING unchanged, Attempts reduced-size at every sizing site, MGC/MNQ string symmetry) any scoring change must keep.
- [Strict trade ruleset](strict-trade-ruleset.md) — READY gate MODE-TUNABLE: SWING zone+vwap+structure@80; SCALP demotes ZONE ONLY (GATE_REQUIRE_ZONE False; vwap+structure stay hard), Edge bands 40/50/60 (setup-building/half-size EARLY/full READY); alert_level≠conviction_tier; recency-aware conflict; every WAIT names failed gate(s); ticker-authoritative.
- [full_analysis single return path](full-analysis-return-parity.md) — one return dict now; hard-indexed consumers make a missing key a state-dependent 500; mirror keys if an early return is re-added.
- [Zone Mitigated detection](zone-mitigated-detection.md) — mitigation state is PER-INSTRUMENT (no global flag) w/ SCALP TTL; broken/consumed zone blocks ONLY in SWING — SCALP fully demotes zone at EVERY site (gate, full_analysis override, webhook short-circuit, setup-state invalidation, structure-reset clear), all cfg-guarded; demoting a gate flag alone LEAKS.
- [ET display vs UTC storage](timezone-display-et.md) — timestamps stored UTC, displayed in America/New_York via fmt_et(); Discord embeds' native `timestamp` field must stay ISO-UTC (Discord localizes per viewer).
- [full_analysis data quirks & card seam](analysis-data-quirks.md) — vwap_status is freshness NOT direction (derive above/below from price vs vwap_value); _build_card_entry is the single source for journal+card; screenshots are passed to Discord never fetched; analytics terminal-only+deduped.
- [Per-instrument dashboard view](per-instrument-dashboard-view.md) — MGC/MNQ tabs switch displayed analysis via /status?ticker → full_analysis(ticker_override); per-instrument price/VWAP/price-context invariants.
- [VWAP auto-fetch](vwap-auto-fetch.md) — VWAP auto-sourced (MGC≈GC=F, MNQ≈NQ=F); chart/manual push wins a grace window then auto resumes; gate never trades on stale VWAP.
- [Live alert trade-card](live-alert-card.md) — clean card is the single alert format for journal + main channel; fires once per READY setup + re-posts every TRADE_READY_INTERVAL; per-instrument throttle prevents instant+periodic double-post.
- [Production deployment topology](deployment-topology.md) — deploy as one Reserved VM; api-server prod build/run installs Flask deps + supervises Flask+Express; static `home` artifact serves `/` (publish needs a root); target chosen in Publish UI.
- [App-side DB convention: INSERT/SELECT only](db-app-insert-select-only.md) — app.py runs NO DDL (zero CREATE TABLE); boot does a no-DDL readiness probe + `*_DB_READY` flag; new tables created via database tool (dev) + Publish schema-diff (prod), like the learning engine.
- [Unified Edge Score](unified-edge-score.md) — transparent score is the only user-visible Edge Score; legacy is ranking-only; zone-broken blocker must be instrument-scoped or it zeros the other instrument.
- [Edge Score, grades & session bonus](edge-score-card-block.md) — Edge = ONE shared helper; EDGE_COMPONENTS BOS20/CHOCH20/VWAP15/Sweep15/Volume15/CVD15/Session10 = max110 (zone & candle NO LONGER score; RVOL is NOT a modifier) backing BOTH gate & display; grade ≥85A+/≥70A/≥50B/<50 WAIT; risks are warnings only; never fabricate a label. OPERATOR GOTCHA: gate booleans (zone/vwap/struct=Y) ≠ READY — zone scores 0, so if CHOCH never fires edge caps ~50 (BOS+VWAP+Volume) and "all gates green" sits below threshold forever; bias/structure flip only on inbound demand-vs-supply structure alerts (one-sided TV stream = stuck bias, not a bug).
- [CVD hard filter + RVOL→Volume component](cvd-rvol-filter.md) — CVD = HARD fail-open directional veto (reject long-on-bearish/short-on-bullish, +15 on agreement, no-op when unknown → SWING unchanged); RVOL feeds the Volume +15 component (≥~1.5), NOT a standalone ± modifier; ceiling is 110; display via alert_diagnostics.
- [Backtest optimization study](optimization-study.md) — research sweep simulates ONLY strategy×management; the other 5 dims are post-hoc subsets; BT score MUST mirror live EDGE_COMPONENTS (max110) or score/grade rankings diverge; best_overall falls back when no combo meets min_trades.
- [No-signals = structure gate](no-signals-alert-config-gap.md) — "0 signals" usually = no structure alert; structure = ANY-ONE of CHOCH/BOS/HH/HL/LH/LL (shared, un-prefixed, need a `ticker`); diagnose via `gate_debug`/`strict_reason` + the scored "Alert:" `Gate:` field.
- [Dashboard auth edge & open paths](dashboard-auth-edge.md) — dashboard/trade auth lives in Express (proxy strips cookies/headers); never lock /, /ping, /webhook, /healthz; Basic Auth + same-origin CSRF (XFH/Host proxy-trusted), fail-closed in prod, manual enter→/enter; webhook ENTER/CLOSE intentionally OPEN (TradingView can't send a password) + no real broker execution (Discord only).
- [Volatility monitor gate](volatility-monitor-gate.md) — per-instrument ATR-ratio layer, FAIL-OPEN; mode-split via VOL_HARD_GATE: SWING hard-gates BLOCK→WAIT via `blocked` (score_adj=0); SCALP volatility is DISPLAY/DIAGNOSTIC ONLY — `_vol_regime_score_adj` is NOT wired into the Edge (the `vol_adj` read in evaluate_strict_setup is dead), so it never blocks/docks a SCALP setup; single get_volatility feeds gate(SWING)+display; diag shows ATR/baseline/multiplier/threshold/decision.
- [Dashboard "not updating" = quiet data](dashboard-liveness-vs-stale-data.md) — 3s poll w/ silent catch; prove it via PROD logs (200s every 3s) before blaming the timer; freeze is usually stale price (no TV alerts)/WAIT verdict; "Last updated" clock now distinguishes.
- [1-min repeat alert diagnostics](one-min-repeat-diagnostics.md) — dedup/setup-state/latency are counting+display ONLY, never touch the trade decision; dedup never suppresses dispatch (downstream throttles do); INVALIDATED/EXPIRED intentionally re-form after the condition clears.
- [Observability / eval-metrics heartbeat](observability-eval-metrics.md) — diagnostic heartbeat re-eval + counters; must stay full_analysis+_record_eval_metrics only (no Discord/journal); COUNTERS_LOCK never inside EVAL_METRICS_LOCK; WAIT reason is `strict_reason` not `reason`.
- [EARLY intrabar pre-READY alert](early-pre-ready-alert.md) — additive display-only ⚡EARLY fires on sweep+structure before candle-close; never touches gate; fire-once dedupe (ambiguity PRESERVES anchors), READY owns the signal, stale per-instrument READY must not taint diagnostics.
- [Deployment log interpretation](deployment-log-interpretation.md) — Flask INFO shows as `[Error]` (stderr); boot-window `/api` 500s are benign cold-start (Express not yet bound), distinct from the in-app 502 (Express up, Flask down).
- [Tiered alerts & webhook-tail latency](tiered-alerts-and-webhook-tail-latency.md) — alert_level is display-only (SWING parity); any Discord POST in the /webhook tail must go through _enqueue_slow, not inline (only the READY live card sends inline).
- [Eval diagnostics & alert-before-journal ordering](eval-diagnostics-and-alert-ordering.md) — webhook worker sends trade alert BEFORE journal Discord embed (embed deferred to slow-task worker via post_discord=False); thread-local _timed() is a no-op for read-only callers; EVAL_METRICS deque needs a lock for list() snapshots; /eval-metrics + /diagnostics-live are owner-only.
- [Per-gate webhook diagnostics](gate-diagnostics.md) — owner-only /diagnostics shows per-gate PASS/FAIL + real "Blocked by" for every scored webhook (the authoritative "why WAIT"); observability-only, candidate-side-only, no-VWAP candidate can default opposite the alert (not a bug).
- [Dev/prod share Discord secrets](dev-prod-shared-discord.md) — secrets shared dev↔prod, so unconditional notifiers double-fire; gate time-based Discord schedulers to the live instance via DISCORD_LIVE_ENABLED (REPLIT_DEPLOYMENT/DISCORD_LIVE).
- [Per-direction dashboard toggle](per-direction-dashboard-toggle.md) — Long/Short toggle shows both bull & bear cases via result["directions"]; additive/display-only, favored side reuses authoritative edge for parity, non-favored never floored, conflict zeros both.
- [Confirmation Candle = every-bar noise](confirmation-candle-every-bar.md) — the TV indicator fires CONFIRMATION webhooks on every 5m bar & can't be filtered TV-side; confirmation must be timestamp-anchored >= latest same-dir BOS/CHOCH, never bare presence.
- [Zone presence from data not strings](zone-presence-not-from-strings.md) — Zone string defaults to "{word} intact" w/ no zone; Edge Score/notes must gate on nearest_demand/supply, never substring-match the display string (caused phantom "Demand Zone Active" +5).
- [Curated endpoint serialization](curated-endpoint-serialization.md) — /status & peer read endpoints whitelist keys, not jsonify(full_analysis); a new field needs adding to each route's dict too or it's None on the wire despite being computed.
- [Dashboard display modules: equity & news](display-modules-equity-news.md) — equity curve (today, no backfill) + ForexFactory news are /status-fed & DISPLAY-ONLY; news must NEVER feed the gate without an explicit money-path decision.
- [Dashboard ENTER → execution gateway](dashboard-enter-broker-bridge.md) — ENTER drives ONE configurable gateway via audited /traderspost; gatewayEligible (execution_enabled, no typed price, snapshot=selected tab, READY) → status sent/simulated/manual_required; send-before-track, abort if not 'sent'.
- [Market-session (CME/COMEX) awareness](market-session-hours.md) — MNQ/MGC pause on weekend + daily 17-18 ET halt + US exchange holidays (full-day & ~13:00 ET half-days), all inside market_session_status; closed-override runs LAST in full_analysis & appends all keys; new override goes before it.
- [EARLY tier, score-aware conflict & alert_diagnostics](early-tier-and-alert-diagnostics.md) — SCALP-only: two-floor tier via cfg knobs (now collapsed: SCALP 75/75 → READY-or-WAIT, no EARLY); score-aware conflict; single alert_diagnostics block feeding /status+Discord+dashboard modules (incl. CVD/RVOL); SWING parity via knobs; use verdict helpers not literal strings.
- [Dashboard potential-plan preview](dashboard-potential-plan-preview.md) — forming-setup entry/stop/TP preview is display-only in directions[*].potential_plan; broker/money path keys ONLY off actionable verdict + top-level trade_plan, never the preview.
- [Execution gateway (EXECUTION_MODE)](traderspost-order-safety.md) — /traderspost is now ONE configurable gateway (manual_only/paper/traderspost/pickmytrade) via canonical intent + per-provider adapters; traderspost payload byte-equivalent; paper/manual never send/dedupe; all fail-closed money invariants preserved.
- [TradersPost connectivity probe](traderspost-connectivity-probe.md) — verify TRADERSPOST_WEBHOOK_URL by POSTing a no-action/no-ticker payload; HTTP 400 invalid-payload = password VALID, `invalid-password` = bad last URL segment, curl HTTP 000 = transient OR trailing-whitespace (app .strip()s it so 000 ≠ broken); probe the STRIPPED value, never print the secret.
- [Focus vs Mute decoupling](focus-vs-mute-decoupling.md) — dashboard focus = display-only/per-device/localStorage; mute = server-side/global `/alerts/mute`; never cross-write; mute is in-memory (restart→all-unmuted is the fail-safe); only NEW-SETUP alerts muted, never live-position lifecycle.
- [Astral emoji \\u surrogate-escape 500](emoji-surrogate-escape-bug.md) — emoji as \\uXXXX surrogate-pairs passes py_compile but 500s at UTF-8 encode (use glyph/\U0001XXXX/BMP); a 500 here MASKS inline-JS bugs on the same route — HTTP-smoke alone isn't enough.
- [Multi-strategy engine](strategy-engine.md) — regime→strategy (fixed priority); ORB replaced Exhaustion Fade in LIVE engine; display|control mode; display's ONLY money-path effect = sanctioned ORB 1:4 retarget (gated engine.ready); control runs ALL global safety after; closed-override needs key parity; OHLC approximated.
- [Adaptive Learning Engine](adaptive-learning-engine.md) — Postgres per-strategy analytics is DISPLAY-ONLY (never gate/sizing/dedupe/traderspost), strictly fail-open, weights bounded 0.65–1.35 & never disable; best-hours bucket on opened_at (entry hour), recompute serialized by a mutex, persist after the outcome card, no in-app DDL.
- [Learning Engine v2](learning-engine-v2.md) — Governor/Memory/25-trade report are display-only; sole money-path effect is a flag-gated demote-only veto (READY→WAIT, default OFF, mirrors trade_debate); governor uses RAW win-rates (no ±15 double-count).
- [Read-only backtesting engine](backtest-engine.md) — /backtest/* is walled off from the money path; "INSERT/SELECT only" = no runtime DDL + no live-table mutation (its own UPDATE/DELETE are fine); worst-case fills incl runner→BE same bar; large express.raw scoped to upload path only; owner-only auth; honest "unavailable" agreement.
- [Dashboard inline-JS backslash-escape bug](dashboard-js-string-escape-bug.md) — single \n/\t/\r in the plain triple-quoted dashboard HTML string = raw newline = whole-script SyntaxError (all buttons+poll die); node --check the served <script>, py_compile misses it.
- [Backtest single-run management](backtest-single-run-management.md) — "really bad numbers" = BE-capped partial exit caps winners (~+0.5R) not lack of edge; fix via management selector (exit-only, entry unchanged) + MIN_TARGET_R ≤ 1.0 (1.5 zeroes MGC); never fake fills to loosen.
- [Per-instrument MUTE ALERTS](alerts-mute-per-instrument.md) — server-side in-memory flag suppresses ONLY NEW-SETUP Discord sends; engine/journal-record/tracking/diagnostics keep running, active-position lifecycle/outcome/execution NEVER muted; resolve instrument from ticker OR instrument OR alert_type (not ticker alone), unknown→not-muted; senders that drive accounting must return a dispatched bool.
- [Auto-trade arming lifecycle & "no trades" diagnosis](auto-trade-arming-lifecycle.md) — AUTO arm is in-memory per-instrument, resets OFF on every restart/republish + only fires live on the published instance; "bot not taking trades" right after a publish ≈ not re-armed (check /auto-trade enabled map); arming is intentionally NON-persistent (user declined boot auto-arm for safety) — don't add persistence without re-asking.
- [Dashboard lands on best-probability setup](dashboard-landing-best-setup.md) — on page load autoSelectBestSetup() picks instrument+direction by: actionable (READY/EARLY) beats non-actionable, then higher edge_score, else MGC/Long; DISPLAY-ONLY, once-on-load, respects focus, never overrides a manual pick (userPickedSetup), fail-open; no peer endpoint (2× /status).
- [Dashboard media via inlined data URI](dashboard-media-data-uri.md) — add dashboard audio/images by base64-injecting a data URI into the HTML via html.replace() (like __EDGE_MAX__), NOT a Flask route; dodges proxy whitelist + dashboard-auth + binary-proxy. Trade-taken bell = ACTIVE_TRADE opened_at transition.
- [SCALP dynamic exits](scalp-dynamic-exits.md) — SCALP TP1/TP2/runner+delayed-BE replaces 1:1; LOCAL/paper lifecycle on MANAGED_TRADES (broker sends ONE TP); paper auto tracked by managed trade not ACTIVE_TRADE (Option C); one shared geometry helper; ORB nulls nested keys; SWING/live byte-identical.
- [Fixed 1:1 (+ORB 1:4, $100 cap)](fixed-1to1-rr.md) — all live plans 1:1 EXCEPT a truly-ready ORB→1:4 via _apply_orb_target_override (gated on engine.ready; fallback ORB must NOT earn it); $100/trade ceiling skips(409) over-cap, clamp DOWN only; serialize prices :.2f; tracking re-derives t1=entry±rr_num*risk on every surface.
- [Dual-timeframe SCALP engine](dual-tf-engine.md) — flag-gated (DUAL_TF_ENGINE) 1m-bias+5s CONVERGENCE: READY = standing bias + ≥2 distinct aligned confirms (CVD/sweep/volume ONLY — VWAP/DELTA never count, NO entry-trigger) within 10s; flag-OFF byte-identical needs a dormant guard for REUSED names (bare sweeps) + store-CVD/volume-BEFORE-enqueue (worker re-derives full_analysis → race); gateway bypasses only Edge is_actionable/EARLY, all money safety shared, firing stays single-threaded; threading source kwarg into a gateway call needs the helper signature updated in lockstep or flag-OFF NameErrors.
- [Golden session-bonus pin](golden-session-bonus-flakiness.md) — scalp/swing goldens PINNED to a fixed 03:00 ET session (bonus 0), so clock-deterministic; a uniform ±10 score drift = the pin got reverted (restore it, don't rebaseline); parity is always clock-independent.
- [SCALP auto-execute trigger timing](scalp-auto-execute-trigger.md) — SCALP auto fires on LIVE is_actionable (EARLY tier 50-59 INCLUDED, half-size; gateway EARLY-skip backstop removed), not journal dedup; AUTO_FIRED_KEYS keys EARLY≡FULL same zone = one entry per setup; SCALP STACKS (allow_stack, no `not ACTIVE_TRADE`) bounded by daily cap; STOP_HIT re-arms (a WIN does NOT); single ACTIVE_TRADE tracks one position; SWING byte-identical.
- [Pine webhook source scripts](pine-webhook-source-scripts.md) — confirmation/sweep/volume/structure/cvd/zones/fvg_ob webhooks come from repo-owned Pine scripts that auto-detect the instrument & default unknowns to MGC; adding a contract needs editing them too, not just app.py's registry.
- [SWING HTF data layer](swing-htf-data-layer.md) — SWING overhaul: `_swing_htf_enabled()` master gate; auto-computed 1H/4H(resampled)/Daily bias+levels in HTF_STATE_BY_INST, folded into VWAP loop; compute_swing_context() stable schema fail-OPEN (gate fails CLOSED); SCALP+flag-off byte-identical via 3 goldens.
- [Per-instrument open-position isolation](per-instrument-active-trade-isolation.md) — ACTIVE_TRADES_BY_INST (one slot per instrument, RLock); management endpoints resolve via fail-closed _resolve_active_trade (explicit bad ticker→400, never act on the wrong trade); ALERT_HISTORY stays SHARED+widened (per-instrument would break the legacy mixed-instrument score), unbounded calculate_scores capped at last 100 for MGC byte-identity.
- [Per-asset safety controls](per-asset-safety-controls.md) — per-asset emergency/maxTrades-day/maxContracts/maxOpenTrades/maxDailyLoss/cooldowns layer via safety_cfg through ONE gateway; defaults are no-ops (MGC/MNQ byte-identical); money path is fail-CLOSED (unknown→block, never instrument_of MGC-default; maxOpen/maxContracts<1 hard-block; daily-loss uncomputable→block); AUTO_TRADE_LOCK is plain Lock so SAFETY_LOCK/snapshot must never nest under it.
- [Instrument rollout surfaces](instrument-rollout-surfaces.md) — adding a futures contract spans many lockstep surfaces (grep an existing symbol to find them); backtest BT_SPECS MUST byte-match live INSTRUMENT_SPECS or stop parity breaks; overlapping price bands (MES~MGC, MYM~MNQ) make price-only auto-detect fail closed → filename/dropdown is primary; goldens stay byte-identical for pure instrument-adds.
- [Per-instrument structure isolation](instrument-structure-isolation.md) — suspected cross-instrument BOS/CHOCH leak DISPROVEN (no code change); all 3 structure readers share one a_inst!=inst filter; display label/global bias are red herrings vs gate struct=Y; diagnose by replaying real webhooks + rebasing timestamps; guarded by instrument_isolation smoke.
- [Advisor auto-trade review gate](advisor-auto-trade-gate.md) — opt-in GLOBAL gate on AUTO exec only (default/restart OFF → goldens byte-identical); FAIL-CLOSED when ON via a `reviewed` marker in BOTH analyst returns (engine_enabled can't tell a real review from a fail-open fallback); never touches base gate/scoring/manual ENTER.
- [Analyst Reasoning Engine](analyst-reasoning-engine.md) — pro-analyst layer over the ASSEMBLED result (above strict → goldens free); display-first ON, veto OFF & only DEMOTES actionable→WAIT (recompute decision_support on fire); analyst risk reads rr_num NOT the rr display string; result["analyst"] on both open+closed paths; /status whitelisted; FVG/OB now a display-only `analyst` side that MUST short-circuit early in /webhook (else price/zone/_WEBHOOK_JOBS side-effects leak into the gate).
- [Cross-market index alignment](cross-market-index-alignment.md) — MNQ/MES/MYM directional-agreement is DISPLAY+NOTIFY only (gate untouched, goldens byte-identical); refresh runs dev+prod but Discord SEND gated on DISCORD_LIVE_ENABLED; only Aligned alerts (transition+cooldown dedup, mute-aware, channel-grouped, cooldown arms on 2xx only).
- [Trade Debate Engine](trade-debate-engine.md) — Bull/Bear/Judge mirrors pro_review (full_analysis-level, display-on, veto default-OFF); final_verdict TAKE iff actionable+decisive+aligned (≡ not veto_would_fire), else WAIT; veto only demotes; smoke-guarded not goldens.
- [Professional Review layer](pro-review-layer.md) — pre-READY pro-grader at full_analysis level (above goldens); per-instrument SCALP/SWING models + prime windows; display default; gated veto only DEMOTES actionable→WAIT; own smoke.
- [Unified Analyst Report](unified-analyst-report.md) — display-only thesis at the single full_analysis return path that CONSUMES (never recomputes) the analyst/debate/governor/memory/vol/news blocks; 15-min OPEN-position journal loop is DISCORD_LIVE_ENABLED-only; prev-conf re-baselines on |Δ|≥eps/TTL; early-exit hints must respect directional polarity.
- [Shared Trade Memory engine](shared-trade-memory.md) — find_similar_trades is the ONE similar-history source feeding governor + trade_memory + analyst.memory_review (compute ctx ONCE in full_analysis, never recompute); 4-lens governor w/ capped nudges + non-domination floor so history alone never vetoes; recency×version weights; goldens byte-identical (display-only).
- [Analyst professional game-plan](analyst-game-plan.md) — DISPLAY-ONLY nested analyst["game_plan"] (probabilistic plan/scenarios/R:R/conclusion), NOT a top-level block; fail-open, mirror schema in neutral block, flows to /status via wholesale analyst pass; own smoke not goldens.
- [Dashboard panel collapse + drag-reorder](dashboard-panel-layout-controls.md) — per-device display-only localStorage layer (dashCollapsed/dashOrder) over `#view-live .mod`; new panels MUST be `.mod`>`.mod-h`+stable id to inherit it; money-path untouched.
- [Request-logger redaction](request-logger-redaction.md) — the before_request logger echoes EVERY body unless the path is on a hardcoded allowlist; `_redact()` only masks keys named password/token (NOT signed URLs/tokens in other fields) — any new endpoint with a sensitive/free-form payload must add its own metadata-only branch.
- [Trade Idea Review (/review-idea)](trade-idea-review.md) — owner-only display-only feature grading a manually-typed trade by REUSING read-only engines; Option A = manual ticket only, NEVER a money path; transparent RR25/market30/entry25/memory10/conviction10 scoring; goldens byte-identical.
- [TradeZella integration](tradezella-integration.md) — imported journal feeds shared memory as a DOWN-WEIGHTED `source:"tradezella"` record (can't dominate live) + a pure display-only build_reviews presenter; review-only/INSERT-SELECT/owner-only; memory DB read stays OUTSIDE LEARNING_LOCK (only the swap is locked) — don't "fix" it.
- [Entry Quality location engine](entry-quality-engine.md) — 0-100 LOCATION scorer (≠strict gate); display-first flag-gated DEMOTE-ONLY veto (score<70 & not Edge≥90); ATR-extension uses MODE-CORRECT ATR (SCALP=1m≡unit, SWING=HTF 1H/4H/Daily, never 1m→mark unavailable); hard-trap DOWN-only cap so good room can't override bad location; absence-of-bad subs default 1.0; SCALP-only smoke guards veto wiring.
- [Broker payload pre-send guard](broker-payload-presend-guard.md) — every LIVE broker POST is audit-logged (redacted exact JSON, never the URL/secrets) + required-field checked (TP ticker/action, PMT symbol/data); invalid → local reject (no send) + dashboard panel; fail-closed, valid payload never mutated.
